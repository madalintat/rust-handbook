{"unit": "04-control-flow", "questions": [{"n": 1, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let retries = 3;\nif retries {\n    println!(\"retrying\");\n}</code></pre></div>", "options": [{"key": "A", "text": "Yes, a non-zero integer is true", "correct": false}, {"key": "B", "text": "No, the condition must have type <code>bool</code>", "correct": true}, {"key": "C", "text": "Yes, but it warns about an implicit conversion", "correct": false}, {"key": "D", "text": "No, <code>if</code> requires an <code>else</code>", "correct": false}], "answer": "B", "why": "<p><code>error[E0308]: expected bool, found integer</code>. Rust gives no value a truth value: not <code>0</code>, not <code>\"\"</code>, not an empty <code>Vec</code>, not <code>None</code>. The condition of an <code>if</code> is a <code>bool</code> or it is a type error.</p><p>A is the C and Python intuition, and it is the reason <code>if (fd)</code> is a bug for file descriptor zero and <code>if items:</code> silently conflates \"empty\" with \"missing\". You write <code>retries &gt; 0</code>, <code>!items.is_empty()</code> or <code>opt.is_some()</code>: three different questions that no longer share one spelling.</p>"}, {"n": 2, "stem": "<p>What is <code>n</code> at the end?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let mut i = 1;\nlet n = loop {\n    i *= 2;\n    if i &gt; 20 {\n        break i;\n    }\n};</code></pre></div>", "options": [{"key": "A", "text": "<code>()</code>; loops do not produce values", "correct": false}, {"key": "B", "text": "20", "correct": false}, {"key": "C", "text": "32", "correct": true}, {"key": "D", "text": "16", "correct": false}], "answer": "C", "why": "<p><code>loop</code> is an expression, and <code>break i</code> is its <code>return</code>. The doubling goes 2, 4, 8, 16, 32; the test fires at 32, which is the first value over 20 and the value the loop produces.</p><p>D is the trap: the check happens <em>after</em> the multiplication, so 16 never reaches the comparison as a candidate. The loop tests 16, finds it is not over 20, and goes round once more.</p>"}, {"n": 3, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let mut n = 0;\nlet found = while n &lt; 10 {\n    n += 1;\n    if n == 4 {\n        break n;\n    }\n};</code></pre></div>", "options": [{"key": "A", "text": "Yes, <code>found</code> is 4", "correct": false}, {"key": "B", "text": "No, a <code>while</code> loop cannot <code>break</code> with a value", "correct": true}, {"key": "C", "text": "No, <code>n</code> was moved into the loop", "correct": false}, {"key": "D", "text": "Yes, but <code>found</code> is always <code>()</code>", "correct": false}], "answer": "B", "why": "<p><code>error[E0571]: can only break with a value inside loop</code>. A <code>while</code> has two ways to finish: your <code>break</code>, or the condition testing false. If <code>break n</code> produced a value, the compiler would need a second value for the other path and there is nowhere to get one.</p><p><code>loop</code> has exactly one exit, a <code>break</code> you wrote, so every exit can carry a value and they must all agree on its type. That is what makes <code>loop</code> an expression and <code>while</code> and <code>for</code> statements.</p>"}, {"n": 4, "stem": "<p>What does this print?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>for i in (1..=3).rev() {\n    print!(\"{i} \");\n}</code></pre></div>", "options": [{"key": "A", "text": "<code>1 2 3</code>", "correct": false}, {"key": "B", "text": "<code>3 2 1</code>", "correct": true}, {"key": "C", "text": "<code>2 1 0</code>", "correct": false}, {"key": "D", "text": "It does not compile; a range cannot be reversed", "correct": false}], "answer": "B", "why": "<p><code>1..=3</code> yields 1, 2, 3, and <code>.rev()</code> reverses that.</p><p>C is the tempting one: it is what <code>(0..3).rev()</code> prints, and the difference is <code>..=</code> including its end where <code>..</code> excludes it. That is also why <code>..</code> is the default: <code>a..b</code> always has length <code>b - a</code>, and <code>0..v.len()</code> is exactly the valid indices.</p><p>D misses that a range is an ordinary struct implementing <code>Iterator</code>, so every adapter is available on it: <code>.rev()</code>, <code>.step_by()</code>, <code>.map()</code>.</p>"}, {"n": 5, "stem": "<p>Which of these are legal after <code>in</code> in a <code>for</code> loop? Choose all that apply.</p>", "options": [{"key": "A", "text": "<code>0..5</code>", "correct": true}, {"key": "B", "text": "<code>0..=5</code>", "correct": true}, {"key": "C", "text": "<code>..5</code>", "correct": false}, {"key": "D", "text": "<code>&amp;v</code> where <code>v: Vec&lt;String&gt;</code>", "correct": true}, {"key": "E", "text": "<code>\"hello\"</code>", "correct": false}], "answer": "ABD", "why": "<p><code>for</code> accepts exactly one thing: a value implementing <code>IntoIterator</code>.</p><p><code>..5</code> is a <code>RangeTo</code>, and it has no start to count from, so it cannot be an iterator. It exists for slicing, as in <code>&amp;v[..5]</code>. <code>&amp;str</code> deliberately does not implement <code>IntoIterator</code> either, because a string could reasonably yield bytes, <code>char</code>s or grapheme clusters and Rust refuses to guess: you write <code>.chars()</code>, <code>.bytes()</code> or <code>.lines()</code>.</p><p><code>0..=5</code> is an iterator but carries an extra <code>bool</code> compared to <code>0..5</code>, because it has to remember whether it already yielded its end value. Otherwise <code>0..=u8::MAX</code> could never terminate.</p>"}, {"n": 6, "stem": "<p>What is the type of <code>x</code>?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let v = vec![String::from(\"a\"), String::from(\"b\")];\nfor x in &amp;v {\n    // ...\n}</code></pre></div>", "options": [{"key": "A", "text": "<code>String</code>", "correct": false}, {"key": "B", "text": "<code>&amp;String</code>", "correct": true}, {"key": "C", "text": "<code>&amp;mut String</code>", "correct": false}, {"key": "D", "text": "<code>&amp;Vec&lt;String&gt;</code>", "correct": false}], "answer": "B", "why": "<p><code>for x in &amp;v</code> calls <code>IntoIterator::into_iter(&amp;v)</code>, and the impl for <code>&amp;Vec&lt;T&gt;</code> yields <code>&amp;T</code>.</p><p>There are three impls and one character between them: <code>for x in v</code> consumes the vector and yields <code>String</code>, <code>for x in &amp;v</code> borrows and yields <code>&amp;String</code>, and <code>for x in &amp;mut v</code> borrows uniquely and yields <code>&amp;mut String</code>. This is the single most common place a newcomer moves a collection by accident.</p>"}, {"n": 7, "stem": "<p>Which of these leave <code>v</code> usable afterwards? Choose all that apply.</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let mut v = vec![1, 2, 3];</code></pre></div>", "options": [{"key": "A", "text": "<code>for x in &amp;v {}</code>", "correct": true}, {"key": "B", "text": "<code>for x in v {}</code>", "correct": false}, {"key": "C", "text": "<code>for x in &amp;mut v {}</code>", "correct": true}, {"key": "D", "text": "<code>for x in v.iter() {}</code>", "correct": true}, {"key": "E", "text": "<code>for x in v.into_iter() {}</code>", "correct": false}], "answer": "ACD", "why": "<p>B and E consume the vector: both call <code>into_iter</code> on it by value, which takes <code>self</code>.</p><p>A, C and D borrow. <code>v.iter()</code> is the explicit spelling of what <code>&amp;v</code> does in a <code>for</code> header, and <code>.iter_mut()</code> the spelling of <code>&amp;mut v</code>. Writing them out is sometimes clearer, and it is required as soon as you want to chain an adapter: <code>v.iter().filter(..)</code>.</p>"}, {"n": 8, "stem": "<p>What does this print?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>'outer: for i in 0..3 {\n    for j in 0..3 {\n        if i + j == 2 {\n            break 'outer;\n        }\n        print!(\"{i}{j} \");\n    }\n}</code></pre></div>", "options": [{"key": "A", "text": "<code>00 01 10 </code>", "correct": false}, {"key": "B", "text": "<code>00 01 </code>", "correct": true}, {"key": "C", "text": "<code>00 01 10 11 </code>", "correct": false}, {"key": "D", "text": "<code>00 </code>", "correct": false}], "answer": "B", "why": "<p>The pairs run 00 (sum 0, printed), 01 (sum 1, printed), 02 (sum 2, which breaks out of <em>both</em> loops). Nothing after that runs.</p><p>A is what a plain <code>break</code> would give: it would leave only the inner loop, so <code>i</code> would advance to 1 and <code>10</code> would print before <code>11</code> broke out again. The label is the whole difference, and it is why Rust gets by without <code>goto</code>. A label may only name a loop that lexically encloses the <code>break</code>, so control always moves outward and never sideways.</p>"}, {"n": 9, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn sign(n: i32) -&gt; &amp;'static str {\n    match n {\n        x if x &lt; 0 =&gt; \"negative\",\n        x if x == 0 =&gt; \"zero\",\n        x if x &gt; 0 =&gt; \"positive\",\n    }\n}</code></pre></div>", "options": [{"key": "A", "text": "Yes, the three guards cover every <code>i32</code>", "correct": false}, {"key": "B", "text": "No, guarded arms do not count towards exhaustiveness", "correct": true}, {"key": "C", "text": "No, you cannot put a guard on a binding pattern", "correct": false}, {"key": "D", "text": "Yes, but only because <code>x</code> is a catch-all binding", "correct": false}], "answer": "B", "why": "<p><code>error[E0004]: i32::MIN..=i32::MAX not covered</code>. Between them the guards do cover every integer, and the compiler will not try to prove that: a guard is arbitrary Rust that could call a function, read an atomic, or return <code>false</code> on Tuesdays.</p><p>So the rule is flat: <strong>a guarded arm never contributes to exhaustiveness.</strong> A <code>match</code> whose arms all carry guards always needs one more that does not. That restriction is what keeps exhaustiveness decidable, and decidable exhaustiveness is what turns \"I added an enum variant\" into a list of every place that must change.</p>"}, {"n": 10, "stem": "<p>What does this return for <code>describe(300)</code>?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn describe(len: usize) -&gt; String {\n    match len {\n        n @ 0..=99 =&gt; format!(\"short {n}\"),\n        n @ 100..=999 =&gt; format!(\"medium {n}\"),\n        n =&gt; format!(\"long {n}\"),\n    }\n}</code></pre></div>", "options": [{"key": "A", "text": "<code>\"medium\"</code>; <code>n</code> is not bound in that arm", "correct": false}, {"key": "B", "text": "<code>\"medium 300\"</code>", "correct": true}, {"key": "C", "text": "<code>\"long 300\"</code>", "correct": false}, {"key": "D", "text": "It does not compile; <code>@</code> needs a type annotation", "correct": false}], "answer": "B", "why": "<p><code>n @ pattern</code> matches the pattern <em>and</em> binds the matched value to <code>n</code>. So the second arm matches, <code>n</code> is 300, and the arm produces <code>\"medium 300\"</code>.</p><p>Without <code>@</code> the range arms would match without naming anything, and <code>{n}</code> in those two <code>format!</code> calls would be <code>error[E0425]: cannot find value n</code>. The third arm needs no <code>@</code> because <code>n</code> alone is already a binding pattern: it matches anything and names it.</p>"}, {"n": 11, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn port(raw: Option&lt;&amp;str&gt;) -&gt; u16 {\n    let Some(text) = raw else { 8080 };\n    text.parse().unwrap_or(8080)\n}</code></pre></div>", "options": [{"key": "A", "text": "Yes, the <code>else</code> block produces the fallback", "correct": false}, {"key": "B", "text": "No, the <code>else</code> block of a <code>let ... else</code> must diverge", "correct": true}, {"key": "C", "text": "No, <code>let else</code> only works on <code>Result</code>", "correct": false}, {"key": "D", "text": "Yes, but <code>text</code> is only in scope inside the <code>else</code>", "correct": false}], "answer": "B", "why": "<p><code>error[E0308]: else clause of let...else does not diverge</code>, expected type <code>!</code>.</p><p>Think about what happens after that block finishes. The next line names <code>text</code>, and <code>text</code> was never bound, because the pattern did not match. So the block is not allowed to fall through: it must <code>return</code>, <code>break</code>, <code>continue</code> or panic. <code>else { return 8080; }</code> compiles.</p><p>D inverts the actual feature. The point of <code>let else</code> over <code>if let</code> is precisely that the binding escapes into the rest of the function, which is what lets the happy path run straight down the left margin.</p>"}, {"n": 12, "stem": "<p>Which are true of Rust's <code>match</code> but not C's <code>switch</code>? Choose all that apply.</p>", "options": [{"key": "A", "text": "Arms never fall through into the next one", "correct": true}, {"key": "B", "text": "The compiler rejects it if some case is unhandled", "correct": true}, {"key": "C", "text": "It is an expression, so it produces a value", "correct": true}, {"key": "D", "text": "Only integers and characters can be matched", "correct": false}, {"key": "E", "text": "Arms are tried in order, first match wins", "correct": false}], "answer": "ABC", "why": "<p>A, B and C are the differences. No fallthrough means the C bug of a missing <code>break</code> silently running the next case is not expressible. Exhaustiveness turns a new enum variant into a compile error listing every place that must change. And being an expression is why <code>let x = match .. { }</code> reads naturally.</p><p>D is backwards. <code>match</code> works on any type and on structure: tuples, structs, enums, slices, ranges, or-patterns.</p><p>E is true of both, and worth knowing: a broad arm above a narrow one swallows it, and rustc warns <code>unreachable pattern</code> when it can prove that happened.</p>"}, {"n": 13, "stem": "<p>What does this print?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let mut stack = vec![1, 2, 3];\nwhile let Some(top) = stack.pop() {\n    print!(\"{top} \");\n}</code></pre></div>", "options": [{"key": "A", "text": "<code>1 2 3 </code>", "correct": false}, {"key": "B", "text": "<code>3 2 1 </code>", "correct": true}, {"key": "C", "text": "Nothing; the pattern never matches", "correct": false}, {"key": "D", "text": "It loops forever", "correct": false}], "answer": "B", "why": "<p><code>pop</code> removes from the end, so the values come off 3, 2, 1. When the vector empties, <code>pop</code> returns <code>None</code>, the pattern stops matching, and the loop ends.</p><p>D is the right instinct applied to the wrong code. <code>while let</code> re-evaluates its scrutinee every pass and nothing forces progress, so <code>while let Some(x) = v.first()</code> with no removal really does run forever. The reason this version terminates is that <code>pop</code> mutates the stack, not anything about <code>while let</code> itself.</p>"}, {"n": 14, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let bytes = 2000;\nlet size = if bytes &gt; 1024 { \"large\" };</code></pre></div>", "options": [{"key": "A", "text": "Yes, <code>size</code> is <code>\"large\"</code> or unset", "correct": false}, {"key": "B", "text": "No, <code>if</code> used as a value needs an <code>else</code>", "correct": true}, {"key": "C", "text": "No, <code>bytes</code> must be annotated", "correct": false}, {"key": "D", "text": "Yes, and <code>size</code> has type <code>Option&lt;&amp;str&gt;</code>", "correct": false}], "answer": "B", "why": "<p><code>error[E0317]: if may be missing an else clause</code>, expected <code>&amp;str</code> and found <code>()</code>. On the false path there is no block at all, so the expression produces <code>()</code>, and a <code>let</code> needs a single type.</p><p>An <code>if</code> with no <code>else</code> is perfectly legal as a <em>statement</em>, where both paths are <code>()</code> and the value is discarded. Binding it is what forces both arms to produce a value of the same type.</p><p>D is a reasonable guess from other languages and Rust does not do it: no implicit wrapping into <code>Option</code>, no implicit <code>null</code>. If you want an <code>Option</code>, write <code>if .. { Some(\"large\") } else { None }</code>.</p>"}, {"n": 15, "stem": "<p>Which of these compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn a() -&gt; i32 { loop { } }\nfn b() -&gt; i32 { while true { } }</code></pre></div>", "options": [{"key": "A", "text": "Only <code>a</code>", "correct": true}, {"key": "B", "text": "Only <code>b</code>", "correct": false}, {"key": "C", "text": "Both", "correct": false}, {"key": "D", "text": "Neither", "correct": false}], "answer": "A", "why": "<p><code>loop</code> with no <code>break</code> never finishes, so its type is <code>!</code>, the never type, which coerces to anything, <code>i32</code> included. <code>a</code> type-checks.</p><p><code>while true</code> has type <code>()</code> no matter what is inside it, because the compiler does not evaluate the condition to decide whether the loop can end. <code>b</code> is <code>error[E0308]: expected i32, found ()</code>.</p><p>This is why clippy nudges <code>while true</code> towards <code>loop</code>, and it is not a style rule: the two constructs genuinely have different types. <code>loop</code> is also the only one of the three loops that can <code>break</code> with a value, for the same underlying reason: it has exactly one exit.</p>"}]}