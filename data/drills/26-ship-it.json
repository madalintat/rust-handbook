{"unit": "26-ship-it", "questions": [{"n": 1, "stem": "<p>A test in <code>tests/cli.rs</code> can call which of these? Choose all that apply.</p>", "options": [{"key": "A", "text": "A <code>pub fn</code> in <code>src/lib.rs</code>", "correct": true}, {"key": "B", "text": "A private <code>fn</code> in <code>src/lib.rs</code>", "correct": false}, {"key": "C", "text": "A <code>fn</code> in <code>src/main.rs</code>", "correct": false}, {"key": "D", "text": "The compiled binary, via <code>std::process::Command</code>", "correct": true}], "answer": "AD", "why": "<p>A file in <code>tests/</code> is compiled as its own crate that depends on your library, so it sees exactly what any external user sees: the public API of <code>lib.rs</code>.</p><p>C is the answer that catches people. <code>main.rs</code> is a separate crate with no library interface at all, so nothing can <code>use</code> anything from it. That is the mechanical reason for the bin/lib split: code left in <code>main.rs</code> is code no test can reach, except by running the whole binary as in D.</p>"}, {"n": 2, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>pub fn search(pattern: &amp;str, contents: &amp;str) -&gt; Vec&lt;&amp;str&gt; {\n    contents.lines().filter(|l| l.contains(pattern)).collect()\n}</code></pre></div>", "options": [{"key": "A", "text": "Yes, because elision ties the output to the first argument", "correct": false}, {"key": "B", "text": "No, because with two input references elision cannot choose", "correct": true}, {"key": "C", "text": "No, because <code>lines()</code> cannot be collected into a <code>Vec</code>", "correct": false}, {"key": "D", "text": "Yes, but the results borrow from <code>pattern</code>", "correct": false}], "answer": "B", "why": "<p><code>error[E0106]: missing lifetime specifier</code>. Elision has a rule for exactly one input reference; with two, rustc will not guess, because the choice is load-bearing. If the output borrowed from <code>pattern</code>, the caller could drop <code>contents</code> while still holding the results.</p><p>A describes a rule that does not exist for free functions. That one is for methods, where <code>&amp;self</code> wins. The fix here is <code>search&lt;'a&gt;(pattern: &amp;str, contents: &amp;'a str) -&gt; Vec&lt;&amp;'a str&gt;</code>: tie only the argument the slices are actually cut from.</p>"}, {"n": 3, "stem": "<p>Which of these belong on <strong>stderr</strong>? Choose all that apply.</p>", "options": [{"key": "A", "text": "The matching lines the user asked for", "correct": false}, {"key": "B", "text": "<code>could not read locked.txt: permission denied</code>", "correct": true}, {"key": "C", "text": "A progress bar", "correct": true}, {"key": "D", "text": "<code>--verbose</code> diagnostic output", "correct": true}, {"key": "E", "text": "The output of <code>--help</code> when the user asked for it", "correct": false}], "answer": "BCD", "why": "<p>Stdout is the tool's data; stderr is everything the user might want to see but a pipe must not receive. <code>minigrep foo *.txt | wc -l</code> has to count matches, so a warning on stdout corrupts the answer.</p><p>E is the deliberate exception and it is worth knowing: help <em>requested</em> with <code>--help</code> is what the user asked for, so it goes to stdout and can be piped into a pager. Help printed <em>because</em> the arguments were wrong is a diagnostic, and goes to stderr. <code>clap</code> already does both correctly.</p>"}, {"n": 4, "stem": "<p>With <code>#[derive(Parser)]</code> and this field, what does the command line look like?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>#[arg(short, long)]\npub ignore_case: bool,</code></pre></div>", "options": [{"key": "A", "text": "A positional argument that must be <code>true</code> or <code>false</code>", "correct": false}, {"key": "B", "text": "<code>-i</code> and <code>--ignore-case</code>, both optional flags defaulting to false", "correct": true}, {"key": "C", "text": "<code>--ignore_case</code> only, with the underscore preserved", "correct": false}, {"key": "D", "text": "<code>-I</code> and <code>--ignore-case</code>", "correct": false}], "answer": "B", "why": "<p><code>short</code> takes the first letter of the field name and <code>long</code> takes the whole name with underscores turned into hyphens. A <code>bool</code> field becomes a flag: present is true, absent is false.</p><p>A is what you get if you <em>omit</em> the attribute. A bare <code>bool</code> field is a positional argument, and the tool then demands <code>minigrep pattern path true</code>, which is a confusing bug because it still compiles and still runs.</p>"}, {"n": 5, "stem": "<p><code>thiserror</code> and <code>anyhow</code>: which goes where?</p>", "options": [{"key": "A", "text": "<code>anyhow</code> in the library, <code>thiserror</code> in the binary", "correct": false}, {"key": "B", "text": "<code>thiserror</code> in the library, <code>anyhow</code> in the binary", "correct": true}, {"key": "C", "text": "Either, they do the same job", "correct": false}, {"key": "D", "text": "<code>anyhow</code> in both, since <code>thiserror</code> is only for async code", "correct": false}], "answer": "B", "why": "<p>They sit on opposite sides of the file boundary because the two halves want different things. A library's caller needs to <code>match</code> on what went wrong, so the library returns a concrete enum, and <code>thiserror</code> derives the <code>Display</code> and <code>Error</code> impls for it. <code>main</code> never matches; it prints and exits. So it wants one type that absorbs every error and carries human context, which is <code>anyhow::Error</code>.</p><p>C is tempting because both crates are \"error handling\". The distinction is not style: <code>anyhow::Error</code> is opaque, so a caller who gets one back cannot branch on it without downcasting.</p>"}, {"n": 6, "stem": "<p><code>run</code> returns <code>anyhow::Result&lt;usize&gt;</code> and <code>?</code> on a <code>Result&lt;_, SearchError&gt;</code> is rejected. What is missing?</p>", "options": [{"key": "A", "text": "<code>impl From&lt;SearchError&gt; for anyhow::Error</code>", "correct": false}, {"key": "B", "text": "<code>impl std::error::Error for SearchError</code>, which requires <code>Display</code> first", "correct": true}, {"key": "C", "text": "<code>#[derive(Clone)]</code> on <code>SearchError</code>", "correct": false}, {"key": "D", "text": "<code>SearchError</code> must be an enum, not a struct", "correct": false}], "answer": "B", "why": "<p><code>anyhow::Error</code> will hold any <code>E: std::error::Error + Send + Sync + 'static</code>. A plain enum of integers already satisfies the last three; <code>Error</code> is the one you have to write, and <code>Error</code> has <code>Debug + Display</code> as supertraits, so <code>Display</code> comes with it.</p><p>A is the intuition from ordinary <code>?</code> conversions, and it is worse than unnecessary. <code>anyhow</code> already provides that blanket impl, and yours would collide with it.</p>"}, {"n": 7, "stem": "<p><code>e</code> is an <code>anyhow::Error</code> from a failed <code>.context(\"could not read notes.md\")</code>. What does <code>format!(\"{e}\")</code> print?</p>", "options": [{"key": "A", "text": "<code>could not read notes.md: No such file or directory (os error 2)</code>", "correct": false}, {"key": "B", "text": "<code>could not read notes.md</code>", "correct": true}, {"key": "C", "text": "<code>No such file or directory (os error 2)</code>", "correct": false}, {"key": "D", "text": "The whole chain plus a backtrace", "correct": false}], "answer": "B", "why": "<p><code>{}</code> prints only the outermost message. <code>{:#}</code> prints the chain joined with colons, which is answer A. <code>{:?}</code> prints it multi-line with a backtrace if one was captured, which is the form <code>main</code> uses when it returns <code>anyhow::Result</code>.</p><p>The trap is shipping a tool that prints <code>{e}</code> and wondering why the error messages are useless: the <em>cause</em> is the half you dropped.</p>"}, {"n": 8, "stem": "<p>What is the cost of <code>println!</code> inside a loop over a million lines?</p>", "options": [{"key": "A", "text": "Nothing; the macro is compiled away", "correct": false}, {"key": "B", "text": "A million lock acquisitions on stdout, plus a flush policy you did not choose", "correct": true}, {"key": "C", "text": "A heap allocation per call", "correct": false}, {"key": "D", "text": "One system call per character", "correct": false}], "answer": "B", "why": "<p><code>println!</code> calls <code>io::stdout()</code>, which locks, writes and unlocks on every call. Taking the lock once with <code>let mut out = io::stdout().lock();</code> and using <code>writeln!</code> removes a million lock/unlock pairs, and is often several times faster in a tool whose whole job is printing lines.</p><p>The other half of that change is using <code>?</code> rather than <code>.unwrap()</code>. When the reader pipes your tool into <code>head -3</code>, the pipe closes early; unwrapping turns an ordinary broken pipe into a panic and a backtrace.</p>"}, {"n": 9, "stem": "<p>Which of these does <code>cargo test</code> run? Choose all that apply.</p>", "options": [{"key": "A", "text": "<code>#[test]</code> functions in <code>#[cfg(test)] mod tests</code> inside <code>src/lib.rs</code>", "correct": true}, {"key": "B", "text": "Every file in <code>tests/</code>, each as its own binary", "correct": true}, {"key": "C", "text": "Code examples inside <code>///</code> doc comments", "correct": true}, {"key": "D", "text": "Code examples inside <code>//</code> ordinary comments", "correct": false}], "answer": "ABC", "why": "<p>All three of A, B and C, and C is the one people forget. A fenced code block in a doc comment is compiled and executed as a test, so an out-of-date example is a failing build rather than a lie on a documentation page.</p><p>That is worth exploiting deliberately: write the example you wish existed, and the compiler keeps it honest for the life of the crate. Ordinary <code>//</code> comments are invisible to everything, which is precisely why they rot.</p>"}, {"n": 10, "stem": "<p>Inside <code>tests/cli.rs</code>, what is <code>env!(\"CARGO_BIN_EXE_minigrep\")</code>?</p>", "options": [{"key": "A", "text": "The name of the package, as a string", "correct": false}, {"key": "B", "text": "The absolute path to the built binary, filled in by cargo at compile time", "correct": true}, {"key": "C", "text": "A runtime lookup that fails unless <code>cargo build</code> ran first", "correct": false}, {"key": "D", "text": "The directory containing <code>Cargo.toml</code>", "correct": false}], "answer": "B", "why": "<p>Cargo sets that environment variable when compiling an integration test, so <code>env!</code> bakes the absolute path in at compile time and cargo guarantees the binary was built first. No path guessing, no <code>target/debug/</code> hard-coded, and it is correct under <code>--release</code> too.</p><p>C is the natural worry and it is unfounded. The dependency is part of the build graph, which is exactly why this is the right way to test a CLI.</p>"}, {"n": 11, "stem": "<p>Which of these measurably shrink a release binary? Choose all that apply.</p>", "options": [{"key": "A", "text": "<code>strip = true</code>", "correct": true}, {"key": "B", "text": "<code>lto = true</code>", "correct": true}, {"key": "C", "text": "<code>panic = \"abort\"</code>", "correct": true}, {"key": "D", "text": "<code>codegen-units = 1</code>", "correct": false}, {"key": "E", "text": "<code>debug = true</code>", "correct": false}], "answer": "ABC", "why": "<p><code>strip</code> removes the symbol table and often roughly halves the file. That is the biggest single win, and free apart from losing symbol names in backtraces. <code>lto</code> lets the linker discard code across crate boundaries. <code>panic = \"abort\"</code> deletes the unwinding tables and landing pads, at the price of <code>catch_unwind</code> and of tests that expect a panic.</p><p>D is about <em>speed</em>: it stops the parallel-codegen optimisation loss, and usually makes the binary very slightly larger. E adds debug info, which is the opposite of shrinking.</p>"}, {"n": 12, "stem": "<p>A tool computes the right answer under <code>cargo run</code> and a wrong one under <code>cargo run --release</code>. What is the most likely cause?</p>", "options": [{"key": "A", "text": "LTO reordered the code", "correct": false}, {"key": "B", "text": "An integer overflow, which panics in debug and wraps in release", "correct": true}, {"key": "C", "text": "<code>--release</code> uses a different edition", "correct": false}, {"key": "D", "text": "Undefined behaviour in safe code", "correct": false}], "answer": "B", "why": "<p>The default profiles disagree on exactly one runtime check: <code>overflow-checks</code> is on in debug and off in release. A subtraction that goes below zero panics loudly during development and silently wraps to a huge number in the binary you ship.</p><p>D would be a compiler bug, since safe Rust does not have undefined behaviour. The fix for B is to say what you meant: <code>checked_sub</code>, <code>saturating_sub</code> or <code>wrapping_sub</code> mean three different things and all three are explicit.</p>"}, {"n": 13, "stem": "<p><code>Args::parse()</code> is called inside a <code>#[test]</code>. What happens?</p>", "options": [{"key": "A", "text": "It returns the test binary's own arguments, which is usually harmless", "correct": false}, {"key": "B", "text": "It reads the test runner's arguments, fails to match them, and exits the process", "correct": true}, {"key": "C", "text": "It returns <code>Err</code>, which the test can assert on", "correct": false}, {"key": "D", "text": "It will not compile inside a test", "correct": false}], "answer": "B", "why": "<p><code>parse()</code> reads <code>std::env::args_os()</code>, which under <code>cargo test</code> means the test harness's own arguments, and on a mismatch it calls <code>std::process::exit</code>. That kills the test binary rather than failing a test, so you get a confusing abort with no useful output.</p><p>Use <code>parse_from(argv)</code> to supply the arguments yourself, or <code>try_parse_from</code> when you want the parse error back as a value. The general shape is the same as the bin/lib split: push the process boundary as far out as you can.</p>"}, {"n": 14, "stem": "<p>You published <code>minigrep 0.1.0</code> and it has a serious bug. What can you do?</p>", "options": [{"key": "A", "text": "Delete the version and republish <code>0.1.0</code> fixed", "correct": false}, {"key": "B", "text": "<code>cargo yank</code> it so nothing new resolves to it, and publish <code>0.1.1</code>", "correct": true}, {"key": "C", "text": "Overwrite it with <code>cargo publish --force</code>", "correct": false}, {"key": "D", "text": "Ask crates.io to rename the crate so you can start again", "correct": false}], "answer": "B", "why": "<p>Published versions are permanent. <code>yank</code> marks a version so that no <em>new</em> dependency resolution picks it, but it does not delete anything: existing lockfiles keep building, and they have to, or yanking one crate would break thousands of builds.</p><p>The other permanence worth knowing before your first <code>cargo publish</code>: the name is taken forever the moment you publish, which is why <code>--dry-run</code> exists and why reserving a name with a placeholder is a decision you cannot walk back.</p>"}, {"n": 15, "stem": "<p>Why return <code>ExitCode</code> from <code>main</code> rather than <code>anyhow::Result&lt;()&gt;</code>?</p>", "options": [{"key": "A", "text": "<code>Result</code> from <code>main</code> does not compile", "correct": false}, {"key": "B", "text": "<code>Result</code> gives you 0 or 1 only, and cannot express \"worked, found nothing\"", "correct": true}, {"key": "C", "text": "<code>ExitCode</code> prints the error more nicely", "correct": false}, {"key": "D", "text": "<code>Result</code> is slower, because it allocates", "correct": false}], "answer": "B", "why": "<p>Returning <code>Result</code> is a perfectly good default: on <code>Err</code> it prints the error with <code>{:?}</code> and exits 1. What it cannot express is a <em>successful</em> run with a non-zero status, and a search tool needs exactly that. <code>grep</code> has used 0 for found, 1 for not found and 2 for an error for forty years, and scripts branch on it.</p><p>So <code>main</code> returns <code>ExitCode</code>, matches on the library's <code>Result</code>, prints the error to stderr itself, and picks the number deliberately.</p>"}]}