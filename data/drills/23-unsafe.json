{"unit": "23-unsafe", "questions": [{"n": 1, "stem": "<p>Which of these does <code>unsafe</code> permit that safe Rust does not? Choose all that apply.</p>", "options": [{"key": "A", "text": "Dereferencing a raw pointer", "correct": true}, {"key": "B", "text": "Having two <code>&amp;mut</code> to the same value", "correct": false}, {"key": "C", "text": "Calling a function declared <code>unsafe fn</code>", "correct": true}, {"key": "D", "text": "Reading a field of a <code>union</code>", "correct": true}, {"key": "E", "text": "Moving out of a value that is borrowed", "correct": false}], "answer": "ACD", "why": "<p>The list is exactly five: dereference a raw pointer, call an unsafe function, implement an unsafe trait, access a <code>static mut</code>, read a union field. A, C and D are on it.</p><p>B and E are the tempting ones and they are the whole misconception. Aliasing rules and move rules are checked identically inside and outside an <code>unsafe</code> block \u2014 E0499 and E0505 fire either way. <code>unsafe</code> is not a borrow-checker override, and typing it in response to one of those errors means the message has not been read yet.</p>"}, {"n": 2, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let x = 42;\nlet p = &amp;x as *const i32;\nprintln!(\"{p:?}\");</code></pre></div>", "options": [{"key": "A", "text": "Yes \u2014 creating and printing a raw pointer is safe", "correct": true}, {"key": "B", "text": "No \u2014 any use of a raw pointer needs <code>unsafe</code>", "correct": false}, {"key": "C", "text": "No \u2014 <code>*const i32</code> does not implement <code>Debug</code>", "correct": false}, {"key": "D", "text": "Yes, but only because <code>x</code> is <code>Copy</code>", "correct": false}], "answer": "A", "why": "<p>Creating a raw pointer, casting one, comparing two and printing one are all safe, because none of them can cause harm. Only the <strong>dereference</strong> is gated, because only the dereference can read memory that is not there.</p><p>B is the intuition most people arrive with, and getting rid of it tells you where to look when auditing unsafe code: at the <code>*p</code>, never at the cast.</p>"}, {"n": 3, "stem": "<p>What is the runtime cost of an <code>unsafe</code> block?</p>", "options": [{"key": "A", "text": "A bounds check is skipped, so it is faster", "correct": false}, {"key": "B", "text": "None \u2014 it is a compile-time permission and emits no code", "correct": true}, {"key": "C", "text": "A small check that the block did not violate any invariants", "correct": false}, {"key": "D", "text": "It disables optimisations inside the block", "correct": false}], "answer": "B", "why": "<p><code>unsafe</code> generates nothing. It changes which operations the compiler will accept in that region and nothing else.</p><p>A is the seductive wrong answer. Unsafe code can be faster \u2014 <code>get_unchecked</code> really does skip a comparison \u2014 but that is the <em>function you chose to call</em>, not the keyword. Wrapping ordinary code in <code>unsafe</code> makes it exactly as fast as it was.</p>"}, {"n": 4, "stem": "<p>Which of these are true of <code>*const T</code> but not of <code>&amp;T</code>? Choose all that apply.</p>", "options": [{"key": "A", "text": "It may be null", "correct": true}, {"key": "B", "text": "It may point at freed memory", "correct": true}, {"key": "C", "text": "Nothing tracks what else points at the same place", "correct": true}, {"key": "D", "text": "It is <code>Copy</code>", "correct": false}, {"key": "E", "text": "It is one machine word wide", "correct": false}], "answer": "ABC", "why": "<p>A, B and C are the three guarantees a reference carries and a raw pointer does not, and together they are why dereferencing one needs a promise from you.</p><p>D and E are true of both, which is what makes them good distractors. <code>&amp;T</code> is <code>Copy</code> and a thin reference is one word, exactly like a <code>*const T</code>. On the machine they are the same bits; the difference is entirely in what the compiler is allowed to assume about them.</p>"}, {"n": 5, "stem": "<p>Why is undefined behaviour worse than a crash?</p>", "options": [{"key": "A", "text": "It is not \u2014 a crash is worse because the program stops", "correct": false}, {"key": "B", "text": "The optimiser may assume UB cannot happen and rewrite unrelated code on that assumption", "correct": true}, {"key": "C", "text": "It always corrupts the heap", "correct": false}, {"key": "D", "text": "It can only be found with a debugger", "correct": false}], "answer": "B", "why": "<p>UB is not a behaviour, it is the absence of a specification. The compiler optimises on the assumption that your program has none, so a null dereference does not merely fault \u2014 it licenses the optimiser to delete the null check three lines later as unreachable, and the crash then surfaces somewhere with no <code>unsafe</code> anywhere near it.</p><p>C is too specific: UB may corrupt nothing, may work perfectly in debug, and may change behaviour on the next compiler release. That instability is the point.</p>"}, {"n": 6, "stem": "<p>Does this compile, and is it sound?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>pub fn get(v: &amp;[i32], i: usize) -&gt; i32 {\n    unsafe { *v.get_unchecked(i) }\n}</code></pre></div>", "options": [{"key": "A", "text": "It does not compile \u2014 <code>get_unchecked</code> needs a bounds check", "correct": false}, {"key": "B", "text": "Compiles, and is sound because the <code>unsafe</code> block is explicit", "correct": false}, {"key": "C", "text": "Compiles, and is unsound \u2014 a safe caller can pass any index", "correct": true}, {"key": "D", "text": "Compiles, and is sound because <code>i32</code> has no invalid bit patterns", "correct": false}], "answer": "C", "why": "<p>It compiles perfectly, which is exactly the problem. The signature is safe, so <code>get(&amp;v, 900)</code> is an ordinary call with no warning attached, and it reads 3,600 bytes past the allocation.</p><p>An API is <strong>unsound</strong> when some safe caller can trigger undefined behaviour, and the bug belongs to whoever wrote the <code>unsafe</code>, not to the caller who found it. D is a real fact used as a red herring: the value read would indeed be a valid <code>i32</code>, but the <em>read itself</em> was out of bounds, and that is the UB.</p>"}, {"n": 7, "stem": "<p>What does <code>unsafe</code> on a function signature mean?</p>", "options": [{"key": "A", "text": "The body of the function contains unsafe operations", "correct": false}, {"key": "B", "text": "Callers must uphold preconditions the compiler cannot check", "correct": true}, {"key": "C", "text": "The function is exempt from borrow checking", "correct": false}, {"key": "D", "text": "The function may panic", "correct": false}], "answer": "B", "why": "<p><code>unsafe fn</code> pushes an obligation outward: every call site must promise something, which is why the promise belongs in a <code>/// # Safety</code> section.</p><p>A is the near-miss, and the distinction matters. A function full of unsafe operations that checks all its own preconditions should have a <strong>safe</strong> signature \u2014 that is what a safe abstraction is. <code>Vec::push</code> is stuffed with unsafe code and is not an <code>unsafe fn</code>.</p>"}, {"n": 8, "stem": "<p>Since edition 2024, what is true of the body of an <code>unsafe fn</code>?</p>", "options": [{"key": "A", "text": "It is an implicit <code>unsafe</code> block, as before", "correct": false}, {"key": "B", "text": "Unsafe operations inside it still need their own <code>unsafe</code> block", "correct": true}, {"key": "C", "text": "Unsafe functions can no longer be declared", "correct": false}, {"key": "D", "text": "The body is checked by Miri automatically", "correct": false}], "answer": "B", "why": "<p>The two claims are unrelated, so the language stopped conflating them. That your <em>callers</em> owe you a precondition says nothing about which of your own lines are sound, and treating the whole body as pre-approved hid real bugs in large unsafe functions.</p><p>Upgrading old code to edition 2024 therefore produces a wave of <code>error[E0133]</code> inside <code>unsafe fn</code> bodies. The fix is not to delete the error but to add the inner block and the <code>// SAFETY:</code> line that says why it holds.</p>"}, {"n": 9, "stem": "<p>Which types in the standard library are implemented with <code>unsafe</code> inside? Choose all that apply.</p>", "options": [{"key": "A", "text": "<code>Vec&lt;T&gt;</code>", "correct": true}, {"key": "B", "text": "<code>String</code>", "correct": true}, {"key": "C", "text": "<code>RefCell&lt;T&gt;</code>", "correct": true}, {"key": "D", "text": "<code>Rc&lt;T&gt;</code>", "correct": true}, {"key": "E", "text": "None \u2014 the standard library is entirely safe Rust", "correct": false}], "answer": "ABCD", "why": "<p>All four, and many more. <code>Vec</code> manages a raw allocation, <code>String</code> maintains a UTF-8 invariant over <code>Vec&lt;u8&gt;</code>, <code>RefCell</code> mutates through a shared reference via <code>UnsafeCell</code>, and <code>Rc</code> writes a refcount through an aliased pointer.</p><p>That is the point of the feature rather than an embarrassment. Each of them exposes an API where no safe call can cause undefined behaviour, so the unsafe code was written once, audited once, and every user gets the guarantee for free. Writing your own unsafe means making the same bargain.</p>"}, {"n": 10, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let x = 5u32;\nlet p: *mut u32 = &amp;x;\nunsafe { *p = 6; }</code></pre></div>", "options": [{"key": "A", "text": "Yes \u2014 <code>as</code> casts make any pointer conversion legal", "correct": false}, {"key": "B", "text": "No \u2014 <code>&amp;x</code> is a shared borrow and coerces only to <code>*const u32</code>", "correct": true}, {"key": "C", "text": "No \u2014 <code>x</code> is not declared <code>mut</code>", "correct": false}, {"key": "D", "text": "Yes, and it is sound", "correct": false}], "answer": "B", "why": "<p><code>error[E0308]: expected raw pointer *mut u32, found reference &amp;u32</code>. A <code>&amp;T</code> coerces to <code>*const T</code> and stops there.</p><p>C is a genuinely tempting reading, because <code>x</code> not being <code>mut</code> <em>is</em> also wrong \u2014 but the error you get is the type mismatch, and it would still be a type mismatch with <code>let mut x</code>. The deeper point is <span class=\"term\" data-g=\"The record of which allocation and which permission a pointer was derived from, which is part of whether using it is legal even when the address is right.\" data-t=\"provenance\">provenance</span>: a pointer derived from a shared borrow carries permission to read only, so writing through it is undefined behaviour even after an <code>as *mut u32</code> cast has silenced the compiler.</p>"}, {"n": 11, "stem": "<p>What is <code>#[repr(C)]</code> for?</p>", "options": [{"key": "A", "text": "Making a type compile faster", "correct": false}, {"key": "B", "text": "Pinning field order, alignment and padding to C's rules so another language can agree about the layout", "correct": true}, {"key": "C", "text": "Marking a type as unsafe", "correct": false}, {"key": "D", "text": "Removing padding from a struct", "correct": false}], "answer": "B", "why": "<p>Default <code>repr(Rust)</code> layout guarantees nothing: the compiler may reorder fields to shrink padding and may change that decision between releases. <code>#[repr(C)]</code> fixes the layout so a C header and a Rust struct describe the same bytes.</p><p>D is <code>#[repr(packed)]</code>, which is a different attribute with a different hazard \u2014 it produces misaligned fields, and taking a reference to one is a hard error because a reference promises alignment.</p>"}, {"n": 12, "stem": "<p>Why is <code>static mut</code> almost always the wrong tool?</p>", "options": [{"key": "A", "text": "Two threads touching it is a data race, which is immediate undefined behaviour", "correct": true}, {"key": "B", "text": "It is slower than an atomic", "correct": false}, {"key": "C", "text": "It cannot hold a heap-allocated value", "correct": false}, {"key": "D", "text": "It is deprecated and will be removed", "correct": false}], "answer": "A", "why": "<p>There is no synchronisation of any kind, so any concurrent access is a data race, and a data race is UB rather than a merely wrong number. Writing <code>unsafe</code> around it is you certifying that no two threads can ever reach that line \u2014 a promise you usually cannot make about your own library's callers.</p><p>B is backwards: <code>static mut</code> is nominally faster, which is exactly what makes it tempting. The menu that is actually correct: <code>OnceLock</code> for write-once, an atomic for a counter, <code>Mutex</code>/<code>RwLock</code> for anything larger, <code>thread_local!</code> when it need not be shared.</p>"}, {"n": 13, "stem": "<p>Reading a field of a <code>union</code> is unsafe because\u2026</p>", "options": [{"key": "A", "text": "Unions are only allowed in FFI code", "correct": false}, {"key": "B", "text": "Nothing tracks which field was last written, so the read may reinterpret the wrong type's bytes", "correct": true}, {"key": "C", "text": "Unions have no defined layout", "correct": false}, {"key": "D", "text": "It requires a heap allocation", "correct": false}], "answer": "B", "why": "<p>A union's fields overlap in one piece of storage. Reading <code>u.f</code> asserts that <code>f</code> is the field last written; if an <code>f32</code> was written and a <code>u32</code> is read, the bytes are reinterpreted, and for some types that produces a value the language says cannot exist.</p><p>The Rust answer to the same problem is an <code>enum</code>, which stores a discriminant so the compiler can check. Unions exist for C layouts and for <code>MaybeUninit</code> \u2014 cases where the discriminant is somewhere else or is not wanted at all.</p>"}, {"n": 14, "stem": "<p>Which of these is a genuine reason to write <code>unsafe</code>? Choose all that apply.</p>", "options": [{"key": "A", "text": "The borrow checker will not accept a correct-looking function", "correct": false}, {"key": "B", "text": "Calling into a C library", "correct": true}, {"key": "C", "text": "Implementing a data structure whose aliasing the checker cannot express, behind a safe API", "correct": true}, {"key": "D", "text": "A profile shows a bounds check in the hottest loop and the index is already proven", "correct": true}, {"key": "E", "text": "A lifetime error you cannot work out", "correct": false}], "answer": "BCD", "why": "<p>B, C and D are the real cases, and the last two come with obligations: a safe wrapper, a <code>// SAFETY:</code> comment, and Miri.</p><p>A and E are the same mistake twice. Borrow and lifetime errors are questions about ownership and about time, and <code>unsafe</code> cannot answer either \u2014 it does not even apply to them. The exception proves the rule: <code>split_at_mut</code> uses raw pointers precisely because the checker cannot reason about index ranges, but the result is a <em>safe</em> function that asserts <code>mid &lt;= len</code> first.</p>"}, {"n": 15, "stem": "<p>What does Miri do?</p>", "options": [{"key": "A", "text": "Proves an unsafe block is sound", "correct": false}, {"key": "B", "text": "Interprets the program and reports undefined behaviour along the paths your tests actually execute", "correct": true}, {"key": "C", "text": "Rewrites unsafe code into safe code", "correct": false}, {"key": "D", "text": "Checks unsafe code at compile time, like the borrow checker", "correct": false}], "answer": "B", "why": "<p>Miri runs the program at the MIR level and detects out-of-bounds pointer arithmetic, use-after-free, misaligned reads, aliasing violations, uninitialised reads and data races \u2014 things a normal test run will happily let past.</p><p>A overstates it, and the overstatement is worth naming: Miri only sees the code paths your tests take, so a clean run is evidence, not proof. It is still the single most effective thing you can do to unsafe code, and it routinely finds bugs in crates that have been in production for years.</p>"}]}