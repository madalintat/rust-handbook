{"unit": "13-generics", "questions": [{"n": 1, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn largest&lt;T&gt;(v: &amp;[T]) -&gt; &amp;T {\n    let mut best = &amp;v[0];\n    for x in v { if x &gt; best { best = x; } }\n    best\n}</code></pre></div>", "options": [{"key": "A", "text": "Yes, <code>&gt;</code> works on any type", "correct": false}, {"key": "B", "text": "No, <code>T</code> has promised nothing, so <code>&gt;</code> is unavailable", "correct": true}, {"key": "C", "text": "No, you cannot return a reference into a slice parameter", "correct": false}, {"key": "D", "text": "Yes, but only for numeric types", "correct": false}], "answer": "B", "why": "<p><code>&gt;</code> is not a primitive operation; it is <code>PartialOrd::gt</code> with punctuation for a name. A bare <code>T</code> stands for every type in the language, including closures and <code>File</code>, so the body is rejected at the definition. <code>T: PartialOrd</code> fixes it.</p><p>D is the tempting one, and it misreads how the check works. There is no \"numeric types\" case, and rustc does not look at your call sites at all when checking the body. It checks once, against the bounds, and here there are none.</p>"}, {"n": 2, "stem": "<p>A program calls <code>fn id&lt;T&gt;(x: T) -&gt; T</code> with <code>i32</code> twice, <code>String</code> once and <code>char</code> once. How many copies of <code>id</code> are in the binary?</p>", "options": [{"key": "A", "text": "1", "correct": false}, {"key": "B", "text": "3", "correct": true}, {"key": "C", "text": "4", "correct": false}, {"key": "D", "text": "0, it is resolved at runtime", "correct": false}], "answer": "B", "why": "<p>Monomorphisation emits one copy per <strong>concrete type</strong>, not per call site. Three distinct types were used, so three functions appear in the object file, each with the type baked in and independently optimisable.</p><p>C counts call sites, which is the natural guess and the wrong unit. A hundred calls with <code>i32</code> still produce one <code>id_i32</code>.</p>"}, {"n": 3, "stem": "<p>What does Rust pay for generics being free at runtime?</p>", "options": [{"key": "A", "text": "Nothing, it is free in every dimension", "correct": false}, {"key": "B", "text": "Compile time and binary size", "correct": true}, {"key": "C", "text": "A pointer indirection on every generic call", "correct": false}, {"key": "D", "text": "A hidden type tag on every generic value", "correct": false}], "answer": "B", "why": "<p>The duplication does not disappear; it moves into the compiler. Ten types means ten copies to type-check, optimise, link and ship, plus ten near-identical loops competing for instruction cache. That is the bill.</p><p>C and D describe the <em>other</em> mechanism, <code>dyn Trait</code>, which is one copy with a vtable pointer alongside the data and no inlining. Both designs exist in Rust because both trades are sometimes right.</p>"}, {"n": 4, "stem": "<p>Which of these need a trait bound on <code>T</code> to compile inside a generic function? Choose all that apply.</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn f&lt;T&gt;(a: T, b: T) { /* body varies */ }</code></pre></div>", "options": [{"key": "A", "text": "<code>println!(\"{a}\")</code>", "correct": true}, {"key": "B", "text": "<code>a == b</code>", "correct": true}, {"key": "C", "text": "<code>let c = a.clone();</code>", "correct": true}, {"key": "D", "text": "<code>let v = vec![a, b];</code>", "correct": false}, {"key": "E", "text": "<code>a + b</code>", "correct": true}], "answer": "ABCE", "why": "<p>A needs <code>Display</code>, B needs <code>PartialEq</code>, C needs <code>Clone</code>, E needs <code>Add</code>. Operators in Rust are trait methods, so <code>==</code>, <code>+</code>, <code>&lt;</code> and <code>[i]</code> are all bound-requiring calls in disguise.</p><p>D is the odd one out and worth noticing. Putting values into a <code>Vec&lt;T&gt;</code>, moving them, returning them, storing them in a struct: none of that requires anything of <code>T</code>. Ownership operations are available on every type; it is <em>behaviour</em> that must be promised.</p>"}, {"n": 5, "stem": "<p>Why does <code>let n = \"42\".parse().unwrap();</code> need an annotation?</p>", "options": [{"key": "A", "text": "Because <code>parse</code> can fail", "correct": false}, {"key": "B", "text": "Because <code>parse</code> is generic in its return type, and nothing on that line picks it", "correct": true}, {"key": "C", "text": "Because string literals have no type until bound", "correct": false}, {"key": "D", "text": "Because <code>unwrap</code> erases the type", "correct": false}], "answer": "B", "why": "<p><code>parse</code> is <code>fn parse&lt;F: FromStr&gt;(&amp;self) -&gt; Result&lt;F, F::Err&gt;</code>. <code>F</code> appears only in the return position, so there is no argument for inference to run backwards from, and <code>u8</code>, <code>f64</code>, <code>bool</code> and <code>IpAddr</code> are all valid answers.</p><p>The tempting reasoning is that integer literals default to <code>i32</code>, so surely this does too. That fallback applies to unconstrained <em>literals</em>, not to an unresolved generic parameter. Say it with <code>parse::&lt;i64&gt;()</code> or <code>let n: i64</code>.</p>"}, {"n": 6, "stem": "<p>A Java <code>ArrayList&lt;Integer&gt;</code> and a Rust <code>Vec&lt;i32&gt;</code> both hold a million integers. What is the structural difference?</p>", "options": [{"key": "A", "text": "None, both erase to a list of pointers", "correct": false}, {"key": "B", "text": "Java boxes each integer as a heap object behind a pointer; Rust stores the four bytes inline", "correct": true}, {"key": "C", "text": "Rust boxes each integer; Java stores them inline", "correct": false}, {"key": "D", "text": "Both store them inline, but Rust adds a type tag", "correct": false}], "answer": "B", "why": "<p>Java generics erase to <code>Object</code>, and <code>Object</code> cannot be a primitive, so each element becomes an <code>Integer</code> on the heap and the list holds pointers to them. Rust monomorphises <code>Vec&lt;i32&gt;</code> into a type that genuinely holds <code>i32</code>s in one flat allocation.</p><p>The trade is exactly opposite: Java keeps one class in the binary and pays at runtime in indirection and allocation; Rust emits a copy per element type and pays in binary size.</p>"}, {"n": 7, "stem": "<p>When does a C++ compiler report that a template's body cannot compare its elements, and when does rustc?</p>", "options": [{"key": "A", "text": "Both at the definition", "correct": false}, {"key": "B", "text": "Both at the first instantiation", "correct": false}, {"key": "C", "text": "C++ at instantiation, Rust at the definition", "correct": true}, {"key": "D", "text": "C++ at the definition, Rust at instantiation", "correct": false}], "answer": "C", "why": "<p>A C++ template is a pattern, type-checked only once a concrete type is substituted, so the error surfaces inside your template, on a line the caller never wrote, with an instantiation stack attached.</p><p>Rust checks the generic body <strong>once</strong>, against its declared bounds. The consequence is worth stating precisely: if a generic function compiles, it works for every <code>T</code> satisfying its bounds, and a bad call is one line at the call site saying which bound is missing.</p>"}, {"n": 8, "stem": "<p>What can a <code>where</code> clause express that <code>&lt;T: Bound&gt;</code> cannot?</p>", "options": [{"key": "A", "text": "Multiple bounds on one parameter", "correct": false}, {"key": "B", "text": "Bounds on types other than the declared parameters, such as <code>where Vec&lt;T&gt;: Debug</code>", "correct": true}, {"key": "C", "text": "Bounds involving lifetimes", "correct": false}, {"key": "D", "text": "Nothing, it is purely cosmetic", "correct": false}], "answer": "B", "why": "<p><code>&lt;T: A + B&gt;</code> handles multiple bounds fine, so A is not it. The real gain is that <code>where</code> may constrain arbitrary types: <code>where Vec&lt;T&gt;: Debug</code>, <code>where T::Item: Display</code>, <code>where for&lt;'a&gt; &amp;'a T: IntoIterator</code>. The inline form can only attach bounds to the parameters as they are declared.</p><p>D is the common belief and it is right about the <em>usual</em> case: most <code>where</code> clauses in real code are there for readability. It is wrong as a rule.</p>"}, {"n": 9, "stem": "<p>Which is better, and why?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>struct Cache&lt;T: Clone&gt; { value: T }     // A\nstruct Cache&lt;T&gt; { value: T }            // B, with impl&lt;T: Clone&gt; Cache&lt;T&gt;</code></pre></div>", "options": [{"key": "A", "text": "A, since the bound is stated once, where the type is defined", "correct": false}, {"key": "B", "text": "B, since the bound belongs on the impl block that needs it", "correct": true}, {"key": "C", "text": "They are identical after monomorphisation, so it makes no difference", "correct": false}, {"key": "D", "text": "A, because otherwise <code>Cache&lt;File&gt;</code> would be constructible", "correct": false}], "answer": "B", "why": "<p>A bound on a struct propagates: every <code>impl</code>, every function signature mentioning <code>Cache</code>, and every other struct holding one must repeat it. It also cannot be relaxed later without a breaking change, and it buys no safety, since the impl block would reject a bad <code>T</code> anyway.</p><p>D is the trap, because it names a real consequence and gets its sign wrong. <code>Cache&lt;File&gt;</code> being constructible is <em>good</em>: storing a file is sensible, and only the cloning methods should be unavailable. <code>HashMap&lt;K, V&gt;</code> declares <code>K</code> with no bounds at all for exactly this reason.</p>"}, {"n": 10, "stem": "<p>How many copies of <code>checksum</code> does this produce?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn checksum&lt;const N: usize&gt;(a: [u8; N]) -&gt; u32 { a.iter().map(|b| *b as u32).sum() }\n\nchecksum([1, 2]);\nchecksum([3, 4]);\nchecksum([5, 6, 7]);</code></pre></div>", "options": [{"key": "A", "text": "1", "correct": false}, {"key": "B", "text": "2", "correct": true}, {"key": "C", "text": "3", "correct": false}, {"key": "D", "text": "0, <code>N</code> is erased", "correct": false}], "answer": "B", "why": "<p>Const generics monomorphise like type parameters: one copy per distinct value of <code>N</code>. Two calls use <code>N = 2</code> and share a copy; the third uses <code>N = 3</code> and gets its own.</p><p>The payoff for the extra copy is that <code>N</code> is a compile-time constant inside each one, so the loop can be fully unrolled and the bounds checks removed. Taking <code>&amp;[u8]</code> instead would give one function for all lengths, carrying the length at runtime and losing the unrolling.</p>"}, {"n": 11, "stem": "<p><code>Option&lt;T&gt;</code> is\u2026</p>", "options": [{"key": "A", "text": "A compiler built-in with special generic support", "correct": false}, {"key": "B", "text": "An ordinary generic enum, defined in the standard library with no privileges", "correct": true}, {"key": "C", "text": "A trait object under the hood", "correct": false}, {"key": "D", "text": "A nullable pointer, special-cased by rustc", "correct": false}], "answer": "B", "why": "<p><code>enum Option&lt;T&gt; { None, Some(T) }</code> is source you could have written. <code>Result&lt;T, E&gt;</code> likewise. Neither gets compiler privileges; <code>?</code> and <code>if let</code> work on them through ordinary traits and pattern matching.</p><p>D is half a truth worth separating out. rustc <em>does</em> apply a niche optimisation so that <code>Option&lt;&amp;T&gt;</code> and <code>Option&lt;Box&lt;T&gt;&gt;</code> are one word, using the impossible null value as the <code>None</code> tag. That is a layout optimisation available to any enum with a niche, not a special case for <code>Option</code>.</p>"}, {"n": 12, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn main() {\n    let mut v = Vec::new();\n    v.push(1u8);\n    println!(\"{}\", v.len());\n}</code></pre></div>", "options": [{"key": "A", "text": "Yes, the <code>push</code> on the next line determines the element type", "correct": true}, {"key": "B", "text": "No, <code>Vec::new()</code> needs a turbofish", "correct": false}, {"key": "C", "text": "No, <code>v</code> must be annotated at the <code>let</code>", "correct": false}, {"key": "D", "text": "Yes, and <code>T</code> defaults to <code>i32</code>", "correct": false}], "answer": "A", "why": "<p>Inference is not line-by-line. rustc collects constraints across the whole function body, so <code>push(1u8)</code> fixes <code>T = u8</code> even though it appears after the declaration. Remove that <code>push</code> and you get <code>E0282</code>.</p><p>D is wrong twice: there is no fallback for a generic parameter, and even if there were, <code>1u8</code> is explicit.</p>"}, {"n": 13, "stem": "<p>What is the machine-level cost of calling <code>fn show&lt;T: Display&gt;(x: T)</code> versus <code>fn show(x: &amp;dyn Display)</code>?</p>", "options": [{"key": "A", "text": "Identical, both are direct calls", "correct": false}, {"key": "B", "text": "The generic is a direct call and can inline; the <code>dyn</code> version loads an address from a vtable and calls through it", "correct": true}, {"key": "C", "text": "The generic allocates; the <code>dyn</code> version does not", "correct": false}, {"key": "D", "text": "The <code>dyn</code> version is faster because there is only one copy in cache", "correct": false}], "answer": "B", "why": "<p>After monomorphisation the generic call has a known target, so it is a direct call and everything downstream of inlining becomes available. The <code>dyn</code> version compiles once; the target is unknown until runtime, so it costs one load plus one indirect call and cannot be inlined.</p><p>D is not absurd, and one shared copy really can be kinder to the instruction cache than thirty specialised ones. It is not the usual outcome, though, and the lost inlining dominates.</p>"}, {"n": 14, "stem": "<p>You are storing a mixed collection of shapes: circles, rectangles and text boxes, decided at runtime. What do you reach for?</p>", "options": [{"key": "A", "text": "Generics, as <code>Vec&lt;T: Shape&gt;</code>", "correct": false}, {"key": "B", "text": "<code>Vec&lt;Box&lt;dyn Shape&gt;&gt;</code>", "correct": true}, {"key": "C", "text": "An enum with one variant per shape", "correct": false}, {"key": "D", "text": "Either B or an enum, depending on whether the set of shapes is open", "correct": true}, {"key": "E", "text": "<code>Vec&lt;impl Shape&gt;</code>", "correct": false}], "answer": "BD", "why": "<p><code>Vec&lt;T&gt;</code> holds one <code>T</code>. A mixed collection has no single <code>T</code>, so generics cannot express it, and <code>Vec&lt;impl Shape&gt;</code> is not valid syntax anyway. That leaves two real designs.</p><p><code>Box&lt;dyn Shape&gt;</code> is right when the set is <strong>open</strong>: plugins, user-supplied types, anything a downstream crate can extend. An enum is right when the set is <strong>closed</strong>: nothing is allocated, no vtable is consulted, and <code>match</code> becomes exhaustive, so adding a variant makes the compiler list every site to update. Both answers are correct for different questions.</p>"}, {"n": 15, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn first&lt;T&gt;(v: &amp;[T]) -&gt; T {\n    v[0]\n}</code></pre></div>", "options": [{"key": "A", "text": "Yes", "correct": false}, {"key": "B", "text": "No, it moves a value out of a slice it only borrowed", "correct": true}, {"key": "C", "text": "No, <code>T</code> needs a <code>Sized</code> bound", "correct": false}, {"key": "D", "text": "Yes, but only for <code>Copy</code> types, decided at each call", "correct": false}], "answer": "B", "why": "<p>Returning <code>T</code> by value means producing an owned value, and <code>v</code> is only borrowed. Taking element zero out would leave a hole in data belonging to someone else. The error is <code>E0507</code>, <em>cannot move out of index of <code>&amp;[T]</code></em>.</p><p>D is the C++ intuition and it is exactly what Rust refuses to do: the body is checked once, against the bounds, not re-checked per call. Either bound <code>T: Copy</code> and copy the bytes, bound <code>T: Clone</code> and write <code>v[0].clone()</code>, or return <code>&amp;T</code> and let the caller decide.</p><p>C is wrong but nearly interesting: <code>T: Sized</code> is already implied on every type parameter unless you write <code>T: ?Sized</code>.</p>"}]}