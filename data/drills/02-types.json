{"unit": "02-types", "questions": [{"n": 1, "stem": "<p>What type does <code>n</code> have?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let n = 42;</code></pre></div>", "options": [{"key": "A", "text": "<code>i32</code>", "correct": true}, {"key": "B", "text": "<code>u32</code>", "correct": false}, {"key": "C", "text": "<code>usize</code>", "correct": false}, {"key": "D", "text": "<code>i64</code>, because the machine is 64-bit", "correct": false}], "answer": "A", "why": "<p>An unsuffixed integer literal with nothing else to constrain it falls back to <code>i32</code>. The choice is deliberate: <code>i32</code> is fast on every target rustc supports and wide enough for almost every counter, so the default is rarely the wrong answer.</p><p>D is the tempting one, because \"64-bit machine\" suggests 64-bit integers. The fallback does not vary by target. That would make the same source produce different overflow behaviour on different machines. Only <code>usize</code> and <code>isize</code> change width with the target, and they do it because they are pointer-sized.</p>"}, {"n": 2, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let v = vec![10, 20, 30];\nlet i: u32 = 1;\nlet x = v[i];</code></pre></div>", "options": [{"key": "A", "text": "Yes, <code>u32</code> is an unsigned integer, so it is a valid index", "correct": false}, {"key": "B", "text": "No, a slice can only be indexed by <code>usize</code>", "correct": true}, {"key": "C", "text": "No, you cannot index a <code>Vec</code> at all, only a slice", "correct": false}, {"key": "D", "text": "Yes, but only in a release build", "correct": false}], "answer": "B", "why": "<p><code>Index</code> is implemented for slices with <code>usize</code> and with ranges of <code>usize</code>, and with nothing else. <code>u32</code> is unsigned and non-negative and still not the right type, because an index is an offset from an address and must be able to express the largest address the target has.</p><p>A is the tempting answer and it is tempting for a good reason: on x86-64 a <code>u32</code> would work fine. On a 16-bit target it would not, and Rust would rather you cast deliberately than discover that during a port.</p>"}, {"n": 3, "stem": "<p><code>a</code> and <code>b</code> are runtime <code>u8</code> values holding 250 and 10. What does <code>a + b</code> do?</p>", "options": [{"key": "A", "text": "Produces 260", "correct": false}, {"key": "B", "text": "Panics in every build", "correct": false}, {"key": "C", "text": "Panics in a debug build, wraps to 4 in a release build", "correct": true}, {"key": "D", "text": "Wraps to 4 in every build", "correct": false}], "answer": "C", "why": "<p>Overflow checking costs a branch per operation. In a debug build that cost is irrelevant and catching the bug is worth everything, so the check is on. In a release build it is off, and the operation is <em>defined</em> to wrap two's-complement style.</p><p>B and D are each half right, which is what makes them plausible. Most people have only ever seen one of the two builds. The important consequence is that a program can pass its tests in debug and compute silently wrong numbers in release, which is exactly why <code>wrapping_add</code>, <code>checked_add</code> and <code>saturating_add</code> exist: they behave the same in both.</p>"}, {"n": 4, "stem": "<p>What is the value of <code>small</code>?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let big: i32 = 300;\nlet small = big as u8;</code></pre></div>", "options": [{"key": "A", "text": "255; it saturates at the maximum", "correct": false}, {"key": "B", "text": "44; the low eight bits, and the rest are discarded", "correct": true}, {"key": "C", "text": "It does not compile; <code>i32</code> to <code>u8</code> needs <code>try_into</code>", "correct": false}, {"key": "D", "text": "It panics in a debug build", "correct": false}], "answer": "B", "why": "<p>300 is <code>0b1_0010_1100</code>. Keeping the low eight bits leaves <code>0b0010_1100</code>, which is</p><ol><li><code>as</code> between integers is a bit-level operation: it keeps what fits and</li></ol><p>discards the rest, with no check, no warning and no panic in any build.</p><p>A is the trap, because float-to-integer casts <em>do</em> saturate: <code>300.0f64 as u8</code> is</p><ol><li>Integer-to-integer casts do not. Two different rules living behind one</li></ol><p>keyword is a good argument for reaching for <code>try_into</code> when the value came from outside your program.</p>"}, {"n": 5, "stem": "<p>What does this print?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let n: i32 = -1;\nprintln!(\"{}\", n as u32);</code></pre></div>", "options": [{"key": "A", "text": "<code>-1</code>", "correct": false}, {"key": "B", "text": "<code>0</code>", "correct": false}, {"key": "C", "text": "<code>4294967295</code>", "correct": true}, {"key": "D", "text": "It does not compile", "correct": false}], "answer": "C", "why": "<p>The bits do not change; the interpretation does. <code>-1i32</code> is all thirty-two bits set, and read as an unsigned number that is 4294967295.</p><p>This is the cast that turns a validation bug into a vulnerability. A signed length that has gone negative through an earlier subtraction becomes an enormous unsigned number, sails past a <code>len &lt; capacity</code> check, and indexes far outside the buffer. <code>i32::try_into::&lt;u32&gt;()</code> returns <code>Err</code> for a negative value, which is why it belongs anywhere the number came from a file or a socket.</p>"}, {"n": 6, "stem": "<p>Which of these evaluate to <code>true</code>? Choose all that apply.</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let nan = f64::NAN;</code></pre></div>", "options": [{"key": "A", "text": "<code>0.1 + 0.2 == 0.3</code>", "correct": false}, {"key": "B", "text": "<code>nan != nan</code>", "correct": true}, {"key": "C", "text": "<code>(0.1f64 + 0.2 - 0.3).abs() &lt; 1e-12</code>", "correct": true}, {"key": "D", "text": "<code>nan &lt; 1.0</code>", "correct": false}, {"key": "E", "text": "<code>nan &gt; 1.0</code>", "correct": false}], "answer": "BC", "why": "<p>A is false: neither 0.1 nor 0.2 is exactly representable in binary, and the rounding errors do not cancel. C is the same question asked with a tolerance, which is the form that gets a useful answer.</p><p>B is true and it is the rule that shapes the type system. IEEE-754 says NaN compares unequal to everything including itself, so <code>f64</code> implements <code>PartialEq</code> but not <code>Eq</code>. D and E are both <strong>false</strong>, which is the part people miss: NaN is not less than, not greater than and not equal to 1.0. All three comparisons fail, which is why <code>f64</code> is not <code>Ord</code> and <code>sort()</code> will not take it.</p>"}, {"n": 7, "stem": "<p>Why does <code>values.sort()</code> not compile when <code>values</code> is a <code>Vec&lt;f64&gt;</code>?</p>", "options": [{"key": "A", "text": "<code>sort</code> is only defined for integers", "correct": false}, {"key": "B", "text": "<code>sort</code> requires <code>Ord</code>, and <code>f64</code> only implements <code>PartialOrd</code>", "correct": true}, {"key": "C", "text": "Floats have to be sorted with <code>sort_unstable</code>", "correct": false}, {"key": "D", "text": "<code>Vec&lt;f64&gt;</code> needs to be converted to a slice first", "correct": false}], "answer": "B", "why": "<p>A correct sort needs a total order: every pair comparable, and the comparison transitive. A slice containing a NaN has neither, because NaN is incomparable to everything. So <code>f64</code> gets <code>PartialOrd</code> and stops there, and <code>sort</code>, which requires <code>Ord</code>, is unavailable.</p><p>The fix is <code>sort_by(|a, b| a.partial_cmp(b).unwrap())</code>, which will panic on a NaN rather than silently producing garbage. The same sort in C++ is undefined behaviour and can walk off the end of the array; here the missing trait makes you decide what to do about it up front.</p>"}, {"n": 8, "stem": "<p>What do these two expressions produce?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let s = \"h\u00e9llo\";\n(s.len(), s.chars().count())</code></pre></div>", "options": [{"key": "A", "text": "<code>(5, 5)</code>", "correct": false}, {"key": "B", "text": "<code>(6, 5)</code>", "correct": true}, {"key": "C", "text": "<code>(5, 6)</code>", "correct": false}, {"key": "D", "text": "<code>(6, 6)</code>", "correct": false}], "answer": "B", "why": "<p><code>len()</code> is a count of <strong>bytes</strong>, not characters. <code>\u00e9</code> is two bytes in UTF-8 (<code>c3 a9</code>), so the five characters occupy six bytes.</p><p>That is also why <code>s[0]</code> does not compile: byte 1 of this string is half of a letter, and Rust will not hand you an object that is half a character. Slicing by byte range works, so <code>&amp;s[0..1]</code> is <code>\"h\"</code>, and it panics if the range would split a character, which is the loudest possible way to be told your index arithmetic assumed ASCII.</p>"}, {"n": 9, "stem": "<p>What is <code>std::mem::size_of::&lt;char&gt;()</code>?</p>", "options": [{"key": "A", "text": "1", "correct": false}, {"key": "B", "text": "2", "correct": false}, {"key": "C", "text": "4", "correct": true}, {"key": "D", "text": "It varies with the character", "correct": false}], "answer": "C", "why": "<p>A <code>char</code> is a Unicode scalar value stored as a 32-bit integer, so every <code>char</code> is four bytes whatever it holds. Fixed size is what lets <code>char</code> be a normal value you can put in an array or a register.</p><p>D confuses <code>char</code> with UTF-8. In a <code>String</code> the <em>encoding</em> is variable-width, one to four bytes per character. A <code>char</code> sitting on the stack is the decoded form, and decoded means fixed-width. Converting between the two is what <code>.chars()</code> and <code>.encode_utf8()</code> do.</p>"}, {"n": 10, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn takes(a: [i32; 4]) -&gt; i32 { a[0] }\n\nlet xs: [i32; 3] = [1, 2, 3];\ntakes(xs);</code></pre></div>", "options": [{"key": "A", "text": "Yes, both are arrays of <code>i32</code>", "correct": false}, {"key": "B", "text": "No, <code>[i32; 3]</code> and <code>[i32; 4]</code> are different types", "correct": true}, {"key": "C", "text": "Yes, and the missing element is zero-filled", "correct": false}, {"key": "D", "text": "No, arrays cannot be passed to functions", "correct": false}], "answer": "B", "why": "<p>The length is part of the type. <code>[i32; 3]</code> and <code>[i32; 4]</code> are as unrelated as <code>i32</code> and <code>String</code>, which is precisely what lets an array live on the stack with no length field and no bounds check on a constant index.</p><p>The fix in real code is to take a <span class=\"term\" data-g=\"A borrowed view of a contiguous run of elements: a pointer plus a length, pointing into something someone else owns.\" data-t=\"slice\">slice</span>: <code>fn takes(a: &amp;[i32]) -&gt; i32</code> accepts any length, because a slice carries its length alongside the pointer at run time. That is the trade. The array knows its length at compile time and gives up flexibility; the slice is flexible and costs an extra word.</p>"}, {"n": 11, "stem": "<p>Which method should you use to add two <code>u32</code> hash values, where wrapping is the intended behaviour?</p>", "options": [{"key": "A", "text": "<code>a + b</code>", "correct": false}, {"key": "B", "text": "<code>a.checked_add(b)</code>", "correct": false}, {"key": "C", "text": "<code>a.saturating_add(b)</code>", "correct": false}, {"key": "D", "text": "<code>a.wrapping_add(b)</code>", "correct": true}], "answer": "D", "why": "<p><code>wrapping_add</code> states the intent and behaves identically in debug and release, which is the whole point. A hash that saturates at <code>u32::MAX</code> would bunch every large value onto one bucket; a hash that returns <code>None</code> on overflow is not a hash.</p><p>A is the trap. Plain <code>+</code> happens to wrap in a release build, so a hash written with <code>+</code> looks correct until someone runs the test suite in debug and it panics. Behaviour that depends on the build profile is behaviour you have not chosen.</p>"}, {"n": 12, "stem": "<p>What happens here?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let names: Vec&lt;&amp;str&gt; = Vec::new();\nlet last = names[names.len() - 1];</code></pre></div>", "options": [{"key": "A", "text": "<code>last</code> is an empty string", "correct": false}, {"key": "B", "text": "It does not compile", "correct": false}, {"key": "C", "text": "It panics; <code>0usize - 1</code> overflows before the index is even used", "correct": true}, {"key": "D", "text": "It panics with an index-out-of-bounds error", "correct": false}], "answer": "C", "why": "<p><code>len()</code> returns <code>usize</code>, which has no negative half, so <code>0 - 1</code> overflows immediately. In a debug build that panics with <code>attempt to subtract with overflow</code>, before any indexing happens.</p><p>D is the tempting answer and it is the wrong panic. In a release build the subtraction wraps to <code>usize::MAX</code> and <em>then</em> the index panics, so the same line produces two different messages depending on the build. This is the most common arithmetic panic in real Rust, and it always looks like a length minus something. <code>names.last()</code> returns an <code>Option</code> and never has the problem.</p>"}, {"n": 13, "stem": "<p>Which of these fix <code>error[E0284]: type annotations needed</code> here? Choose all that apply.</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let n = \"42\".parse().unwrap();\nprintln!(\"{n}\");</code></pre></div>", "options": [{"key": "A", "text": "<code>let n: u32 = \"42\".parse().unwrap();</code>", "correct": true}, {"key": "B", "text": "<code>let n = \"42\".parse::&lt;u32&gt;().unwrap();</code>", "correct": true}, {"key": "C", "text": "<code>let n = \"42\".parse&lt;u32&gt;().unwrap();</code>", "correct": false}, {"key": "D", "text": "<code>let n = \"42\" as u32;</code>", "correct": false}, {"key": "E", "text": "<code>let n = u32::from_str(\"42\").unwrap();</code> with <code>use std::str::FromStr;</code>", "correct": true}], "answer": "ABE", "why": "<p><code>parse</code> is generic over its output type, and <code>println!</code> accepts anything that can be displayed, so nothing in the original tells rustc which type to produce. A, B and E each name it, in a different place.</p><p>C is the one worth naming: <code>parse&lt;u32&gt;()</code> without the <code>::</code> does not parse as a generic call, because <code>&lt;</code> there is ambiguous with a comparison. The <code>::&lt;&gt;</code> form is the <span class=\"term\" data-g=\"The ::&lt;Type&gt; syntax for supplying type arguments explicitly where inference cannot decide, as in parse::&lt;i32&gt;().\" data-t=\"turbofish\">turbofish</span>, and the extra colons exist purely to resolve that.</p><p>D does not compile at all. <code>as</code> works between primitives, and <code>&amp;str</code> is not one.</p>"}, {"n": 14, "stem": "<p>Which conversion is the honest one for a length that arrived from a network packet as a <code>u64</code> and must become a <code>usize</code>?</p>", "options": [{"key": "A", "text": "<code>len as usize</code>", "correct": false}, {"key": "B", "text": "<code>usize::try_from(len)</code> and handle the error", "correct": true}, {"key": "C", "text": "<code>usize::from(len)</code>", "correct": false}, {"key": "D", "text": "<code>len.wrapping_into()</code>", "correct": false}], "answer": "B", "why": "<p>The value came from outside the program, so it can be anything, including a number too large for a <code>usize</code> on a 32-bit target. <code>try_from</code> returns a <code>Result</code> and makes you decide what a bad packet does.</p><p>C does not exist: there is no infallible <code>From&lt;u64&gt; for usize</code>, precisely because <code>usize</code> is not guaranteed to be 64 bits. The standard library refuses to write the impl rather than write one that could lose data, and that refusal is the useful signal. If <code>From</code> is missing between two integer types, the conversion can fail, and <code>as</code> will hide that.</p><p>A compiles and is the classic setup for a heap overflow: a truncated length passes a size check that the real length would have failed.</p>"}, {"n": 15, "stem": "<p>What is the type and size of <code>()</code>?</p>", "options": [{"key": "A", "text": "The unit type, zero bytes", "correct": true}, {"key": "B", "text": "An empty tuple, one byte", "correct": false}, {"key": "C", "text": "The same as <code>None</code>", "correct": false}, {"key": "D", "text": "A null pointer, eight bytes", "correct": false}], "answer": "A", "why": "<p><code>()</code> is a type with exactly one value, also written <code>()</code>. Since there is only one possible value, no bits are needed to distinguish it, so it occupies zero bytes and a <code>Vec&lt;()&gt;</code> of a million elements allocates nothing.</p><p>C is the interesting confusion. <code>None</code> is a <em>value</em> of <code>Option&lt;T&gt;</code>, which is a type with several possible values and does need storage. <code>()</code> means \"there is nothing to say here\"; <code>None</code> means \"there could have been something and there is not\". A function returning <code>()</code> did something; a function returning <code>Option&lt;T&gt;</code> answered a question, possibly with no.</p>"}]}