{"unit": "22-async", "questions": [{"n": 1, "stem": "<p>What has happened after this line runs?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let fut = fetch(\"https://example.com\");   // fetch is an `async fn`</code></pre></div>", "options": [{"key": "A", "text": "The request has been sent and is in flight", "correct": false}, {"key": "B", "text": "Nothing; a state machine value was built and no body code ran", "correct": true}, {"key": "C", "text": "The request has been sent and completed", "correct": false}, {"key": "D", "text": "It does not compile without <code>.await</code>", "correct": false}], "answer": "B", "why": "<p><code>async fn f() -&gt; T</code> is sugar for <code>fn f() -&gt; impl Future&lt;Output = T&gt;</code>. Calling it constructs the state machine in its initial state and returns it. Not one instruction of the body has run.</p><p>A is the JavaScript answer, and there it is correct. <code>const p = fetch(url)</code> really has sent the request, because a promise is a handle to work already in flight. The Rust version has sent nothing, which is why dropping the future is a perfectly good way to cancel the request.</p>"}, {"n": 2, "stem": "<p>Which of these are true of a Rust <code>Future</code> but not a JavaScript <code>Promise</code>? Choose all that apply.</p>", "options": [{"key": "A", "text": "It does nothing until something polls it", "correct": true}, {"key": "B", "text": "Dropping it cancels the work", "correct": true}, {"key": "C", "text": "It can produce an error", "correct": false}, {"key": "D", "text": "Awaiting it consumes it, so it cannot be awaited twice", "correct": true}, {"key": "E", "text": "Awaiting it lets other tasks run in the meantime", "correct": false}], "answer": "ABD", "why": "<p>A, B and D are all consequences of one design decision: a future is an ordinary owned value rather than a running job.</p><p>D catches people hardest. <code>.await</code> takes the future by <code>self</code>, so a second <code>f.await</code> is <code>E0382</code>, use of moved value. In JavaScript several places can await one promise and all get the same result; in Rust you either do the work twice or clone the result, and the language makes you say which.</p><p>E is true of both and is the thing people usually think <code>await</code> <em>is</em>. It is not what distinguishes them: a JS <code>await</code> also yields to the event loop.</p>"}, {"n": 3, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn summary(id: u32) -&gt; String {\n    let name = fetch(id).await;\n    format!(\"got {name}\")\n}</code></pre></div>", "options": [{"key": "A", "text": "Yes, because <code>fetch</code> is async, so the await is fine", "correct": false}, {"key": "B", "text": "No: <code>E0728</code>, await is only allowed inside an async function or block", "correct": true}, {"key": "C", "text": "No, because <code>summary</code> must return a <code>Future</code>", "correct": false}, {"key": "D", "text": "Yes, but it blocks the thread", "correct": false}], "answer": "B", "why": "<p><code>.await</code> compiles into \"poll; if not ready, save my locals and return <code>Pending</code> to my caller\". A plain <code>fn</code> has one entry and one exit and no saved state, so there is nowhere to suspend to.</p><p>C describes the <em>consequence</em> of the fix rather than the error. Adding <code>async</code> to <code>summary</code> does change its return type to <code>impl Future&lt;Output = String&gt;</code>. But you write <code>async fn summary(id: u32) -&gt; String</code>, not the future type by hand.</p>"}, {"n": 4, "stem": "<p><code>a.await; b.await;</code> does what?</p>", "options": [{"key": "A", "text": "Starts both and waits for both", "correct": false}, {"key": "B", "text": "Drives <code>a</code> to completion, then constructs and drives <code>b</code>", "correct": true}, {"key": "C", "text": "Starts both in parallel on the thread pool", "correct": false}, {"key": "D", "text": "Deadlocks unless a runtime is present", "correct": false}], "answer": "B", "why": "<p>The first <code>.await</code> does not \"kick off\" <code>a</code> and continue; it runs <code>a</code> to completion. The expression <code>b</code> has not even been evaluated at that point.</p><p>This is the most common real mistake in async Rust and it produces no warning of any kind. The symptom is a program that is correct and exactly as slow as the blocking version it replaced. <code>join!(a, b)</code> polls both from one task; a <code>spawn</code> each puts them on the pool.</p>"}, {"n": 5, "stem": "<p><code>tokio::join!(a, b)</code> gives you concurrency. Does it give you parallelism?</p>", "options": [{"key": "A", "text": "Yes, since that is what a runtime is for", "correct": false}, {"key": "B", "text": "No, because it polls both futures from one task on one thread", "correct": true}, {"key": "C", "text": "Only if the runtime is multi-threaded", "correct": false}, {"key": "D", "text": "Only if <code>a</code> and <code>b</code> are <code>Send</code>", "correct": false}], "answer": "B", "why": "<p><code>join!</code> expands to a single future that polls each of its arguments in turn. One task, one thread, and no <code>Send</code> bound needed. That is exactly why it can hold non-<code>Send</code> data across awaits when a spawned task could not.</p><p>For actual parallelism you need <code>tokio::spawn</code> per future, and then <code>Send + 'static</code> applies exactly as it does to <code>thread::spawn</code>. C is close: a multi-threaded runtime lets <em>different tasks</em> run on different cores, but it cannot split one <code>join!</code> across two.</p>"}, {"n": 6, "stem": "<p>A future returns <code>Poll::Pending</code> and does not register the waker anywhere. What happens?</p>", "options": [{"key": "A", "text": "The executor polls it again immediately", "correct": false}, {"key": "B", "text": "The executor polls it on a timer", "correct": false}, {"key": "C", "text": "The task is parked and never polled again", "correct": true}, {"key": "D", "text": "The program panics", "correct": false}], "answer": "C", "why": "<p>An executor parks a task on <code>Pending</code> and only re-queues it when <code>wake()</code> is called. No waker registered means no wake, so the task stops for good, and silently.</p><p>A is the tempting one because it describes what a naive loop would do, and <code>cx.waker().wake_by_ref()</code> before returning <code>Pending</code> really does produce that behaviour. That is a busy loop dressed up. A real future hands the waker to an epoll registration or a timer, then costs nothing at all until the OS speaks.</p>"}, {"n": 7, "stem": "<p>Why does <code>Future::poll</code> take <code>self: Pin&lt;&amp;mut Self&gt;</code> rather than <code>&amp;mut self</code>?</p>", "options": [{"key": "A", "text": "To stop two threads polling the same future", "correct": false}, {"key": "B", "text": "Because a generated future can hold a reference into its own fields, so it must not move", "correct": true}, {"key": "C", "text": "Because futures are always heap-allocated", "correct": false}, {"key": "D", "text": "To make the trait object-safe", "correct": false}], "answer": "B", "why": "<p><code>let row = load().await; render(&amp;row).await;</code> produces a state machine holding both <code>row</code> and a future built from <code>&amp;row</code>, which is a self-referential value. Move it and the internal pointer still aims at the old address, in safe code, which the language does not permit.</p><p><code>Pin&lt;&amp;mut T&gt;</code> is a type-level promise that the value will not move again, and <code>poll</code> demands it so no future can be polled before that promise exists. Types that genuinely do not care implement <code>Unpin</code>, and for them <code>Pin</code> is transparent.</p>"}, {"n": 8, "stem": "<p>Which of these make a future non-<code>Send</code>? Choose all that apply.</p>", "options": [{"key": "A", "text": "Holding a <code>std::sync::MutexGuard</code> across an <code>.await</code>", "correct": true}, {"key": "B", "text": "Holding an <code>Rc&lt;T&gt;</code> across an <code>.await</code>", "correct": true}, {"key": "C", "text": "Holding an <code>Rc&lt;T&gt;</code> and dropping it before the <code>.await</code>", "correct": false}, {"key": "D", "text": "Awaiting a future whose output is non-<code>Send</code>, and returning it straight away", "correct": false}], "answer": "AB", "why": "<p>An <code>async fn</code> compiles into a struct whose fields are the values live <em>across</em> each suspension point. Anything live across an await becomes a field, and the future is <code>Send</code> only if every field is.</p><p>C is the fix, not a failure: a value dropped before the await is never a field of any state, so it does not constrain the future at all. That is why \"take the lock, touch the data, drop the guard, then await\" works. D is a distractor: the output is returned by value, not held across anything.</p>"}, {"n": 9, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>async fn countdown(n: u32) -&gt; u32 {\n    if n == 0 { return 0; }\n    countdown(n - 1).await + 1\n}</code></pre></div>", "options": [{"key": "A", "text": "Yes, since recursion is ordinary in Rust", "correct": false}, {"key": "B", "text": "No: <code>E0733</code>, recursion in an async fn requires boxing", "correct": true}, {"key": "C", "text": "No, because <code>async fn</code> cannot return a value from an early <code>return</code>", "correct": false}, {"key": "D", "text": "Yes, but it overflows the stack at runtime", "correct": false}], "answer": "B", "why": "<p>The value live across the <code>.await</code> is the future returned by <code>countdown</code>, so the state machine contains itself and has no computable size. It is the same infinite type as a recursive <code>struct</code> without a <code>Box</code>, arrived at from a different direction.</p><p><code>Box::pin(countdown(n - 1)).await</code> fixes it by putting the inner future on the heap so the outer holds a pointer of known size. <code>Pin</code> rather than plain <code>Box</code> because <code>poll</code> needs a pinned receiver.</p>"}, {"n": 10, "stem": "<p>What determines the size of a future?</p>", "options": [{"key": "A", "text": "The size of the stack frame of the <code>async fn</code>", "correct": false}, {"key": "B", "text": "The largest of its states, meaning the variables live across each suspension point", "correct": true}, {"key": "C", "text": "A fixed 4 KB, like a green thread", "correct": false}, {"key": "D", "text": "It is always boxed, so one pointer", "correct": false}], "answer": "B", "why": "<p>The compiler lays the states out as an enum and the future is as big as the largest variant. Variables that never cross an <code>.await</code> are not fields at all; they live in <code>poll</code>'s own stack frame while it runs.</p><p>That is the whole memory argument for async. A thread must commit a stack that might reach any depth, so it costs kilobytes to megabytes; a future holds exactly the state it needs at its widest point, which is often tens of bytes.</p>"}, {"n": 11, "stem": "<p>Why does Rust ship no executor in the standard library?</p>", "options": [{"key": "A", "text": "Nobody has written a good enough one", "correct": false}, {"key": "B", "text": "Because <code>async</code> was stabilised before executors existed", "correct": false}, {"key": "C", "text": "Because an executor needs a thread pool, an I/O reactor and a timer, none of which exist on every target", "correct": true}, {"key": "D", "text": "To avoid competing with tokio commercially", "correct": false}], "answer": "C", "why": "<p><code>std</code> has <code>Future</code>, <code>Poll</code>, <code>Context</code>, <code>Waker</code> and the syntax, and stops there. An executor is inherently platform-specific: epoll on Linux, kqueue on BSD, IOCP on Windows, and none of the above on a microcontroller or inside a WASM sandbox, all of which run async Rust today.</p><p>The cost is real and you pay it in dependency choice and libraries written against a specific runtime. The benefit is that the same <code>async fn</code> compiles for a server and a Cortex-M0.</p>"}, {"n": 12, "stem": "<p><code>tokio::spawn(async { data.iter().sum::&lt;i32&gt;() })</code> where <code>data</code> is a local <code>Vec</code>.</p>", "options": [{"key": "A", "text": "Compiles, because the async block ends before <code>data</code> is dropped", "correct": false}, {"key": "B", "text": "Fails, because <code>spawn</code> requires <code>'static</code> and the block borrows <code>data</code>", "correct": true}, {"key": "C", "text": "Fails, because <code>Vec</code> is not <code>Send</code>", "correct": false}, {"key": "D", "text": "Compiles, but the sum may be wrong", "correct": false}], "answer": "B", "why": "<p><code>error[E0373]: async block may outlive the current function</code>. An async block captures like a closure: by reference unless you write <code>async move</code>.</p><p>A is the appealing wrong answer, and it is a true statement about this particular program. It is not a fact the <em>type</em> carries, and <code>tokio::spawn</code> demands <code>Send + 'static</code> because a spawned task is handed to the runtime and may outlive whatever spawned it. <code>async move</code> moves <code>data</code> in and the problem is gone.</p>"}, {"n": 13, "stem": "<p>Which of these compiles?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>// 1\nlet body = fetch(url)?.await;\n// 2\nlet body = fetch(url).await?;</code></pre></div>", "options": [{"key": "A", "text": "Only 1", "correct": false}, {"key": "B", "text": "Only 2", "correct": true}, {"key": "C", "text": "Both", "correct": false}, {"key": "D", "text": "Neither", "correct": false}], "answer": "B", "why": "<p>The order is fixed: <code>.await</code> drives the future to completion and yields the <code>Result</code>, then <code>?</code> unwraps it. Version 1 applies <code>?</code> to the future itself, which asks for <code>Try</code> on a type that does not implement it: <code>E0277</code> again, with a different missing trait.</p><p>Reading it aloud settles it: \"await the request, then propagate its error\". You cannot propagate an error from something that has not run yet.</p>"}, {"n": 14, "stem": "<p>Ten thousand mostly-idle connections. Why is one future each cheaper than one thread each?</p>", "options": [{"key": "A", "text": "Futures run on the GPU", "correct": false}, {"key": "B", "text": "Threads are slower to execute the same code", "correct": false}, {"key": "C", "text": "A thread commits a whole stack and a kernel scheduler slot; a parked future holds only its live state", "correct": true}, {"key": "D", "text": "Futures avoid system calls entirely", "correct": false}], "answer": "C", "why": "<p>The executing code is the same speed either way. This is entirely about what an <em>idle</em> one costs. A parked thread holds 8 KB to 2 MB of stack it may never use, plus a kernel object, and switching to it is a context switch. A parked future is a struct the size of its widest state, and resuming it is a function call.</p><p>D is nearly right in spirit and wrong in fact: async does far <em>fewer</em> syscalls, via one epoll for thousands of sockets, but it certainly makes them.</p>"}, {"n": 15, "stem": "<p>You have a CPU-bound function that takes 200 ms. Where should it go in a tokio program?</p>", "options": [{"key": "A", "text": "Straight into an <code>async fn</code>, letting the runtime schedule around it", "correct": false}, {"key": "B", "text": "Inside a <code>join!</code> with the other work", "correct": false}, {"key": "C", "text": "On a blocking thread pool, via <code>spawn_blocking</code> or a channel to a worker thread", "correct": true}, {"key": "D", "text": "Inside a loop with <code>yield_now().await</code> every iteration", "correct": false}], "answer": "C", "why": "<p>A future only gives up control at an <code>.await</code>. A 200 ms computation with no await inside it holds its executor thread for 200 ms, and every other task assigned to that thread waits, timers and I/O wakeups included. B makes it worse, since <code>join!</code> runs everything on that one task.</p><p>D genuinely works and is what you do when you cannot move the work, but it is fiddly and still competes for the I/O threads. <code>spawn_blocking</code> exists precisely so the CPU work runs somewhere that blocking is expected.</p>"}]}