{"unit": "16-closures", "questions": [{"n": 1, "stem": "<p>What does the compiler actually generate for a closure?</p>", "options": [{"key": "A", "text": "A function pointer plus a hidden heap allocation", "correct": false}, {"key": "B", "text": "An anonymous struct holding the captures, with the body as a method", "correct": true}, {"key": "C", "text": "A <code>Box&lt;dyn Fn&gt;</code> behind the scenes", "correct": false}, {"key": "D", "text": "An inline lambda with no type of its own", "correct": false}], "answer": "B", "why": "<p>One field per captured variable, and a <code>call</code> method carrying the body. That single fact explains the rest: the three traits are the three possible receivers for that method, <code>move</code> decides whether the fields are references or owned values, and each closure expression makes a <em>different</em> struct, hence a different type.</p><p>C is the tempting one because <code>Box&lt;dyn Fn&gt;</code> is how you often <em>store</em> a closure. Nothing is boxed unless you box it.</p>"}, {"n": 2, "stem": "<p>How much memory does <code>let f = |x: i32| x * 2;</code> occupy?</p>", "options": [{"key": "A", "text": "Zero bytes \u2014 it captures nothing", "correct": true}, {"key": "B", "text": "Eight bytes \u2014 a function pointer", "correct": false}, {"key": "C", "text": "Sixteen bytes \u2014 a pointer and a vtable pointer", "correct": false}, {"key": "D", "text": "It is heap-allocated, so the binding is one word", "correct": false}], "answer": "A", "why": "<p>The generated struct has no fields, so it is zero-sized, and calling it is a direct call the optimiser will inline. That is why <code>v.iter().map(|x| x * 2)</code> compiles to the same instructions as a hand-written loop.</p><p>C describes <code>&amp;dyn Fn</code> or <code>Box&lt;dyn Fn&gt;</code>, which is what you get only when you ask for dynamic dispatch. The closure itself has no vtable.</p>"}, {"n": 3, "stem": "<p>Which traits does <code>|| count += 1</code> implement, where <code>count: i32</code> is a local? Choose all that apply.</p>", "options": [{"key": "A", "text": "<code>Fn</code>", "correct": false}, {"key": "B", "text": "<code>FnMut</code>", "correct": true}, {"key": "C", "text": "<code>FnOnce</code>", "correct": true}, {"key": "D", "text": "<code>fn() -&gt; ()</code>, the function-pointer type", "correct": false}], "answer": "BC", "why": "<p>The body mutates a capture, so the generated <code>call</code> takes <code>&amp;mut self</code>. That is <code>FnMut</code>, and <code>FnMut</code> implies <code>FnOnce</code> \u2014 anything callable repeatedly is callable once.</p><p>It is not <code>Fn</code>, which needs <code>&amp;self</code>, and not a <code>fn</code> pointer, which has nowhere to hold the captured <code>count</code>. The hierarchy runs <code>Fn</code> \u2282 <code>FnMut</code> \u2282 <code>FnOnce</code>, so a closure always implements a <em>suffix</em> of that list, never a single one. When a generic function asks for <code>FnOnce</code> it therefore accepts every closure; asking for <code>Fn</code> accepts the fewest.</p>"}, {"n": 4, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let mut count = 0;\nlet bump = || count += 1;\nbump();</code></pre></div>", "options": [{"key": "A", "text": "Yes \u2014 <code>count</code> is already <code>mut</code>", "correct": false}, {"key": "B", "text": "No \u2014 <code>E0596</code>; <code>bump</code> itself must be <code>mut</code>", "correct": true}, {"key": "C", "text": "No \u2014 a closure cannot mutate a captured variable", "correct": false}, {"key": "D", "text": "Yes, but <code>count</code> stays 0", "correct": false}], "answer": "B", "why": "<p>Calling an <code>FnMut</code> closure takes <code>&amp;mut self</code>, so the binding holding the closure must be mutable: <code>let mut bump = ...</code>. The error names <code>bump</code>, not <code>count</code>, which is the part that surprises people.</p><p>C is wrong and worth ruling out. Mutating a capture is precisely what <code>FnMut</code> exists for. The generated struct holds a <code>&amp;mut i32</code> and the borrow checker enforces that nothing else touches <code>count</code> while the closure may still be called.</p>"}, {"n": 5, "stem": "<p>What does <code>move</code> change about a closure?</p>", "options": [{"key": "A", "text": "Every capture is taken by value instead of by reference", "correct": true}, {"key": "B", "text": "Which of the three <code>Fn</code> traits it implements", "correct": false}, {"key": "C", "text": "That it is allocated on the heap", "correct": false}, {"key": "D", "text": "That it can be called only once", "correct": false}], "answer": "A", "why": "<p><code>move</code> is about <strong>capture mode</strong>, not about traits. A <code>move</code> closure whose body only reads is still <code>Fn</code>; a non-<code>move</code> closure that consumes a capture is still <code>FnOnce</code>. The trait is always derived from what the body does.</p><p>B is the most common misreading and it matters: <code>thread::spawn</code> needs <code>FnOnce + Send + 'static</code>, and <code>move</code> helps with <code>'static</code> \u2014 no borrowed locals \u2014 not with the <code>FnOnce</code> part.</p>"}, {"n": 6, "stem": "<p>Which of these can be assigned to a <code>let f: fn(i32) -&gt; i32</code>? Choose all that apply.</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn double(x: i32) -&gt; i32 { x * 2 }\nlet n = 3;</code></pre></div>", "options": [{"key": "A", "text": "<code>double</code>", "correct": true}, {"key": "B", "text": "<code>|x| x * 2</code>", "correct": true}, {"key": "C", "text": "<code>|x| x * n</code>", "correct": false}, {"key": "D", "text": "<code>move |x| x * n</code>", "correct": false}], "answer": "AB", "why": "<p>A <code>fn</code> pointer is a bare code address \u2014 one word, with nowhere to store an environment. A closure coerces to one only if it captures nothing, so A and B work and C and D do not, <code>move</code> or otherwise.</p><p>The error for C is <code>E0308: expected fn pointer, found closure</code>, with the help line spelling out the rule. This coercion is what makes <code>map(str::trim)</code> and <code>unwrap_or_else(Vec::new)</code> work, and it is why you should take <code>impl Fn(..)</code> in your own APIs: it accepts both kinds.</p>"}, {"n": 7, "stem": "<p>Two closures, identical bodies:</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let a = |x: i32| x + 1;\nlet b = |x: i32| x + 1;</code></pre></div><p>What is true of their types?</p>", "options": [{"key": "A", "text": "They have the same type, since the signatures match", "correct": false}, {"key": "B", "text": "They have different types \u2014 each closure expression defines its own", "correct": true}, {"key": "C", "text": "They both have type <code>fn(i32) -&gt; i32</code>", "correct": false}, {"key": "D", "text": "They are both <code>impl Fn(i32) -&gt; i32</code>, which is one type", "correct": false}], "answer": "B", "why": "<p>Every closure expression generates a fresh anonymous struct, so <code>a</code> and <code>b</code> are as unrelated as two distinct structs with identical fields. They cannot go in one <code>Vec</code> without boxing.</p><p>D is the subtle wrong answer. <code>impl Fn(i32) -&gt; i32</code> is not a type you can have two of \u2014 in return position it means \"one specific type I am not naming\", chosen by the compiler. A collection of different closures needs <code>Box&lt;dyn Fn(i32) -&gt; i32&gt;</code>.</p>"}, {"n": 8, "stem": "<p>Which return types work for a function that builds and returns a closure capturing a local? Choose all that apply.</p>", "options": [{"key": "A", "text": "<code>impl Fn(i32) -&gt; i32</code>", "correct": true}, {"key": "B", "text": "<code>Box&lt;dyn Fn(i32) -&gt; i32&gt;</code>", "correct": true}, {"key": "C", "text": "<code>fn(i32) -&gt; i32</code>", "correct": false}, {"key": "D", "text": "<code>dyn Fn(i32) -&gt; i32</code>", "correct": false}], "answer": "AB", "why": "<p>A is the default choice: static dispatch, no allocation, fully inlinable, and it works for exactly one concrete closure type. B costs one allocation and one indirect call, and is what you need when the returned closure could be one of several types.</p><p>C fails because the closure captures. D fails because <code>dyn Fn</code> is unsized \u2014 there is no way to return a value whose size is unknown, which is why it always appears behind <code>Box</code>, <code>&amp;</code>, or <code>Rc</code>.</p><p>Remember the <code>move</code>: the closure must own the local, or it cannot outlive the function.</p>"}, {"n": 9, "stem": "<p>Does this compile in edition 2021?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>struct Config { name: String, retries: u32 }\nlet mut cfg = Config { name: String::from(\"api\"), retries: 0 };\n\nlet mut bump = || cfg.retries += 1;\nprintln!(\"{}\", cfg.name);\nbump();</code></pre></div>", "options": [{"key": "A", "text": "Yes \u2014 the closure captures only <code>cfg.retries</code>", "correct": true}, {"key": "B", "text": "No \u2014 the closure borrows all of <code>cfg</code> mutably", "correct": false}, {"key": "C", "text": "No \u2014 closures cannot capture struct fields", "correct": false}, {"key": "D", "text": "Yes, but only because <code>name</code> is a <code>String</code>", "correct": false}], "answer": "A", "why": "<p>Since edition 2021, closures capture the individual <strong>places</strong> their body mentions rather than the whole variable. <code>bump</code> holds a <code>&amp;mut u32</code> to <code>cfg.retries</code>, leaving <code>cfg.name</code> free to read.</p><p>B is the right answer for editions 2015 and 2018, where this was <code>E0502</code> and people worked around it by writing <code>let r = &amp;mut cfg.retries;</code> above the closure. Disjoint capture removed a genuine annoyance, and it also means closures now capture strictly less than they used to.</p>"}, {"n": 10, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let banner = String::from(\"hi\");\nlet take = move || banner;\nlet a = take();\nlet b = take();</code></pre></div>", "options": [{"key": "A", "text": "Yes \u2014 <code>take</code> was declared with <code>move</code>, so it owns the string", "correct": false}, {"key": "B", "text": "No \u2014 <code>E0382</code>; the closure is <code>FnOnce</code> and was called twice", "correct": true}, {"key": "C", "text": "No \u2014 <code>move</code> closures cannot return their captures", "correct": false}, {"key": "D", "text": "Yes, and both <code>a</code> and <code>b</code> are <code>\"hi\"</code>", "correct": false}], "answer": "B", "why": "<p>The body evaluates to the captured field, so calling it must move that field out of the struct \u2014 which requires <code>self</code> by value. That is the definition of <code>FnOnce</code>: the first call consumes <code>take</code>.</p><p>A confuses ownership with callability. The closure does own the <code>String</code>; that is exactly why giving it away destroys the closure. Change the body to <code>banner.clone()</code> and the receiver becomes <code>&amp;self</code>, making it <code>Fn</code> and callable as often as you like.</p>"}, {"n": 11, "stem": "<p>Why does <code>thread::spawn(|| println!(\"{name}\"))</code> fail where <code>name</code> is a local <code>String</code>?</p>", "options": [{"key": "A", "text": "<code>E0373</code> \u2014 the closure borrows <code>name</code>, and the thread may outlive the frame", "correct": true}, {"key": "B", "text": "<code>String</code> is not <code>Send</code>", "correct": false}, {"key": "C", "text": "<code>println!</code> cannot be used off the main thread", "correct": false}, {"key": "D", "text": "The closure is <code>FnOnce</code>, and <code>spawn</code> requires <code>Fn</code>", "correct": false}], "answer": "A", "why": "<p>The body only reads, so the compiler captured a <code>&amp;String</code> \u2014 normally the best choice. <code>thread::spawn</code> requires <code>F: 'static</code>, and a borrow of a local is not <code>'static</code>, because the frame can disappear while the thread runs. <code>move</code> fixes it by giving the closure ownership.</p><p>D has the hierarchy backwards: <code>spawn</code> asks for <code>FnOnce</code>, the <em>weakest</em> of the three, because it calls the closure exactly once.</p>"}, {"n": 12, "stem": "<p>Two threads both need the same <code>Vec&lt;i32&gt;</code>. What works?</p>", "options": [{"key": "A", "text": "<code>move</code> into both closures", "correct": false}, {"key": "B", "text": "Borrow with <code>&amp;data</code> in both closures", "correct": false}, {"key": "C", "text": "<code>Arc::new(data)</code>, then <code>move</code> a separate <code>Arc::clone</code> into each", "correct": true}, {"key": "D", "text": "<code>Rc::new(data)</code>, then <code>move</code> a separate <code>Rc::clone</code> into each", "correct": false}], "answer": "C", "why": "<p>A fails with <code>E0382</code> \u2014 the first <code>move</code> takes the vector and the second has nothing left. B fails with <code>E0373</code>, because a spawned thread cannot borrow a local. The threads need <em>shared ownership</em>.</p><p>D is the trap and the compiler catches it: <code>Rc</code>'s counter is a plain integer, so two threads could increment it simultaneously and lose a count. <code>Rc</code> is therefore not <code>Send</code>, and you get <code>E0277</code>. <code>Arc</code> pays for an atomic counter and is safe to send. Its clone copies a pointer and bumps a count; the vector is never copied.</p>"}, {"n": 13, "stem": "<p>What is the difference between <code>unwrap_or(compute())</code> and <code>unwrap_or_else(|| compute())</code>?</p>", "options": [{"key": "A", "text": "None \u2014 the closure form is just style", "correct": false}, {"key": "B", "text": "<code>unwrap_or</code> evaluates <code>compute()</code> always; <code>unwrap_or_else</code> only on <code>None</code>", "correct": true}, {"key": "C", "text": "<code>unwrap_or_else</code> is slower because of the closure call", "correct": false}, {"key": "D", "text": "<code>unwrap_or</code> takes <code>Fn</code>, <code>unwrap_or_else</code> takes <code>FnOnce</code>", "correct": false}], "answer": "B", "why": "<p>Arguments are evaluated before the call, so <code>unwrap_or(compute())</code> runs <code>compute()</code> whether or not the <code>Option</code> is <code>Some</code>. Wrapping the work in a closure defers it: <code>unwrap_or_else</code> calls it only on the <code>None</code> path.</p><p>That is the general point of taking a closure rather than a value \u2014 laziness. C is wrong in practice: the closure is zero-sized and monomorphised, so the call inlines away entirely.</p>"}, {"n": 14, "stem": "<p><code>v.iter().map(f)</code> requires <code>f: FnMut</code>. Why not <code>Fn</code>?</p>", "options": [{"key": "A", "text": "Because <code>map</code> needs to mutate the elements", "correct": false}, {"key": "B", "text": "Because <code>map</code> calls <code>f</code> once per element and <code>FnMut</code> lets <code>f</code> keep state", "correct": true}, {"key": "C", "text": "Because <code>Fn</code> closures cannot be passed to generic functions", "correct": false}, {"key": "D", "text": "Because the iterator itself is mutable", "correct": false}], "answer": "B", "why": "<p><code>FnMut</code> is the weaker bound of the two, so asking for it accepts strictly more closures \u2014 including every <code>Fn</code> closure, since <code>Fn</code> implies <code>FnMut</code>. It lets you write <code>map</code> with a running counter or an accumulator without <code>map</code> having to care.</p><p>A misreads which side mutates: <code>f</code> may mutate its own captures, not the elements. The general rule for your own APIs is to demand the weakest bound that works \u2014 <code>FnOnce</code> if you call at most once, <code>FnMut</code> if you call repeatedly, <code>Fn</code> only if you need several callers at once.</p>"}, {"n": 15, "stem": "<p>What does <code>Box&lt;dyn Fn(i32) -&gt; i32&gt;</code> cost compared with <code>impl Fn(i32) -&gt; i32</code>?</p>", "options": [{"key": "A", "text": "Nothing \u2014 both are static dispatch", "correct": false}, {"key": "B", "text": "One heap allocation and one indirect call that cannot be inlined", "correct": true}, {"key": "C", "text": "A reference count update on every call", "correct": false}, {"key": "D", "text": "It is faster, because the code is not duplicated per closure type", "correct": false}], "answer": "B", "why": "<p><code>Box&lt;dyn Fn&gt;</code> is a <span class=\"term\" data-g=\"A value behind a pointer whose concrete type is not known at compile time, carrying a vtable so calls can be dispatched at runtime.\" data-t=\"trait object\">trait object</span>: a pointer to the closure's struct on the heap, plus a vtable pointer to its <code>call</code>. Each invocation goes through the vtable, so the optimiser cannot see the body and cannot inline it.</p><p>D is not simply wrong \u2014 dynamic dispatch does avoid monomorphisation, so it can mean less code and better instruction-cache behaviour when there are many distinct closure types. It is a genuine trade rather than a straight loss. Take <code>impl Fn</code> by default, and reach for <code>Box&lt;dyn Fn&gt;</code> when you need several different closures behind one type.</p>"}]}