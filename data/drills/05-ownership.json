{"unit": "05-ownership", "questions": [{"n": 1, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let a = String::from(\"hi\");\nlet b = a;\nprintln!(\"{a}\");</code></pre></div>", "options": [{"key": "A", "text": "Yes \u2014 <code>a</code> and <code>b</code> both point at the string", "correct": false}, {"key": "B", "text": "No \u2014 <code>a</code> was moved into <code>b</code>", "correct": true}, {"key": "C", "text": "No \u2014 <code>String</code> cannot be printed with <code>{}</code>", "correct": false}, {"key": "D", "text": "Yes, but it prints an empty string", "correct": false}], "answer": "B", "why": "<p><code>String</code> is not <code>Copy</code>, so <code>let b = a;</code> <span class=\"term\" data-g=\"Transferring ownership from one binding to another. It copies the small fixed-size handle, not the heap contents, and the old binding becomes unusable.\" data-t=\"moves\">moves</span> \u2014 it copies the three-word handle and retires <code>a</code>. Reading <code>a</code> afterwards is <code>error[E0382]</code>.</p><p>The tempting wrong answer is D. Nothing is blanked at runtime; <code>a</code>'s bytes are still sitting in the stack frame. The restriction is purely in the compiler's bookkeeping, which is exactly why it costs nothing.</p>"}, {"n": 2, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let a = 5;\nlet b = a;\nprintln!(\"{a} {b}\");</code></pre></div>", "options": [{"key": "A", "text": "Yes \u2014 <code>i32</code> is <code>Copy</code>", "correct": true}, {"key": "B", "text": "No \u2014 <code>a</code> was moved into <code>b</code>", "correct": false}, {"key": "C", "text": "Yes, but only because both are on the stack", "correct": false}, {"key": "D", "text": "No \u2014 you cannot print two values in one <code>println!</code>", "correct": false}], "answer": "A", "why": "<p><code>i32</code> implements <code>Copy</code>, so the assignment duplicates the four bytes and leaves both bindings live. Duplicating those bytes produces a genuinely complete second value, so there is no shared resource to argue about.</p><p>C is close but wrong as stated. Living on the stack is not the criterion \u2014 a fixed-size array <code>[String; 2]</code> is entirely on the stack and is still not <code>Copy</code>, because the <code>String</code> elements own heap buffers.</p>"}, {"n": 3, "stem": "<p>Which of these move <code>s</code>? Choose all that apply.</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let s = String::from(\"hi\");</code></pre></div>", "options": [{"key": "A", "text": "<code>let t = s;</code>", "correct": true}, {"key": "B", "text": "<code>takes(s)</code> where <code>fn takes(x: String)</code>", "correct": true}, {"key": "C", "text": "<code>takes(&amp;s)</code> where <code>fn takes(x: &amp;String)</code>", "correct": false}, {"key": "D", "text": "<code>v.push(s)</code> where <code>v: Vec&lt;String&gt;</code>", "correct": true}, {"key": "E", "text": "<code>for c in s.into_bytes() {}</code>", "correct": true}], "answer": "ABDE", "why": "<p>A, B, D and E all need the value by value, so all four move it.</p><p>C does not: <code>&amp;s</code> creates a reference, which is permission to look without transferring ownership. That is the whole subject of the next unit, and it is the answer to most ownership complaints.</p><p>E is worth pausing on. <code>into_bytes</code> takes <code>self</code>, so it consumes the <code>String</code> to hand back its buffer as a <code>Vec&lt;u8&gt;</code> \u2014 no copying, which is only sound because the original is destroyed in the process.</p>"}, {"n": 4, "stem": "<p>How many bytes are copied by the move on line 2?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let a = String::from(\"a string of some considerable length\");\nlet b = a;</code></pre></div>", "options": [{"key": "A", "text": "36 \u2014 the whole contents", "correct": false}, {"key": "B", "text": "24 \u2014 the pointer, length and capacity", "correct": true}, {"key": "C", "text": "0 \u2014 moves are purely a compile-time concept", "correct": false}, {"key": "D", "text": "8 \u2014 just the pointer", "correct": false}], "answer": "B", "why": "<p>A <code>String</code> is three words on the stack: pointer, length, capacity. On a 64-bit machine that is 24 bytes, and the move copies exactly those. The heap buffer is not touched, not copied and not reallocated \u2014 <code>b</code> now points at the same bytes <code>a</code> pointed at.</p><p>C is the appealing wrong answer. The <em>restriction</em> is compile-time; the handle copy is a real <code>memcpy</code>, though in practice the optimiser usually elides it.</p><p>The takeaway: moves are cheap. People avoid them with <code>clone()</code> to dodge a cost that was never there, and <code>clone()</code> is the operation that actually touches the allocator.</p>"}, {"n": 5, "stem": "<p>Why can <code>String</code> never implement <code>Copy</code>?</p>", "options": [{"key": "A", "text": "Because it is too large to copy efficiently", "correct": false}, {"key": "B", "text": "Because <code>Copy</code> and <code>Drop</code> are mutually exclusive, and <code>String</code> has a destructor", "correct": true}, {"key": "C", "text": "Because the standard library authors chose not to", "correct": false}, {"key": "D", "text": "Because its size is not known at compile time", "correct": false}], "answer": "B", "why": "<p><code>Copy</code> means the bytes can be duplicated to produce a complete, independent value. <code>String</code> has a destructor that frees its buffer, so two byte-identical copies would both free the same allocation \u2014 a double free. The language enforces this structurally: a type cannot be both <code>Copy</code> and <code>Drop</code>.</p><p>D is a common confusion. A <code>String</code>'s <em>size</em> is perfectly well known \u2014 24 bytes, always. It is its <em>contents</em> whose size is not, which is why they live on the heap.</p>"}, {"n": 6, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>#[derive(Copy, Clone)]\nstruct Point { x: i32, y: i32 }\n\nlet p = Point { x: 1, y: 2 };\nlet q = p;\nprintln!(\"{}\", p.x);</code></pre></div>", "options": [{"key": "A", "text": "Yes \u2014 every field is <code>Copy</code>, so <code>Point</code> can be", "correct": true}, {"key": "B", "text": "No \u2014 structs can never be <code>Copy</code>", "correct": false}, {"key": "C", "text": "No \u2014 you must implement <code>Copy</code> manually", "correct": false}, {"key": "D", "text": "Yes, but <code>p.x</code> reads garbage after the copy", "correct": false}], "answer": "A", "why": "<p><code>Copy</code> is inherited from the fields: a struct may be <code>Copy</code> when <strong>every</strong> field is. Both fields here are <code>i32</code>, so the derive is accepted and <code>let q = p;</code> copies sixteen bytes leaving both live.</p><p>Add one <code>String</code> field and the same derive becomes <code>error[E0204]</code>, permanently \u2014 no amount of other integer fields can rescue it.</p>"}, {"n": 7, "stem": "<p>What does this print?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>struct Noisy(&amp;'static str);\nimpl Drop for Noisy {\n    fn drop(&amp;mut self) { println!(\"drop {}\", self.0); }\n}\n\nfn main() {\n    let _a = Noisy(\"a\");\n    let _b = Noisy(\"b\");\n}</code></pre></div>", "options": [{"key": "A", "text": "<code>drop a</code> then <code>drop b</code>", "correct": false}, {"key": "B", "text": "<code>drop b</code> then <code>drop a</code>", "correct": true}, {"key": "C", "text": "Nothing \u2014 <code>Drop</code> is only for heap values", "correct": false}, {"key": "D", "text": "The order is unspecified", "correct": false}], "answer": "B", "why": "<p>Bindings drop in <span class=\"term\" data-g=\"Bindings in a scope are dropped last-declared-first, because later values are the ones most likely to depend on earlier ones.\" data-t=\"reverse declaration order\">reverse declaration order</span>. This is not arbitrary: later bindings are the ones most likely to depend on earlier ones \u2014 a guard taken from a lock declared above it, a writer wrapping a file opened before it \u2014 so unwinding in the opposite order to construction is the only sequence that is always safe.</p>"}, {"n": 8, "stem": "<p>Which of these is dropped at the end of the block? Choose all that apply.</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>{\n    let a = Noisy(\"a\");\n    let b = Noisy(\"b\");\n    let c = b;\n    let _ = Noisy(\"d\");\n}</code></pre></div>", "options": [{"key": "A", "text": "<code>a</code>", "correct": true}, {"key": "B", "text": "<code>b</code>", "correct": false}, {"key": "C", "text": "<code>c</code>", "correct": true}, {"key": "D", "text": "<code>_</code> \u2014 at the end of the block", "correct": false}], "answer": "AC", "why": "<p><code>a</code> and <code>c</code> are dropped at the end. <code>b</code> is not: it was moved into <code>c</code>, so it no longer owns anything, and dropping it too would run one destructor twice \u2014 the double free, exactly.</p><p>D is the trap and it is a real one. <code>let _ = ...</code> is not a binding at all; the value is dropped <strong>immediately</strong>, on that very line. So <code>Noisy(\"d\")</code> prints its drop message before <code>c</code> and <code>a</code> do. This is why <code>let _ = mutex.lock();</code> takes a lock and releases it on the same line, while <code>let _guard = mutex.lock();</code> holds it \u2014 a single underscore that has ended real production incidents.</p>"}, {"n": 9, "stem": "<p><code>s</code> is a <code>String</code>. Which call leaves <code>s</code> usable afterwards?</p>", "options": [{"key": "A", "text": "<code>s.as_bytes()</code>", "correct": true}, {"key": "B", "text": "<code>s.into_bytes()</code>", "correct": false}, {"key": "C", "text": "<code>s.to_uppercase()</code>", "correct": true}, {"key": "D", "text": "<code>s.len()</code>", "correct": true}, {"key": "E", "text": "<code>s.into_boxed_str()</code>", "correct": false}], "answer": "ACD", "why": "<p>The standard library encodes the receiver in the method name, and it is worth learning once:</p><ul><li><code>as_*</code> borrows \u2014 <code>&amp;self</code> in, a cheap view out, no allocation</li><li><code>to_*</code> clones \u2014 <code>&amp;self</code> in, a new owned value out, usually allocating</li><li><code>into_*</code> consumes \u2014 <code>self</code> in, the receiver is gone afterwards</li></ul><p>So A, C and D borrow and leave <code>s</code> intact. B and E take <code>self</code> and consume it.</p>"}, {"n": 10, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn total(v: Vec&lt;i32&gt;) -&gt; i32 {\n    let mut sum = 0;\n    for x in v { sum += x; }\n    sum\n}\n\nlet v = vec![1, 2, 3];\nprintln!(\"{} {}\", total(v), v.len());</code></pre></div>", "options": [{"key": "A", "text": "Yes", "correct": false}, {"key": "B", "text": "No \u2014 <code>v</code> was moved into <code>total</code>", "correct": true}, {"key": "C", "text": "No \u2014 you cannot sum a <code>Vec</code> with a <code>for</code> loop", "correct": false}, {"key": "D", "text": "Yes, because <code>total</code> only reads the vector", "correct": false}], "answer": "B", "why": "<p>D states the intent and misses the mechanism. <code>total</code> <em>does</em> only read, but its signature says <code>Vec&lt;i32&gt;</code> by value, so calling it moves the vector regardless of what the body does. The type is the contract; the body does not get a vote.</p><p>Two separate moves are actually present: <code>total(v)</code> at the call site, and <code>for x in v</code> inside, which calls <code>into_iter</code> and consumes the vector. Taking <code>&amp;[i32]</code> fixes both and makes the function accept arrays and sub-slices too.</p>"}, {"n": 11, "stem": "<p>What is the difference between <code>Clone</code> and <code>Copy</code>?</p>", "options": [{"key": "A", "text": "None \u2014 <code>Copy</code> is just a shorthand for <code>Clone</code>", "correct": false}, {"key": "B", "text": "<code>Copy</code> duplicates implicitly on assignment; <code>Clone</code> duplicates only when you call <code>.clone()</code>", "correct": true}, {"key": "C", "text": "<code>Clone</code> is for stack values, <code>Copy</code> for heap values", "correct": false}, {"key": "D", "text": "<code>Copy</code> is deep, <code>Clone</code> is shallow", "correct": false}], "answer": "B", "why": "<p>The difference is who decides and when. <code>Copy</code> happens silently at every assignment and every by-value use. <code>Clone</code> happens only where you wrote <code>.clone()</code>.</p><p>That visibility is deliberate. <code>Clone</code> may allocate and copy an arbitrary amount of data, and in a language where duplication is implicit, an accidental deep copy in a hot loop stays invisible until you profile. Here it is a method call you can see and grep for.</p><p>Note the relationship: <code>Copy</code> <strong>requires</strong> <code>Clone</code>. Every <code>Copy</code> type is also <code>Clone</code>; the reverse is not true.</p>"}, {"n": 12, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let s = String::from(\"hi\");\nlet r = &amp;s;\nlet t = s;\nprintln!(\"{r}\");</code></pre></div>", "options": [{"key": "A", "text": "Yes \u2014 <code>r</code> is just a reference", "correct": false}, {"key": "B", "text": "No \u2014 <code>s</code> cannot be moved while it is borrowed", "correct": true}, {"key": "C", "text": "No \u2014 you cannot take a reference to a <code>String</code>", "correct": false}, {"key": "D", "text": "Yes, and it prints <code>hi</code>", "correct": false}], "answer": "B", "why": "<p>This is the seam where ownership meets borrowing. <code>r</code> borrows <code>s</code>, and then <code>let t = s;</code> tries to move the value out from under that borrow. If it were allowed, <code>r</code> would point at a stack slot whose value has been retired \u2014 a dangling reference, which is the bug references exist to prevent.</p><p>So a value cannot be moved while any reference to it is still live. The error is <code>error[E0505]: cannot move out of s because it is borrowed</code>, and it is the first rule of the next unit.</p>"}, {"n": 13, "stem": "<p><code>clone()</code> is the right answer when\u2026</p>", "options": [{"key": "A", "text": "Whenever the borrow checker complains", "correct": false}, {"key": "B", "text": "You genuinely need two independent values", "correct": true}, {"key": "C", "text": "Whenever you would otherwise pass by reference", "correct": false}, {"key": "D", "text": "The cost is provably irrelevant and it keeps the code simpler", "correct": true}], "answer": "BD", "why": "<p>B is the honest case, and D is the pragmatic one \u2014 a clone of a short string in setup code that runs once is not worth a design discussion.</p><p>A is the trap and the most common way to write slow, noisy Rust. A borrow-checker complaint is a question \u2014 <em>who should own this, and for how long?</em> \u2014 and <code>clone()</code> answers it by refusing to answer. Sometimes that is fine. Often the intended answer was a single <code>&amp;</code>.</p>"}, {"n": 14, "stem": "<p>After <code>takes(s)</code> where <code>fn takes(s: String)</code>, when is <code>s</code>'s buffer freed?</p>", "options": [{"key": "A", "text": "At the end of the caller's scope", "correct": false}, {"key": "B", "text": "At the end of <code>takes</code>, when its parameter goes out of scope", "correct": true}, {"key": "C", "text": "When the garbage collector next runs", "correct": false}, {"key": "D", "text": "It is never freed \u2014 this leaks", "correct": false}], "answer": "B", "why": "<p>The parameter <em>is</em> the owner now. When <code>takes</code> returns, its parameter goes out of scope and its destructor hands the buffer back to the allocator.</p><p>This is the point of the whole design: the free happens at a place the compiler picked by following ownership, not at a place you had to remember. And it happens on every exit path from <code>takes</code>, including an early <code>return</code> and a panic unwinding through the frame \u2014 which is why Rust needs no <code>finally</code> block.</p>"}, {"n": 15, "stem": "<p>What is the runtime cost of Rust's ownership checking?</p>", "options": [{"key": "A", "text": "A reference count on every value", "correct": false}, {"key": "B", "text": "A small periodic pause, much shorter than a garbage collector's", "correct": false}, {"key": "C", "text": "Essentially none \u2014 the analysis happens entirely at compile time", "correct": true}, {"key": "D", "text": "One pointer indirection per access", "correct": false}], "answer": "C", "why": "<p>Ownership is a static analysis. It runs in the compiler, decides where the drops go, emits them, and then does not exist any more. The machine code contains the right number of frees in the right places \u2014 which is exactly the code a careful C programmer would have written by hand.</p><p>The word \"essentially\" covers one small honest exception: where a value is moved on one branch and not another, the compiler cannot know statically whether to drop it, so it inserts a hidden boolean <span class=\"term\" data-g=\"A hidden boolean the compiler inserts when it cannot tell statically whether a value was moved, so it knows at runtime whether to drop it. Usually optimised away.\" data-t=\"drop flag\">drop flag</span> and checks it. That is a byte and a branch, usually optimised away, and it is the entire runtime footprint.</p>"}]}