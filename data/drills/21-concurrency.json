{"unit": "21-concurrency", "questions": [{"n": 1, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>use std::thread;\n\nfn main() {\n    let name = String::from(\"ada\");\n    thread::spawn(|| println!(\"{name}\")).join().unwrap();\n}</code></pre></div>", "options": [{"key": "A", "text": "Yes, because <code>join</code> on the next line proves the thread ends first", "correct": false}, {"key": "B", "text": "No, because the closure borrows <code>name</code> and <code>spawn</code> requires <code>'static</code>", "correct": true}, {"key": "C", "text": "No, because <code>println!</code> cannot be used off the main thread", "correct": false}, {"key": "D", "text": "Yes, but the output order is unspecified", "correct": false}], "answer": "B", "why": "<p><code>error[E0373]</code>. The fix is <code>move ||</code>.</p><p>A is the tempting one and it is a correct observation about the <em>program</em>. It is not a fact about the <em>types</em>, and the borrow checker works from signatures alone. <code>thread::spawn</code> is declared <code>F: Send + 'static</code>, so it accepts nothing that borrows a caller's frame, regardless of what the caller happens to do next. If you want the compiler to use that knowledge, you have to give it a type that carries it, which is <code>thread::scope</code>.</p>"}, {"n": 2, "stem": "<p>What does this print?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let mut count = 0;\nlet h = std::thread::spawn(move || { count += 1; count });\nlet inner = h.join().unwrap();\nprintln!(\"{count} {inner}\");</code></pre></div>", "options": [{"key": "A", "text": "<code>1 1</code>", "correct": false}, {"key": "B", "text": "<code>0 1</code>", "correct": true}, {"key": "C", "text": "It does not compile, because <code>count</code> was moved", "correct": false}, {"key": "D", "text": "<code>1 0</code>", "correct": false}], "answer": "B", "why": "<p><code>move</code> applies to every capture, including <code>Copy</code> ones, and for a <code>Copy</code> type \"moving\" means copying. The closure got its own <code>i32</code>, incremented that, and returned 1. The original is untouched, so <code>0 1</code>.</p><p>C is the trap. Because <code>i32</code> is <code>Copy</code>, <code>move</code> does not retire the original binding the way it would for a <code>String</code>; both stay usable. If you need a thread's mutation to be visible to the spawner, <code>move</code> is never enough. You need <code>Arc&lt;Mutex&lt;T&gt;&gt;</code>, an atomic, or the thread's return value.</p>"}, {"n": 3, "stem": "<p>Why is <code>Rc&lt;T&gt;</code> not <code>Send</code>?</p>", "options": [{"key": "A", "text": "Because it points at the heap, and heaps are not shared between threads", "correct": false}, {"key": "B", "text": "Because its reference count is incremented non-atomically, so concurrent clones can lose a count", "correct": true}, {"key": "C", "text": "Because <code>T</code> might not be <code>Send</code>", "correct": false}, {"key": "D", "text": "Because <code>Rc</code> has no lock inside it", "correct": false}], "answer": "B", "why": "<p><code>Rc::clone</code> is a load, an add and a store on a plain <code>usize</code>. Two threads doing it simultaneously can both read 1 and both write 2, so a count that should be 3 is 2, and the second drop frees a value the third holder is still using.</p><p>C is a real condition but not the reason: <code>Rc&lt;T&gt;</code> is not <code>Send</code> even when <code>T</code> is <code>Send</code>, because the failure is in <code>Rc</code>'s own bookkeeping. <code>Arc</code> is the identical type with <code>fetch_add</code>, which is exactly one instruction different.</p>"}, {"n": 4, "stem": "<p><code>T: Sync</code> is equivalent to which statement?</p>", "options": [{"key": "A", "text": "<code>T: Send</code>", "correct": false}, {"key": "B", "text": "<code>&amp;T: Send</code>", "correct": true}, {"key": "C", "text": "<code>T</code> contains a lock", "correct": false}, {"key": "D", "text": "<code>&amp;mut T: Send</code>", "correct": false}], "answer": "B", "why": "<p>That is the definition, not a consequence: a type is <code>Sync</code> when a shared reference to it can be sent to another thread. The compiler prints it in the error: <code>required for &amp;RefCell&lt;i32&gt; to implement Send</code>.</p><p>D is a good distractor because <code>&amp;mut T: Send</code> sounds stronger, but it follows from <code>T: Send</code>, not from <code>Sync</code>. A unique reference is exclusive access, which is the same situation as moving the value.</p>"}, {"n": 5, "stem": "<p>Which of these are <code>Send</code>? Choose all that apply.</p>", "options": [{"key": "A", "text": "<code>Arc&lt;Mutex&lt;Vec&lt;u8&gt;&gt;&gt;</code>", "correct": true}, {"key": "B", "text": "<code>Rc&lt;String&gt;</code>", "correct": false}, {"key": "C", "text": "<code>RefCell&lt;i32&gt;</code>", "correct": true}, {"key": "D", "text": "<code>Vec&lt;RefCell&lt;u8&gt;&gt;</code>", "correct": true}, {"key": "E", "text": "<code>MutexGuard&lt;'_, i32&gt;</code>", "correct": false}], "answer": "ACD", "why": "<p>C and D surprise people. <code>RefCell&lt;i32&gt;</code> is <code>Send</code>, because moving it to another thread is fine when only one thread has it afterwards. What it is not is <code>Sync</code>: two threads sharing a <code>&amp;RefCell</code> can both pass its non-atomic borrow check. Auto traits propagate structurally, so a <code>Vec</code> of them is <code>Send</code> too.</p><p>B is out because <code>Rc</code>'s count is not atomic. E is the one nobody guesses: on most platforms a mutex must be released by the thread that took it, so <code>MutexGuard</code> is <code>Sync</code> but deliberately not <code>Send</code>.</p>"}, {"n": 6, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>use std::sync::Arc;\nlet v = Arc::new(vec![1, 2, 3]);\nlet c = Arc::clone(&amp;v);\nc.push(4);</code></pre></div>", "options": [{"key": "A", "text": "Yes, because <code>Arc</code> is for sharing and sharing includes writing", "correct": false}, {"key": "B", "text": "No, because <code>Arc&lt;T&gt;</code> implements <code>Deref</code> but not <code>DerefMut</code>", "correct": true}, {"key": "C", "text": "No, because <code>Vec::push</code> requires the vector to be declared <code>mut</code>", "correct": false}, {"key": "D", "text": "Yes, but only because <code>v</code> and <code>c</code> point at the same allocation", "correct": false}], "answer": "B", "why": "<p><code>error[E0596]</code>. <code>Arc</code> cannot implement <code>DerefMut</code>: its whole purpose is that several owners exist at once, so handing out a <code>&amp;mut</code> would be handing out two unique references to one value, which is <code>E0499</code> laundered through a smart pointer.</p><p>C is close enough to be tempting, and adding <code>mut</code> to the bindings changes nothing: the problem is not the binding's mutability but the type's. Mutation comes back by putting a <code>Mutex</code> (or <code>RwLock</code>, or an atomic) inside the <code>Arc</code>.</p>"}, {"n": 7, "stem": "<p>Why is the data stored <em>inside</em> <code>Mutex&lt;T&gt;</code> rather than beside it?</p>", "options": [{"key": "A", "text": "To save a pointer indirection", "correct": false}, {"key": "B", "text": "Because there is then no way to reach the data without going through <code>lock()</code>", "correct": true}, {"key": "C", "text": "Because <code>Mutex</code> needs to know the size of <code>T</code> to allocate", "correct": false}, {"key": "D", "text": "So that <code>T</code> can implement <code>Drop</code>", "correct": false}], "answer": "B", "why": "<p>In C, <code>pthread_mutex_t lock;</code> and <code>int counter;</code> are two unrelated variables and the connection between them lives in a comment. Nothing stops you touching the counter without the lock, and nothing warns you when someone does.</p><p><code>Mutex&lt;i32&gt;</code> has no field you can reach except through <code>lock()</code>, which returns the guard. \"Forgot to take the lock\" stops being a class of bug and becomes a type error, the same trick as putting ownership in the type system rather than in a convention.</p>"}, {"n": 8, "stem": "<p>When is the lock released here?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let mut n = counter.lock().unwrap();\ndo_slow_io();\n*n += 1;\nprintln!(\"done\");</code></pre></div>", "options": [{"key": "A", "text": "Immediately after <code>lock()</code> returns", "correct": false}, {"key": "B", "text": "After <code>*n += 1;</code>", "correct": false}, {"key": "C", "text": "At the end of the enclosing block, when <code>n</code> is dropped", "correct": true}, {"key": "D", "text": "When <code>unwrap()</code> is called", "correct": false}], "answer": "C", "why": "<p>The guard is an ordinary value bound to <code>n</code>, so it lives until end of scope and unlocks in its <code>Drop</code>. That means <code>do_slow_io()</code> runs with the lock held, and every other thread is serialised behind your slowest operation.</p><p>The idiom that avoids it is to make the guard a temporary: <code>*counter.lock().unwrap() += 1;</code>, where the guard is dropped at the end of that statement. Or bind it inside a <code>{ }</code> around only the lines that need it.</p>"}, {"n": 9, "stem": "<p>What happens with <code>let _ = counter.lock().unwrap();</code>?</p>", "options": [{"key": "A", "text": "The lock is held to the end of the scope", "correct": false}, {"key": "B", "text": "The lock is taken and released on that line", "correct": true}, {"key": "C", "text": "It does not compile, because the guard must be bound", "correct": false}, {"key": "D", "text": "The lock is never taken because the value is unused", "correct": false}], "answer": "B", "why": "<p><code>_</code> is not a binding; it is a pattern that matches and discards. The guard is therefore a temporary with no owner and is dropped at the end of the statement, so the mutex is locked and unlocked immediately and every following line runs unprotected.</p><p><code>let _guard = ...</code> is a real name, even with the leading underscore, and holds it to end of scope. One character apart, and the wrong one silently removes the protection rather than failing.</p>"}, {"n": 10, "stem": "<p>Two threads take <code>m1</code> then <code>m2</code>; a third takes <code>m2</code> then <code>m1</code>. What does Rust do?</p>", "options": [{"key": "A", "text": "Rejects it at compile time, since this is a data race", "correct": false}, {"key": "B", "text": "Panics at runtime with \"deadlock detected\"", "correct": false}, {"key": "C", "text": "Compiles and runs, and may deadlock", "correct": true}, {"key": "D", "text": "Reorders the locks automatically", "correct": false}], "answer": "C", "why": "<p>Deadlock is not a data race and no borrow rule is broken: each thread has genuine exclusive access to everything it holds. What is wrong is the <em>order</em> of two statements, and lock order is not a property of any type, so nothing in the type system can see it.</p><p>This is the honest boundary of the guarantee. Rust promises no data races and no use-after-free, not \"no concurrency bugs\". Deadlock, livelock, <code>Rc</code> cycles and unbounded queues are all still yours, and the discipline is the same as in C: one global lock order, or one lock at a time.</p>"}, {"n": 11, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>use std::sync::mpsc;\nlet (tx, rx) = mpsc::channel();\nlet msg = String::from(\"hi\");\ntx.send(msg).unwrap();\nprintln!(\"{msg}\");</code></pre></div>", "options": [{"key": "A", "text": "Yes, because <code>send</code> copies the message into the channel", "correct": false}, {"key": "B", "text": "No, because <code>send</code> takes the value by value, so <code>msg</code> was moved", "correct": true}, {"key": "C", "text": "No, because a channel needs at least two threads", "correct": false}, {"key": "D", "text": "Yes, but the receiver would get an empty string", "correct": false}], "answer": "B", "why": "<p><code>error[E0382]</code>. <code>send</code> is <code>fn send(&amp;self, t: T)</code>, so the message moves into the channel.</p><p>That is the entire safety argument for channels, not an implementation detail: after the send, the producer holds no reference to the message, so the consumer has nothing to race against and no lock is required. In a C queue of pointers the same rule exists as a comment; here it is a compile error.</p>"}, {"n": 12, "stem": "<p>Why does this program hang?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let (tx, rx) = mpsc::channel();\nfor i in 0..3 {\n    let tx = tx.clone();\n    thread::spawn(move || tx.send(i).unwrap());\n}\nfor got in rx { println!(\"{got}\"); }</code></pre></div>", "options": [{"key": "A", "text": "The threads are never joined", "correct": false}, {"key": "B", "text": "<code>rx</code> needs to be wrapped in a <code>Mutex</code>", "correct": false}, {"key": "C", "text": "The original <code>tx</code> is still alive, so the receiver never sees the channel close", "correct": true}, {"key": "D", "text": "Three sends cannot fit in an unbounded channel", "correct": false}], "answer": "C", "why": "<p>Iterating a <code>Receiver</code> ends when <strong>every</strong> <code>Sender</code> has been dropped. Three clones go into threads and are dropped there, but the original <code>tx</code> is still in scope in <code>main</code>, so the receiver keeps waiting for a fourth message that nobody will send.</p><p><code>drop(tx)</code> after the spawn loop fixes it. The symptom is distinctive and worth recognising: a hung program using no CPU at all, because every thread is parked rather than spinning.</p>"}, {"n": 13, "stem": "<p>What is the cost difference between <code>Rc::clone</code> and <code>Arc::clone</code>?</p>", "options": [{"key": "A", "text": "<code>Arc::clone</code> allocates; <code>Rc::clone</code> does not", "correct": false}, {"key": "B", "text": "<code>Arc::clone</code> deep-copies the value", "correct": false}, {"key": "C", "text": "<code>Arc::clone</code> uses an atomic increment instead of a plain one", "correct": true}, {"key": "D", "text": "None; they compile to the same instructions", "correct": false}], "answer": "C", "why": "<p>Both copy a pointer and bump a count. The only difference is that <code>Arc</code> uses <code>fetch_add</code> with an atomic ordering, which prevents the two cores from losing an update. It costs a few nanoseconds and, on a contended cache line, a little more.</p><p>Neither allocates and neither touches the pointee. This is why Rust ships both rather than making everything atomic: most reference counting in most programs is single-threaded, and there is no reason to charge it for a guarantee it does not use.</p>"}, {"n": 14, "stem": "<p>Which of these need <code>Arc</code>? Choose all that apply.</p>", "options": [{"key": "A", "text": "Sharing a <code>Vec</code> with a thread spawned by <code>thread::spawn</code>", "correct": true}, {"key": "B", "text": "Sharing a <code>Vec</code> with threads inside <code>thread::scope</code>", "correct": false}, {"key": "C", "text": "Keeping a <code>Mutex</code> alive across several <code>thread::spawn</code> calls", "correct": true}, {"key": "D", "text": "Passing a <code>String</code> to one thread that is the only user of it", "correct": false}], "answer": "AC", "why": "<p>B is the one worth remembering. <code>thread::scope</code> joins every thread before it returns, so the compiler knows the borrows cannot outlive the local. Plain <code>&amp;</code> and <code>&amp;mut</code> are allowed, with nothing cloned and no <code>Arc</code> in sight. Before this stabilised in 1.63 people reached for <code>Arc</code> because there was nothing else, and a lot of code still carries that habit.</p><p>D needs nothing at all: one user means ownership, so <code>move</code> the <code>String</code> in.</p>"}, {"n": 15, "stem": "<p>A counter incremented from many threads and never read until the end. What is the cheapest correct choice?</p>", "options": [{"key": "A", "text": "<code>Arc&lt;Mutex&lt;u64&gt;&gt;</code>", "correct": false}, {"key": "B", "text": "<code>Arc&lt;AtomicU64&gt;</code> with <code>Ordering::Relaxed</code>", "correct": true}, {"key": "C", "text": "<code>Arc&lt;RwLock&lt;u64&gt;&gt;</code>", "correct": false}, {"key": "D", "text": "<code>Rc&lt;Cell&lt;u64&gt;&gt;</code>", "correct": false}], "answer": "B", "why": "<p>An atomic add is one instruction that never blocks and leaves no guard. A <code>Mutex</code> is correct but adds a lock, a guard, an unlock and potential contention for an operation the hardware already does atomically.</p><p><code>Relaxed</code> is sufficient precisely because of the \"never read until the end\" condition: nothing branches on the intermediate value, so no ordering with respect to other memory is needed. The moment another thread uses the counter to decide something, you need <code>Acquire</code>/<code>Release</code>. And if you have to think about which, a <code>Mutex</code> is usually the better trade.</p>"}]}