{"unit": "08-structs", "questions": [{"n": 1, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>struct Config { host: String, port: u16 }\n\nlet c = Config { host: String::from(\"localhost\") };</code></pre></div>", "options": [{"key": "A", "text": "Yes, <code>port</code> defaults to 0", "correct": false}, {"key": "B", "text": "No, every field must be given a value", "correct": true}, {"key": "C", "text": "Yes, but <code>c.port</code> panics when read", "correct": false}, {"key": "D", "text": "No, <code>host</code> must be a <code>&amp;str</code>", "correct": false}], "answer": "B", "why": "<p><code>error[E0063]: missing field </code>port<code> in initializer of </code>Config`<code>. There is no partial construction and no implicit default: a </code>Config` either has both fields or does not exist.</p><p>A is the intuition from C, Go or Java, where an uninitialised field is zero or null and every method downstream has to defend against it. Rust's position is that a type should not be able to hold a value that makes no sense.</p><p>The two ways to write fewer field names, <code>..other</code> and <code>..Default::default()</code>, are not exceptions. Both still produce a complete struct; they source the missing values from somewhere else.</p>"}, {"n": 2, "stem": "<p>What is the difference between an associated function and a method?</p>", "options": [{"key": "A", "text": "A method takes a form of <code>self</code> as its first parameter; an associated function does not", "correct": true}, {"key": "B", "text": "Methods are public, associated functions are private", "correct": false}, {"key": "C", "text": "Associated functions must be called <code>new</code>", "correct": false}, {"key": "D", "text": "Methods live in <code>impl</code> blocks, associated functions do not", "correct": false}], "answer": "A", "why": "<p>That is the entire distinction, and it decides the call syntax: <code>Config::new(x)</code> with <code>::</code> for the one with no receiver, <code>c.url()</code> with <code>.</code> for the one with a receiver. Both live in the same <code>impl</code> block.</p><p>C is worth killing explicitly. <code>new</code> is a <strong>convention</strong>, and nothing in the language knows the name. A type may have several constructors (<code>Vec::new</code>, <code>Vec::with_capacity</code>, <code>Vec::from</code>) or none at all, and calling one <code>create</code> or <code>open</code> is perfectly legal.</p>"}, {"n": 3, "stem": "<p><code>fn area(self) -&gt; u32</code> on a non-<code>Copy</code> struct. What does calling <code>r.area()</code> commit the caller to?</p>", "options": [{"key": "A", "text": "Nothing, <code>self</code> is passed by reference internally", "correct": false}, {"key": "B", "text": "Giving up ownership of <code>r</code>; it cannot be named afterwards", "correct": true}, {"key": "C", "text": "Declaring <code>r</code> as <code>mut</code>", "correct": false}, {"key": "D", "text": "Cloning <code>r</code> at the call site", "correct": false}], "answer": "B", "why": "<p><code>r.area()</code> is <code>Rect::area(r)</code> with the receiver moved to the left of the dot. A by-value receiver is a by-value argument, so the value moves in and the binding is dead afterwards, giving <code>error[E0382]</code> on the next use.</p><p>A is the Python intuition, where <code>self</code> is always a reference. Rust splits that one word into three (<code>&amp;self</code>, <code>&amp;mut self</code>, <code>self</code>) and the split is in the signature, so a caller knows before calling whether a method reads, writes or consumes.</p><p>Taking <code>self</code> for a getter is the most common self-inflicted wound in a new API: it compiles fine while you write the type and forces <code>.clone()</code> on everyone who uses it.</p>"}, {"n": 4, "stem": "<p>Which receiver should a method that only reads two fields and returns a number have?</p>", "options": [{"key": "A", "text": "<code>&amp;self</code>", "correct": true}, {"key": "B", "text": "<code>&amp;mut self</code>", "correct": false}, {"key": "C", "text": "<code>self</code>", "correct": false}, {"key": "D", "text": "No receiver, make it an associated function taking the struct", "correct": false}], "answer": "A", "why": "<p><code>&amp;self</code> asks for the least and therefore permits the most: the caller keeps the value, can call the method twice, can hold other shared borrows at the same time, and can call it through a <code>&amp;Config</code> it does not own.</p><p><code>&amp;mut self</code> would compile and would then require a <code>mut</code> binding at every call site and exclude every other live borrow, for no benefit. <code>self</code> would destroy the value.</p><p>The rule of thumb: <strong><code>&amp;self</code> unless you must mutate, <code>&amp;mut self</code> unless you must consume.</strong> Take <code>self</code> for exactly two reasons: moving a field out without copying it, or deliberately ending the value's life (<code>build</code>, <code>close</code>, <code>finish</code>).</p>"}, {"n": 5, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>struct Settings { host: String, port: u16 }\n\nlet base = Settings { host: String::from(\"x\"), port: 80 };\nlet dev = Settings { port: 3000, ..base };\nprintln!(\"{}\", base.port);</code></pre></div>", "options": [{"key": "A", "text": "Yes, <code>..base</code> copies the remaining fields", "correct": false}, {"key": "B", "text": "No, <code>..base</code> moved <code>host</code> out, so <code>base</code> is partially moved", "correct": true}, {"key": "C", "text": "No, you cannot use <code>..</code> with only two fields", "correct": false}, {"key": "D", "text": "Yes, because <code>port</code> was listed explicitly and never touched", "correct": false}], "answer": "B", "why": "<p><code>error[E0382]: borrow of partially moved value: base</code>. <code>..base</code> does not copy the struct; it moves each field it needs, one at a time. <code>host</code> is a <code>String</code>, so its heap handle moved into <code>dev</code>, and a struct with a moved-out field cannot be used as a value any more.</p><p>D is the sharpest distractor because its reasoning is half right: <code>port</code> really was untouched. But <code>base</code> as a whole is no longer a complete <code>Settings</code>, and the <code>{}</code> in the <code>println!</code> borrows the struct to reach the field.</p><p>The trap is that this depends entirely on the field types. Drop the <code>String</code> and the identical code compiles, because <code>u16</code> is <code>Copy</code>.</p>"}, {"n": 6, "stem": "<p>What does <code>#[derive(Clone)]</code> actually generate?</p>", "options": [{"key": "A", "text": "A memcpy of the struct's bytes", "correct": false}, {"key": "B", "text": "A <code>clone</code> method that calls <code>clone</code> on every field", "correct": true}, {"key": "C", "text": "A deep copy of everything reachable, including behind references", "correct": false}, {"key": "D", "text": "Nothing, it is a marker trait", "correct": false}], "answer": "B", "why": "<p>The derive is mechanical: field by field, calling each field's own <code>clone</code>. That is why it requires every field to be <code>Clone</code>, and why the depth of the copy is whatever the fields say it is.</p><p>C is the interesting near-miss. Cloning a struct holding a <code>&amp;T</code> clones the <em>reference</em>, which copies an address rather than the thing behind it. Cloning one holding an <code>Rc&lt;T&gt;</code> increments a count and shares the same allocation. \"Deep\" and \"shallow\" are not properties of <code>Clone</code>; they are properties of the fields.</p><p>A describes <code>Copy</code>, which is a different trait, generates no code at all, and is forbidden for any type with a <code>Drop</code> impl.</p>"}, {"n": 7, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>#[derive(Copy, Clone)]\nstruct User { id: u32, name: String }</code></pre></div>", "options": [{"key": "A", "text": "Yes", "correct": false}, {"key": "B", "text": "No, <code>String</code> is not <code>Copy</code>, so <code>User</code> cannot be", "correct": true}, {"key": "C", "text": "No, you cannot derive two traits at once", "correct": false}, {"key": "D", "text": "Yes, but <code>name</code> is shared between copies", "correct": false}], "answer": "B", "why": "<p><code>error[E0204]: the trait Copy cannot be implemented for this type; field name does not implement Copy</code>. <code>Copy</code> is inherited from the fields, and one owned field rules it out permanently, no matter how many <code>u32</code>s sit beside it.</p><p>D describes what would happen if it were allowed, and is exactly why it is not: two <code>User</code> values pointing at one heap buffer, both dropping it. <code>Copy</code> and <code>Drop</code> are mutually exclusive for this reason.</p><p>Removing <code>Copy</code> and keeping <code>Clone</code> is the fix, and it costs nothing you wanted: the difference is only whether duplication is implicit or written down.</p>"}, {"n": 8, "stem": "<p>What does <code>{:#?}</code> do that <code>{:?}</code> does not?</p>", "options": [{"key": "A", "text": "It prints private fields as well", "correct": false}, {"key": "B", "text": "It prints one field per line, indented", "correct": true}, {"key": "C", "text": "It uses <code>Display</code> instead of <code>Debug</code>", "correct": false}, {"key": "D", "text": "It escapes non-ASCII characters", "correct": false}], "answer": "B", "why": "<p><code>{:#?}</code> is the <em>alternate</em> form of the same <code>Debug</code> impl: the <code>#</code> flag is passed to the formatter, and the derived implementation responds by spreading the output over multiple lines.</p><p>Both forms print every field regardless of visibility; <code>Debug</code> is generated inside the type's own module, so privacy does not apply to it.</p><p><code>{}</code> is <code>Display</code> and is never derived: how a value should be shown to a user has no mechanical answer, so Rust makes you write it or do without.</p><p>Worth adopting: <code>dbg!(&amp;x)</code> prints the file, the line, the expression source <em>and</em> the <code>{:#?}</code> form, then hands the value back.</p>"}, {"n": 9, "stem": "<p><code>#[derive(Default)]</code> on <code>struct Client { timeout_ms: u32, retries: u8 }</code> produces <code>Client { timeout_ms: 0, retries: 0 }</code>. When is that the wrong thing to want?</p>", "options": [{"key": "A", "text": "Whenever zero is a meaningful but wrong value for the field", "correct": true}, {"key": "B", "text": "Whenever the struct has more than three fields", "correct": false}, {"key": "C", "text": "Never, the derive is always the right default", "correct": false}, {"key": "D", "text": "Whenever the struct derives <code>Debug</code>", "correct": false}], "answer": "A", "why": "<p>A timeout of zero and zero retries is not a neutral starting point, it is a broken client, and the derive produces it silently, so nothing points at the problem until something times out immediately in production.</p><p>Derive <code>Default</code> when the zero really is the sensible start: a counter, an empty collection, an accumulator. Write the impl by hand the moment a field has a meaningful default that is not the zero.</p><p><code>Default</code> earns its keep either way through <code>..Default::default()</code>, which is the closest Rust has to named optional arguments: list the fields that differ, let the impl supply the rest, and adding a field later breaks no call site.</p>"}, {"n": 10, "stem": "<p>Why does the newtype <code>struct Cents(u64);</code> cost nothing at runtime?</p>", "options": [{"key": "A", "text": "A single-field struct has exactly the layout of its field", "correct": true}, {"key": "B", "text": "The compiler caches the wrapper", "correct": false}, {"key": "C", "text": "It is optimised away only in release builds", "correct": false}, {"key": "D", "text": "It does cost: one extra pointer indirection", "correct": false}], "answer": "A", "why": "<p>A struct is its fields laid out contiguously. One field, with no tag, no header and no vtable, so <code>Cents</code> is eight bytes with the same alignment as <code>u64</code>, and the arithmetic compiles to the same instruction it would have without the wrapper.</p><p>C is a real misconception about Rust generally. Layout is not an optimisation; <code>size_of::&lt;Cents&gt;() == size_of::&lt;u64&gt;()</code> is true in a debug build too. What debug builds keep is the bounds checks and the overflow checks, not extra struct headers.</p><p>What you bought for that zero cost is a compile error when a <code>Dollars</code> is passed where <code>Cents</code> was expected. A unit that used to live in a variable name now sits in the type system, where it is checked.</p>"}, {"n": 11, "stem": "<p>You want <code>impl Display for Vec&lt;String&gt;</code>. It is rejected. What is the fix?</p>", "options": [{"key": "A", "text": "Implement it in the same file as <code>Vec</code>", "correct": false}, {"key": "B", "text": "Wrap it: <code>struct Csv(Vec&lt;String&gt;);</code> and implement <code>Display</code> for that", "correct": true}, {"key": "C", "text": "Add <code>#[derive(Display)]</code> to your module", "correct": false}, {"key": "D", "text": "Use <code>impl Display for &amp;Vec&lt;String&gt;</code> instead", "correct": false}], "answer": "B", "why": "<p>The coherence rules require that either the trait or the type be local to your crate, so that two crates cannot each define a different <code>Display for Vec&lt;String&gt;</code> and break whichever one links second. Neither half is yours here.</p><p>A newtype makes the type local, and the impl is then allowed. This is why newtypes appear all over real crates: it is the standard answer to \"the type I need to extend belongs to somebody else\".</p><p>C is not a thing: <code>Display</code> has no derive, on purpose. D changes nothing: <code>&amp;Vec&lt;String&gt;</code> is still built entirely from foreign types.</p>"}, {"n": 12, "stem": "<p><code>struct Record { id: u8, count: u32, flag: bool }</code>. What is <code>size_of::&lt;Record&gt;()</code>?</p>", "options": [{"key": "A", "text": "6, one plus four plus one", "correct": false}, {"key": "B", "text": "12, with C's padding rules", "correct": false}, {"key": "C", "text": "8, because the compiler reorders the fields", "correct": true}, {"key": "D", "text": "Unspecified, so the question has no answer", "correct": false}], "answer": "C", "why": "<p>Rust's default layout is unspecified, and the compiler uses that freedom to reorder: <code>count</code> first at four bytes, then <code>id</code> and <code>flag</code> sharing the tail, then two bytes of padding to keep the whole thing four-aligned. Eight.</p><p>B is what <code>#[repr(C)]</code> gives you: declaration order preserved, three bytes of padding after <code>id</code> to align <code>count</code>, three more after <code>flag</code>. Twelve bytes for the same six bytes of data.</p><p>A ignores alignment entirely. A <code>u32</code> must sit at an address divisible by four; a misaligned load is slower on x86 and a fault on some architectures, so the padding is not waste, it is the cost of the alignment.</p><p>D is technically the pedantic answer and practically unhelpful: the layout is unspecified so that the compiler <em>may</em> do this, and it does.</p>"}, {"n": 13, "stem": "<p>When do you need <code>#[repr(C)]</code>?</p>", "options": [{"key": "A", "text": "Whenever a struct has more than four fields", "correct": false}, {"key": "B", "text": "When the bytes are shared with something outside Rust", "correct": true}, {"key": "C", "text": "To make a struct <code>Copy</code>", "correct": false}, {"key": "D", "text": "To make a struct smaller", "correct": false}], "answer": "B", "why": "<p><code>#[repr(C)]</code> pins the layout to C's rules: declaration order, C's padding. You need it exactly when something other than rustc is going to read those bytes: an FFI call, a memory-mapped hardware register, a file or wire format read with a straight cast.</p><p>D has it backwards. C's rules usually make the struct <em>larger</em>, because they forbid the reordering that lets narrow fields share a gap. <code>#[repr(C)]</code> on the <code>Record</code> above takes it from eight bytes to twelve.</p><p>Everywhere else, leave it off. Field order in your source stays a readability decision rather than a performance one, which is a small freedom you get to keep.</p>"}, {"n": 14, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>struct B { n: u32 }\nimpl B {\n    fn set(&amp;mut self, n: u32) -&gt; &amp;mut Self { self.n = n; self }\n    fn build(self) -&gt; u32 { self.n }\n}\n\nlet x = B { n: 0 }.set(5).build();</code></pre></div>", "options": [{"key": "A", "text": "Yes", "correct": false}, {"key": "B", "text": "No, <code>build</code> needs the value and <code>set</code> returned a borrow", "correct": true}, {"key": "C", "text": "No, <code>B { n: 0 }</code> is a temporary and cannot have methods called on it", "correct": false}, {"key": "D", "text": "Yes, but <code>x</code> is a reference", "correct": false}], "answer": "B", "why": "<p><code>error[E0507]: cannot move out of a mutable reference</code>. <code>set</code> hands back <code>&amp;mut B</code>, and <code>build(self)</code> needs the <code>B</code> itself, by value. Moving out of a borrow is never allowed: the borrow is a promise to give the value back intact, and a moved-from value is not.</p><p>The fix that makes chained builders work is <code>fn set(mut self, n: u32) -&gt; Self</code>. Ownership then walks down the chain one call at a time and arrives at <code>build</code> still owned.</p><p>C is wrong and worth being sure about: methods can be called on temporaries, and the temporary lives to the end of the statement.</p>"}, {"n": 15, "stem": "<p>What is <code>mut self</code> in a parameter list?</p>", "options": [{"key": "A", "text": "A fourth kind of receiver, between <code>&amp;mut self</code> and <code>self</code>", "correct": false}, {"key": "B", "text": "<code>self</code>, moved in, with the local binding marked mutable", "correct": true}, {"key": "C", "text": "A way to mutate the caller's value in place", "correct": false}, {"key": "D", "text": "Invalid syntax", "correct": false}], "answer": "B", "why": "<p>The value is moved in exactly as with <code>self</code>; <code>mut</code> only says the body may reassign or mutate its own binding. There are three receivers, not four.</p><p>C is the trap. The caller's value has been moved away, so there is nothing left to mutate in place. If you want the caller to keep the value and see the change, that is <code>&amp;mut self</code>.</p><p>Mutability of a parameter is never part of a signature's contract, which is why you can add or remove the <code>mut</code> on any parameter without it being a breaking change. It is a statement about the body, not about the interface.</p>"}]}