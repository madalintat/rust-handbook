{"unit": "14-traits", "questions": [{"n": 1, "stem": "<p>Your crate defines <code>trait Loud</code> and <code>struct Bot</code>. Which impls are legal? Choose all that apply.</p>", "options": [{"key": "A", "text": "<code>impl Loud for Bot</code>", "correct": true}, {"key": "B", "text": "<code>impl Loud for String</code>", "correct": true}, {"key": "C", "text": "<code>impl std::fmt::Display for Bot</code>", "correct": true}, {"key": "D", "text": "<code>impl std::fmt::Display for Vec&lt;String&gt;</code>", "correct": false}, {"key": "E", "text": "<code>impl Loud for Vec&lt;String&gt;</code>", "correct": true}], "answer": "ABCE", "why": "<p>The orphan rule: you may write <code>impl Trait for Type</code> only if you own the trait or you own the type. A, B and E are legal because <code>Loud</code> is yours; C is legal because <code>Bot</code> is yours. Only D is both-foreign.</p><p>The point of the restriction is <span class=\"term\" data-g=\"The restriction that a trait impl must be in the crate defining either the trait or the type, so two crates cannot supply conflicting impls.\" data-t=\"coherence\">coherence</span>. If two crates could each implement <code>Display for Vec&lt;String&gt;</code>, a program depending on both would have two candidate impls, no principled way to choose, and no author able to see the conflict. The rule guarantees at most one impl per (trait, type) pair in any linked program.</p>"}, {"n": 2, "stem": "<p>What does wrapping <code>Vec&lt;String&gt;</code> in <code>struct Csv(Vec&lt;String&gt;)</code> cost at runtime?</p>", "options": [{"key": "A", "text": "Nothing \u2014 same size, same layout, no indirection", "correct": true}, {"key": "B", "text": "One pointer indirection per access", "correct": false}, {"key": "C", "text": "A heap allocation for the wrapper", "correct": false}, {"key": "D", "text": "A vtable pointer alongside the data", "correct": false}], "answer": "A", "why": "<p>A single-field tuple struct has the layout of its field. <code>Csv</code> <em>is</em> a <code>Vec&lt;String&gt;</code> in memory; the wrapper exists in the type checker and is gone by codegen.</p><p>That is what makes newtype the right answer to the orphan rule rather than a grudging workaround. It also earns its place independently: <code>struct UserId(u64)</code> and <code>struct OrderId(u64)</code> are both a <code>u64</code> and cannot be passed to the wrong function by accident.</p>"}, {"n": 3, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>trait Named { fn name(&amp;self) -&gt; String; }\ntrait Greet: Named {\n    fn greet(&amp;self) -&gt; String { format!(\"hi, {}\", self.name()) }\n}\nstruct Bot;\nimpl Greet for Bot {}</code></pre></div>", "options": [{"key": "A", "text": "Yes \u2014 <code>greet</code> has a default body, so nothing is missing", "correct": false}, {"key": "B", "text": "No \u2014 <code>Bot</code> must implement <code>Named</code> as well", "correct": true}, {"key": "C", "text": "No \u2014 a trait cannot have a default method that calls another trait's method", "correct": false}, {"key": "D", "text": "Yes, and <code>name</code> falls back to the type name", "correct": false}], "answer": "B", "why": "<p><code>trait Greet: Named</code> is not inheritance. It is a bound on <code>Self</code>: anything implementing <code>Greet</code> must also implement <code>Named</code>. <code>Bot</code> does not, so <code>error[E0277]: the trait bound Bot: Named is not satisfied</code>.</p><p>A is tempting because the impl block really is complete \u2014 there are no missing <em>items</em>. The failure is the supertrait obligation, which is checked separately. Nothing is inherited: <code>Bot</code> needs two impl blocks, one per trait.</p>"}, {"n": 4, "stem": "<p>On a 64-bit machine, what is <code>size_of::&lt;&amp;dyn Display&gt;()</code>?</p>", "options": [{"key": "A", "text": "8", "correct": false}, {"key": "B", "text": "16", "correct": true}, {"key": "C", "text": "24", "correct": false}, {"key": "D", "text": "It depends on the concrete type", "correct": false}], "answer": "B", "why": "<p>A trait object is a <span class=\"term\" data-g=\"A pointer carrying a second word of metadata \u2014 a length for slices, a vtable address for trait objects.\" data-t=\"fat pointer\">fat pointer</span>: one word to the data, one word to the vtable for that (type, trait) pair. Sixteen bytes, regardless of what it points at \u2014 which is exactly why D is wrong and why the whole mechanism works.</p><p><code>&amp;str</code> and <code>&amp;[T]</code> are fat for a different reason: their second word is a length rather than a vtable pointer. Same size, different payload.</p>"}, {"n": 5, "stem": "<p>What does a vtable contain?</p>", "options": [{"key": "A", "text": "A copy of the value's fields", "correct": false}, {"key": "B", "text": "The type's size and alignment, a destructor pointer, and one function pointer per dyn-visible trait method", "correct": true}, {"key": "C", "text": "The type's name, for reflection", "correct": false}, {"key": "D", "text": "A reference count", "correct": false}], "answer": "B", "why": "<p>The vtable is a small static table in read-only data, one per (type, trait) pair, shared by every trait object of that type. It carries <code>size</code>, <code>align</code>, <code>drop_in_place</code> and the method addresses \u2014 enough to call, and enough to drop and deallocate correctly without knowing the concrete type.</p><p>C is what a language with runtime reflection would store. Rust has none; the type name is not there, which is why you cannot downcast a <code>dyn Trait</code> without opting into <code>Any</code>.</p>"}, {"n": 6, "stem": "<p>Which method prevents this trait from being used as <code>dyn Store</code>?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>trait Store {\n    fn len(&amp;self) -&gt; usize;\n    fn save&lt;T: Display&gt;(&amp;self, item: T);\n    fn clear(&amp;mut self);\n}</code></pre></div>", "options": [{"key": "A", "text": "<code>len</code> \u2014 it returns a value", "correct": false}, {"key": "B", "text": "<code>save</code> \u2014 it is generic", "correct": true}, {"key": "C", "text": "<code>clear</code> \u2014 it takes <code>&amp;mut self</code>", "correct": false}, {"key": "D", "text": "None; the trait is already object safe", "correct": false}], "answer": "B", "why": "<p>A vtable is a fixed table of function pointers, built when the crate is compiled. <code>save&lt;T&gt;</code> is monomorphised, so it is not one function \u2014 it is one per <code>T</code> anyone ever passes, across crates that may not exist yet. There is no single address to put in the slot. The error is <code>E0038</code>.</p><p><code>&amp;self</code> and <code>&amp;mut self</code> are both fine; a receiver is exactly what dispatch needs. The other disqualifiers are methods with no <code>self</code> at all, and methods returning <code>Self</code> by value, whose size the caller cannot know.</p><p>The escape hatch is <code>where Self: Sized</code> on <code>save</code>, which drops it from the vtable and leaves the rest usable as <code>dyn</code>. <code>Iterator</code> does this for all seventy of its adapters.</p>"}, {"n": 7, "stem": "<p>Why is <code>Iterator</code>'s <code>Item</code> an associated type rather than <code>Iterator&lt;T&gt;</code>?</p>", "options": [{"key": "A", "text": "Associated types compile faster", "correct": false}, {"key": "B", "text": "An iterator yields exactly one type, so the implementor should choose it, not the caller", "correct": true}, {"key": "C", "text": "Type parameters cannot be used in return position", "correct": false}, {"key": "D", "text": "Because <code>Item</code> can then be inferred at runtime", "correct": false}], "answer": "B", "why": "<p>A <code>Vec&lt;u8&gt;</code>'s iterator yields <code>u8</code> and nothing else \u2014 one right answer, and it belongs to the implementor. Were it <code>Iterator&lt;T&gt;</code>, a type could implement it several times, <code>for x in it</code> would be ambiguous, and annotations would be required everywhere.</p><p>Compare <code>From&lt;T&gt;</code>, which <em>is</em> a parameter, precisely because <code>String</code> genuinely converts from <code>&amp;str</code>, from <code>char</code>, from <code>Box&lt;str&gt;</code>. Several answers, chosen by the caller.</p><p>The rule: one answer per implementing type \u2192 associated type; several, caller picks \u2192 type parameter.</p>"}, {"n": 8, "stem": "<p>You implement <code>Display</code> for your type. Which of these do you now also get, without writing anything else? Choose all that apply.</p>", "options": [{"key": "A", "text": "<code>.to_string()</code>", "correct": true}, {"key": "B", "text": "<code>format!(\"{}\", x)</code>", "correct": true}, {"key": "C", "text": "<code>format!(\"{:?}\", x)</code>", "correct": false}, {"key": "D", "text": "Passing it to <code>fn f&lt;T: Display&gt;(x: T)</code>", "correct": true}, {"key": "E", "text": "<code>.clone()</code>", "correct": false}], "answer": "ABD", "why": "<p><code>.to_string()</code> arrives through a <span class=\"term\" data-g=\"Implementing a trait for every type satisfying some bound at once, such as impl&lt;T: Display&gt; ToString for T.\" data-t=\"blanket impl\">blanket impl</span> in the standard library: <code>impl&lt;T: Display&gt; ToString for T</code>. One block, applying to every current and future <code>Display</code> type. You never implement <code>ToString</code> by hand.</p><p>C is the trap. <code>Debug</code> and <code>Display</code> are separate traits with separate impls \u2014 <code>{:?}</code> needs <code>#[derive(Debug)]</code>. They are deliberately distinct: <code>Debug</code> is for programmers and may be ugly, <code>Display</code> is for users and cannot be guessed.</p>"}, {"n": 9, "stem": "<p>What does this print?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>trait Greet {\n    fn name(&amp;self) -&gt; String;\n    fn hello(&amp;self) -&gt; String { format!(\"hello, {}\", self.name()) }\n}\nstruct Bot;\nimpl Greet for Bot {\n    fn name(&amp;self) -&gt; String { \"bot\".into() }\n    fn hello(&amp;self) -&gt; String { \"oi\".into() }\n}\nfn main() { println!(\"{}\", Bot.hello()); }</code></pre></div>", "options": [{"key": "A", "text": "<code>hello, bot</code>", "correct": false}, {"key": "B", "text": "<code>oi</code>", "correct": true}, {"key": "C", "text": "It does not compile \u2014 <code>hello</code> already has a body", "correct": false}, {"key": "D", "text": "<code>hello, oi</code>", "correct": false}], "answer": "B", "why": "<p>A default method is a fallback, not a final method. An impl that supplies its own <code>hello</code> overrides it, and nothing dispatches back to the default.</p><p>C is the reasonable-sounding wrong answer, and the distinction matters: a method <em>without</em> a body is a requirement (skip it and you get <code>E0046</code>), a method <em>with</em> one is a default (override it or don't). Overriding is how a type that already knows its length gives <code>Iterator::count</code> an O(1) implementation instead of walking the whole sequence.</p>"}, {"n": 10, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn pick(flag: bool) -&gt; impl Iterator&lt;Item = u32&gt; {\n    if flag { (1..4).map(|n| n * 2) } else { (1..4).filter(|n| n % 2 == 0) }\n}</code></pre></div>", "options": [{"key": "A", "text": "Yes \u2014 both are iterators of <code>u32</code>", "correct": false}, {"key": "B", "text": "No \u2014 <code>impl Trait</code> in return position means one concrete type, and these are two", "correct": true}, {"key": "C", "text": "No \u2014 you cannot return an iterator from a function", "correct": false}, {"key": "D", "text": "Yes, but only with a <code>Box</code>", "correct": false}], "answer": "B", "why": "<p><code>-&gt; impl Iterator</code> does not mean \"some iterator, decided at runtime\". It means <em>one specific type, which I decline to name</em> \u2014 usually because the type is an unnameable compiler-generated closure or adapter. <code>Map&lt;...&gt;</code> and <code>Filter&lt;...&gt;</code> are different types, so the two branches disagree and rustc reports <code>E0308</code>.</p><p>When the branches genuinely differ you need dynamic dispatch: <code>Box&lt;dyn Iterator&lt;Item = u32&gt;&gt;</code>, at the cost of an allocation and a vtable. The difference between A and B is precisely the difference between static and dynamic dispatch.</p>"}, {"n": 11, "stem": "<p>You want <code>let f: Fahrenheit = c.into();</code> to work for your <code>Celsius</code>. What do you implement?</p>", "options": [{"key": "A", "text": "<code>impl Into&lt;Fahrenheit&gt; for Celsius</code>", "correct": false}, {"key": "B", "text": "<code>impl From&lt;Celsius&gt; for Fahrenheit</code>", "correct": true}, {"key": "C", "text": "Both", "correct": false}, {"key": "D", "text": "<code>impl AsRef&lt;Fahrenheit&gt; for Celsius</code>", "correct": false}], "answer": "B", "why": "<p>A blanket <code>impl&lt;T, U: From&lt;T&gt;&gt; Into&lt;U&gt; for T</code> in the standard library means one <code>From</code> impl gives you both directions of syntax. Implementing <code>Into</code> by hand gives you only <code>Into</code>, and duplicates work the library already did.</p><p>A is worse than merely redundant when a foreign type is involved: <code>Into</code> is std's, so <code>impl Into&lt;Fahrenheit&gt; for ParseIntError</code> would be std's trait on std's type and rejected by the orphan rule. <code>impl From&lt;ParseIntError&gt; for MyError</code> is legal because the target type is yours. <strong>Implement <code>From</code>, never <code>Into</code>.</strong></p>"}, {"n": 12, "stem": "<p>What does <code>?</code> do to an error before returning it?</p>", "options": [{"key": "A", "text": "Nothing \u2014 it returns it unchanged", "correct": false}, {"key": "B", "text": "Calls <code>From::from</code> on it, converting into the function's error type", "correct": true}, {"key": "C", "text": "Boxes it as <code>Box&lt;dyn Error&gt;</code>", "correct": false}, {"key": "D", "text": "Panics if the types do not match", "correct": false}], "answer": "B", "why": "<p><code>?</code> desugars to a <code>match</code> whose error arm is <code>return Err(From::from(e))</code>. That one call is why a custom error enum with a handful of <code>From</code> impls makes <code>?</code> compose across <code>io::Error</code>, <code>ParseIntError</code> and your own variants with no conversion code at any call site.</p><p>It also explains the error you get when the impl is missing: `<code> </code>?<code> couldn't convert the error to </code>MyError<code> </code><code>, with *the trait bound </code>MyError: From&lt;ParseIntError&gt;` is not satisfied*. The message names the impl you need to write.</p>"}, {"n": 13, "stem": "<p><code>file</code> is a <code>std::fs::File</code>. <code>file.write_all(b\"hi\")</code> fails with <code>E0599</code>, <em>no method named <code>write_all</code></em>. Why?</p>", "options": [{"key": "A", "text": "<code>File</code> does not implement <code>Write</code>", "correct": false}, {"key": "B", "text": "The <code>Write</code> trait is not in scope", "correct": true}, {"key": "C", "text": "<code>write_all</code> needs <code>&amp;mut File</code> and yours is not mutable", "correct": false}, {"key": "D", "text": "<code>File</code> must be wrapped in a <code>BufWriter</code> first", "correct": false}], "answer": "B", "why": "<p>A trait's methods are callable only where the trait is <strong>in scope</strong>. <code>File</code> implements <code>Write</code>, but without <code>use std::io::Write;</code> the method is invisible. This is why real files open with imports for traits whose names never appear again.</p><p>C describes a genuine second error you may hit next (<code>write_all</code> takes <code>&amp;mut self</code>), but it reports as <code>E0596</code>, not <code>E0599</code>. The rule for reading <code>E0599</code>: on a concrete type, suspect a missing <code>use</code> or a typo; on a <span class=\"term\" data-g=\"A name written in angle brackets, such as the T in fn largest&lt;T&gt;, that stands for a type the caller picks rather than one you fixed in advance.\" data-t=\"type parameter\">type parameter</span>, suspect a missing bound.</p>"}, {"n": 14, "stem": "<p>Which are true of <code>dyn Trait</code> compared with a generic bound? Choose all that apply.</p>", "options": [{"key": "A", "text": "One copy of the function is emitted instead of one per type", "correct": true}, {"key": "B", "text": "The call cannot be inlined", "correct": true}, {"key": "C", "text": "Every method call allocates", "correct": false}, {"key": "D", "text": "It can hold a mix of concrete types in one collection", "correct": true}, {"key": "E", "text": "It is always slower than a generic, in every workload", "correct": false}], "answer": "ABD", "why": "<p>A, B and D are the trade. One shared copy, one indirect call through the vtable, and the ability to express <code>Vec&lt;Box&lt;dyn Draw&gt;&gt;</code> \u2014 a mixed collection, which generics simply cannot represent, since <code>Vec&lt;T&gt;</code> needs one <code>T</code>.</p><p>C confuses the pointer with the dispatch. <code>&amp;dyn Trait</code> allocates nothing; only <code>Box&lt;dyn Trait&gt;</code> does, and that is the box, not the call. E overstates it: the indirect call is roughly what a Java method call costs, and thirty monomorphised copies thrashing the instruction cache can lose to one shared copy. What you reliably lose is inlining, and everything the optimiser would have done after it.</p>"}, {"n": 15, "stem": "<p>Which of these traits cannot be <code>#[derive]</code>d?</p>", "options": [{"key": "A", "text": "<code>Debug</code>", "correct": false}, {"key": "B", "text": "<code>Display</code>", "correct": true}, {"key": "C", "text": "<code>Clone</code>", "correct": false}, {"key": "D", "text": "<code>PartialEq</code>", "correct": false}, {"key": "E", "text": "<code>Hash</code>", "correct": false}], "answer": "B", "why": "<p><code>Display</code> is the only one, and the omission is deliberate. <code>Debug</code> can be derived because there is an obvious mechanical answer \u2014 print the type name and its fields. <code>Display</code> is the user-facing form, and nothing in the struct definition says whether <code>Version { major: 1, minor: 4 }</code> should read <code>1.4</code>, <code>1-4</code>, <code>v1.4</code> or <code>version 1 point 4</code>.</p><p>The rule generalises: a trait is derivable when the field-by-field answer is the only sensible one. That is why <code>Clone</code>, <code>PartialEq</code>, <code>Hash</code>, <code>Default</code> and <code>Ord</code> all derive, and why <code>Iterator</code>, <code>Drop</code> and <code>From</code> never can.</p>"}]}