{"unit": "27-no-std", "questions": [{"n": 1, "stem": "<p>Which crate does <code>Vec</code> actually live in?</p>", "options": [{"key": "A", "text": "<code>core</code>", "correct": false}, {"key": "B", "text": "<code>alloc</code>", "correct": true}, {"key": "C", "text": "<code>std</code>, and nowhere else", "correct": false}, {"key": "D", "text": "<code>collections</code>", "correct": false}], "answer": "B", "why": "<p><code>Vec</code> is defined in <code>alloc</code> and re-exported by <code>std</code>. That is why a <code>#![no_std]</code> crate with <code>extern crate alloc;</code> gets <code>Vec</code>, <code>String</code>, <code>Box</code>, <code>Rc</code> and <code>BTreeMap</code> back the moment a global allocator exists.</p><p>C is the intuition almost everyone starts with, and it is why <code>no_std</code> sounds more drastic than it is. <code>std</code> is largely a re-export shell: <code>std::vec::Vec</code> <em>is</em> <code>alloc::vec::Vec</code>, the same type reached by a different path.</p>"}, {"n": 2, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>#![no_std]\nuse std::cmp::max;\n\npub fn floor(n: u16) -&gt; u16 { max(n, 10) }</code></pre></div>", "options": [{"key": "A", "text": "Yes, because <code>cmp</code> does not need an allocator", "correct": false}, {"key": "B", "text": "No, because the name <code>std</code> does not resolve at all", "correct": true}, {"key": "C", "text": "No, because <code>max</code> is not generic over <code>u16</code>", "correct": false}, {"key": "D", "text": "Yes, with a warning", "correct": false}], "answer": "B", "why": "<p><code>error[E0433]: use of unresolved module or unlinked crate std</code>. <code>#![no_std]</code> stops the crate being linked, so the <em>path</em> is dead even though the function is perfectly available.</p><p>A has the right reasoning and the wrong conclusion, which is what makes it tempting. <code>std::cmp::max</code> needs no allocator, and that is exactly why it is <code>core::cmp::max</code> underneath. Change the first path segment and the line compiles unchanged.</p>"}, {"n": 3, "stem": "<p>Which of these are still in scope under <code>#![no_std]</code>? Choose all that apply.</p>", "options": [{"key": "A", "text": "<code>Option</code>, <code>Some</code>, <code>None</code>", "correct": true}, {"key": "B", "text": "<code>Iterator</code> and its adapters", "correct": true}, {"key": "C", "text": "<code>Result</code>, <code>Ok</code>, <code>Err</code>", "correct": true}, {"key": "D", "text": "<code>println!</code>", "correct": false}, {"key": "E", "text": "<code>String</code>", "correct": false}], "answer": "ABC", "why": "<p>The prelude changes from <code>std::prelude</code> to <code>core::prelude</code>, and the difference is smaller than the name suggests: <code>Option</code>, <code>Result</code>, <code>Iterator</code>, <code>Clone</code>, <code>Drop</code>, <code>Into</code> and the operator traits were all <code>core</code> from the start.</p><p>What leaves is what needs a heap or an OS: <code>String</code>, <code>Vec</code> and <code>Box</code> (available again via <code>alloc</code>), and <code>println!</code> and <code>format!</code> (<code>println!</code> needs stdout, and never comes back, while <code>format!</code> returns with <code>alloc</code>).</p>"}, {"n": 4, "stem": "<p>Why is <code>HashMap</code> in <code>std</code> rather than <code>alloc</code>?</p>", "options": [{"key": "A", "text": "It is too large to compile without an OS", "correct": false}, {"key": "B", "text": "Its default hasher seeds itself from the operating system's randomness", "correct": true}, {"key": "C", "text": "It needs threads for its internal locking", "correct": false}, {"key": "D", "text": "It has no <code>no_std</code> implementation anywhere", "correct": false}], "answer": "B", "why": "<p><code>RandomState</code> asks the OS for entropy so that hash keys differ per process, which is a defence against algorithmic complexity attacks. No OS, no entropy, no default hasher, so the type cannot live below <code>std</code>.</p><p>D is wrong in a useful way: <code>hashbrown</code> is the same implementation and works in <code>no_std</code> once you supply a hasher yourself. On a microcontroller the usual answer is <code>BTreeMap</code> from <code>alloc</code>, or a fixed-capacity map from <code>heapless</code> that never allocates.</p>"}, {"n": 5, "stem": "<p>Does <code>f32::sqrt</code> exist in <code>core</code>?</p>", "options": [{"key": "A", "text": "Yes, because floating point arithmetic is all in <code>core</code>", "correct": false}, {"key": "B", "text": "No, because it is implemented by calling the platform's C maths library", "correct": true}, {"key": "C", "text": "No, because floats do not exist at all in <code>no_std</code>", "correct": false}, {"key": "D", "text": "Only on targets with a hardware FPU", "correct": false}], "answer": "B", "why": "<p><code>+</code>, <code>*</code>, comparison, <code>abs</code> and <code>to_bits</code> are in <code>core</code>, because each is an instruction or bit manipulation. <code>sqrt</code>, <code>sin</code>, <code>ln</code> and <code>powf</code> are not: they come from libm, which is part of a hosted platform's runtime, so <code>(x * x + y * y).sqrt()</code> in a <code>no_std</code> crate is <code>error[E0599]</code>.</p><p>Two fixes. Add the <code>libm</code> crate, which is that library rewritten in Rust. Or notice you were only <em>comparing</em> distances. <code>sqrt</code> is monotonic, so comparing the squares gives the identical ordering, exactly, and for free.</p>"}, {"n": 6, "stem": "<p>Why does <code>extern crate alloc;</code> still exist when Rust 2018 removed <code>extern crate</code>?</p>", "options": [{"key": "A", "text": "Backwards compatibility with old crates", "correct": false}, {"key": "B", "text": "<code>alloc</code> ships with the compiler, so there is no <code>Cargo.toml</code> entry to write", "correct": true}, {"key": "C", "text": "It is required by the <code>#[global_allocator]</code> attribute", "correct": false}, {"key": "D", "text": "It only applies inside macros", "correct": false}], "answer": "B", "why": "<p><code>extern crate</code> disappeared because a <code>Cargo.toml</code> dependency is enough to bring a crate into scope. <code>core</code> and <code>alloc</code> have no <code>Cargo.toml</code> entry, because they come with the toolchain, so something has to say \"link this one\". <code>core</code> is linked automatically; <code>alloc</code> is not.</p><p>The reason <code>alloc</code> is opt-in is the interesting part: linking it is a claim that a heap exists. Most <code>no_std</code> crates deliberately do not make that claim, so that they still work on a part with no allocator at all.</p>"}, {"n": 7, "stem": "<p>How many <code>#[panic_handler]</code> functions must a <code>#![no_std]</code> binary have?</p>", "options": [{"key": "A", "text": "Zero, because panics abort by default", "correct": false}, {"key": "B", "text": "Exactly one, anywhere in the dependency graph", "correct": true}, {"key": "C", "text": "One per crate", "correct": false}, {"key": "D", "text": "One per <code>panic!</code> call site", "correct": false}], "answer": "B", "why": "<p><code>panic!</code> has to end up somewhere, and without <code>std</code> there is no default somewhere, so the language requires exactly one and rejects both zero and two.</p><p>In practice you never write it: you add <code>panic-halt</code>, <code>panic-reset</code> or <code>panic-probe</code> as a dependency and choose the behaviour by choosing the crate. Two of them in the graph is a link error, which is a common and confusing first failure when pulling in an unfamiliar dependency.</p>"}, {"n": 8, "stem": "<p>In the target triple <code>thumbv7em-none-eabihf</code>, what does <code>none</code> mean?</p>", "options": [{"key": "A", "text": "No vendor is specified", "correct": false}, {"key": "B", "text": "There is no operating system on the target", "correct": true}, {"key": "C", "text": "No optimisation level has been chosen", "correct": false}, {"key": "D", "text": "The target has no floating-point support", "correct": false}], "answer": "B", "why": "<p>The middle field is the OS, and <code>none</code> means bare metal: no syscalls, no scheduler, no filesystem, which is precisely why <code>std</code> cannot be built for it. Compare <code>x86_64-unknown-linux-gnu</code>, where the field says <code>linux</code>.</p><p>D is a good trap because it is contradicted by the same string: <code>eabihf</code> is the ABI, and the <code>hf</code> is <em>hard float</em>, meaning the part does have an FPU and floats are passed in FPU registers.</p>"}, {"n": 9, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let mut led = Pin::new(13);   // -&gt; Pin&lt;Unconfigured&gt;\nled.set_high();               // set_high is in impl Pin&lt;Output&gt;</code></pre></div>", "options": [{"key": "A", "text": "Yes, because <code>set_high</code> is a method on <code>Pin</code>", "correct": false}, {"key": "B", "text": "No: <code>E0599</code>, because <code>Pin&lt;Unconfigured&gt;</code> does not have that method", "correct": true}, {"key": "C", "text": "No: <code>E0308</code>, because the types of the two lines disagree", "correct": false}, {"key": "D", "text": "Yes, but the pin stays low at runtime", "correct": false}], "answer": "B", "why": "<p><code>impl Pin&lt;Output&gt;</code> is not an impl for <code>Pin</code>. It is an impl for one instantiation, and <code>Pin&lt;Unconfigured&gt;</code> and <code>Pin&lt;Output&gt;</code> are as unrelated as <code>Vec&lt;u8&gt;</code> and <code>Vec&lt;String&gt;</code>. So the method genuinely does not exist on the value you have, which is <code>error[E0599]: no method named set_high found for struct Pin&lt;Unconfigured&gt;</code>.</p><p>D is what the C version does. It drives a pin whose direction register was never written, reads a floating input, and hands you plausible nonsense.</p>"}, {"n": 10, "stem": "<p>What is <code>size_of::&lt;Pin&lt;Output&gt;&gt;()</code> compared to <code>size_of::&lt;Pin&lt;Unconfigured&gt;&gt;()</code>, where the only difference is a <code>PhantomData&lt;STATE&gt;</code> field?</p>", "options": [{"key": "A", "text": "Larger, by one discriminant byte", "correct": false}, {"key": "B", "text": "Larger, by the size of the state type", "correct": false}, {"key": "C", "text": "Identical, because <code>PhantomData</code> is zero-sized", "correct": true}, {"key": "D", "text": "Unspecified; it depends on the optimiser", "correct": false}], "answer": "C", "why": "<p><code>PhantomData&lt;T&gt;</code> occupies no bytes and has no runtime representation. The two types have identical layout and generate identical machine code; the only difference is which <code>impl</code> blocks apply, and that is resolved and then discarded during compilation.</p><p>That is what makes typestate affordable on a chip with 20 kB of RAM. An entire state machine, checked exhaustively, costing zero bytes of flash and zero cycles. And it is the same mechanism as any other generic parameter.</p>"}, {"n": 11, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>use core::cell::Cell;\npub static TICKS: Cell&lt;u32&gt; = Cell::new(0);</code></pre></div>", "options": [{"key": "A", "text": "Yes, because <code>Cell</code> is exactly what a global counter wants", "correct": false}, {"key": "B", "text": "No, because a <code>static</code> must be <code>Sync</code> and <code>Cell</code> is deliberately not", "correct": true}, {"key": "C", "text": "No, because <code>Cell::new</code> is not a <code>const fn</code>", "correct": false}, {"key": "D", "text": "Yes, but only inside an <code>unsafe</code> block", "correct": false}], "answer": "B", "why": "<p>Every <code>static</code> is reachable from everywhere at once, so its type must be <code>Sync</code>. <code>Cell</code>'s whole purpose is mutation without synchronisation, which is sound only while exactly one context can reach it. So it is <code>!Sync</code> on purpose, and the error is <code>Cell&lt;u32&gt; cannot be shared between threads safely</code>.</p><p>\"Threads\" is a misleading word on a microcontroller with none. An interrupt is preemption too: <code>TICKS.set(TICKS.get() + 1)</code> is load, add, store, and a timer interrupt landing between the load and the store silently loses a tick. <code>AtomicU32::fetch_add</code> is one indivisible instruction.</p>"}, {"n": 12, "stem": "<p>An interrupt handler and <code>main</code> both need to push into a shared byte buffer. What is the standard answer?</p>", "options": [{"key": "A", "text": "<code>std::sync::Mutex</code>", "correct": false}, {"key": "B", "text": "<code>critical_section::with</code>, which disables interrupts for the duration", "correct": true}, {"key": "C", "text": "A <code>static mut</code>, accessed in <code>unsafe</code>", "correct": false}, {"key": "D", "text": "<code>Rc&lt;RefCell&lt;_&gt;&gt;</code>", "correct": false}], "answer": "B", "why": "<p><code>std::sync::Mutex</code> is unavailable and would be wrong anyway: it blocks, and blocking inside an interrupt on a chip with no scheduler is a deadlock nothing can break. <code>critical-section</code> disables interrupts on bare metal and takes a real lock on a hosted target, so the same driver compiles for both.</p><p>The <code>cs</code> token it hands your closure is the clever bit: it carries no data and costs nothing, exists only as proof that interrupts are off, and is <em>required</em> to reach the data. The unsound version cannot be written.</p><p>C is what the C code does, and it is where the data races live.</p>"}, {"n": 13, "stem": "<p>Your driver uses <code>AtomicU32::fetch_add</code> and will not build for a Cortex-M0. Why?</p>", "options": [{"key": "A", "text": "<code>core::sync::atomic</code> is not available in <code>no_std</code>", "correct": false}, {"key": "B", "text": "Thumbv6 has no atomic read-modify-write instruction, so that method does not exist there", "correct": true}, {"key": "C", "text": "<code>u32</code> is not the native word size on that part", "correct": false}, {"key": "D", "text": "Atomics require an allocator", "correct": false}], "answer": "B", "why": "<p>Atomic <em>support</em> is per-target, and the standard library only exposes the operations the instruction set can actually perform. Cortex-M0 and M0+ have loads and stores but no LDREX/STREX pair, so <code>AtomicU32</code> exists with <code>load</code> and <code>store</code> while <code>fetch_add</code> does not.</p><p><code>portable-atomic</code> fills the gap by implementing the missing operations with a critical section. This is one of the few places where \"it compiles for my chip\" is genuinely not evidence that it compiles for yours.</p>"}, {"n": 14, "stem": "<p>With <code>defmt</code>, where does the formatting of <code>defmt::info!(\"adc={}\", v)</code> happen?</p>", "options": [{"key": "A", "text": "On the device, then the string is transmitted", "correct": false}, {"key": "B", "text": "On the host, because the device sends an index and the raw argument bytes", "correct": true}, {"key": "C", "text": "Nowhere; <code>defmt</code> only supports literal strings", "correct": false}, {"key": "D", "text": "On the device, but only in release builds", "correct": false}], "answer": "B", "why": "<p>The format strings are extracted from the binary's debug information and held by the host tool. The firmware transmits an index into that table plus the arguments as raw bytes, so a log line costs a handful of bytes on the wire and almost nothing in flash.</p><p>That matters because <code>core::fmt</code> is not small: pulling in the general formatting machinery for a single <code>write!</code> can add several kB, which on a 32 kB part is a real fraction of the budget you were given.</p>"}, {"n": 15, "stem": "<p>On <code>wasm32-unknown-unknown</code>, which of these work? Choose all that apply.</p>", "options": [{"key": "A", "text": "<code>Vec</code> and <code>String</code>", "correct": true}, {"key": "B", "text": "Iterators, <code>Option</code>, <code>Result</code>", "correct": true}, {"key": "C", "text": "<code>std::fs::File</code>", "correct": false}, {"key": "D", "text": "<code>std::thread::spawn</code>", "correct": false}, {"key": "E", "text": "<code>Box&lt;dyn Trait&gt;</code>", "correct": true}], "answer": "ABE", "why": "<p>The <code>unknown</code> OS field means the same thing as <code>none</code>: no syscalls. But the runtime does provide an allocator, so everything in <code>alloc</code> (<code>Vec</code>, <code>String</code>, <code>Box</code>, <code>Rc</code>) works normally, and much of <code>std</code> compiles and merely fails at the points that would need the OS.</p><p>Files and threads are those points. <code>wasm32-wasip1</code> adds a capability-based system interface and gets most of <code>std</code> genuinely working, which is the difference between a module embedded in a page and one run as a program.</p>"}]}