{"unit": "19-modules", "questions": [{"n": 1, "stem": "<p>What is a crate?</p>", "options": [{"key": "A", "text": "A directory containing a <code>Cargo.toml</code>", "correct": false}, {"key": "B", "text": "One unit of compilation \u2014 what <code>rustc</code> is invoked on, once", "correct": true}, {"key": "C", "text": "Any <code>.rs</code> file", "correct": false}, {"key": "D", "text": "A published package on crates.io", "correct": false}], "answer": "B", "why": "<p>A crate is one compilation. <code>rustc</code> is run once per crate, and that is why the crate is the unit of so much else: <code>use</code> paths, <code>pub(crate)</code>, trait coherence, semver, and incremental rebuilds.</p><p>A is a <span class=\"term\" data-g=\"A directory with a Cargo.toml that Cargo can build, test and publish, holding at most one library crate and any number of binary crates.\" data-t=\"package\">package</span> \u2014 one <code>Cargo.toml</code>, which may contain one library crate and any number of binary crates. C is tempting because in Python a file is a module, but a <code>.rs</code> file that no <code>mod</code> declaration names is not compiled at all.</p>"}, {"n": 2, "stem": "<p><code>src/lib.rs</code> contains <code>mod parse;</code>. Where may the body of <code>parse</code> live? Choose all that apply.</p>", "options": [{"key": "A", "text": "<code>src/parse.rs</code>", "correct": true}, {"key": "B", "text": "<code>src/parse/mod.rs</code>", "correct": true}, {"key": "C", "text": "<code>src/modules/parse.rs</code>", "correct": false}, {"key": "D", "text": "Anywhere \u2014 Cargo searches the whole <code>src</code> tree", "correct": false}], "answer": "AB", "why": "<p>Exactly two locations, and having both is an error. <code>src/parse.rs</code> is the modern style; <code>src/parse/mod.rs</code> is the older one and still supported.</p><p>The modern style pairs <code>src/parse.rs</code> with a <code>src/parse/</code> directory for its children, so the module's own code and its submodules sort next to each other, instead of a project accumulating nine files all called <code>mod.rs</code>.</p><p>D is the Python intuition and it is wrong in a way that bites: a file nobody declares is silently ignored \u2014 no error, no warning, no module.</p>"}, {"n": 3, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>mod store {\n    fn secret() -&gt; u32 { 7 }\n    pub mod index {\n        pub fn get() -&gt; u32 { super::secret() }\n    }\n}</code></pre></div>", "options": [{"key": "A", "text": "Yes \u2014 a child module can see its parent's private items", "correct": true}, {"key": "B", "text": "No \u2014 <code>secret</code> is private", "correct": false}, {"key": "C", "text": "No \u2014 <code>super</code> is only valid at the crate root", "correct": false}, {"key": "D", "text": "Yes, but only because <code>index</code> is <code>pub</code>", "correct": false}], "answer": "A", "why": "<p>Private means <em>visible in this module and in every module below it</em>. <code>index</code> is below <code>store</code>, so <code>store</code>'s private <code>secret</code> is in scope there.</p><p>B is the near-universal first guess, and it has the direction backwards. Privacy points downwards: a child can reach up into its parent's internals, which is exactly what makes a private helper shared between submodules possible. A parent cannot reach down into a child.</p>"}, {"n": 4, "stem": "<p>What is the difference between E0433 and E0603?</p>", "options": [{"key": "A", "text": "Nothing \u2014 they are two names for an unresolved path", "correct": false}, {"key": "B", "text": "E0433 means the name does not exist; E0603 means it exists and you may not use it", "correct": true}, {"key": "C", "text": "E0433 is for functions, E0603 is for modules", "correct": false}, {"key": "D", "text": "E0603 is a warning, E0433 is an error", "correct": false}], "answer": "B", "why": "<p>Resolution happens first, permission second. E0433 (<code>failed to resolve: use of undeclared crate or module</code>) means rustc looked in the current module and found nothing of that name. E0603 (<code>... is private</code>) means it found the item and refused.</p><p>Knowing which you have tells you which fix to reach for. E0433 wants a corrected path or a <code>use</code>; E0603 wants a <code>pub</code> \u2014 often on a module partway along the path, not on the item the call names.</p>"}, {"n": 5, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>pub mod store {\n    mod index {\n        pub fn lookup() -&gt; u32 { 7 }\n    }\n}\n\nfn main() { store::index::lookup(); }</code></pre></div>", "options": [{"key": "A", "text": "Yes \u2014 <code>lookup</code> is <code>pub</code>", "correct": false}, {"key": "B", "text": "No \u2014 the module <code>index</code> is private", "correct": true}, {"key": "C", "text": "No \u2014 <code>lookup</code> needs to be <code>pub(crate)</code>", "correct": false}, {"key": "D", "text": "Yes, because <code>store</code> is <code>pub</code>", "correct": false}], "answer": "B", "why": "<p>Reachability is a chain, and every segment of the path has to be visible from where you stand. <code>store</code> is <code>pub</code>, <code>index</code> is not, and the chain breaks at the second segment. Marking <code>lookup</code> as <code>pub</code> opened the last door in a corridor whose second door is locked.</p><p>This is what makes a private module a real implementation detail: mark everything inside it <code>pub</code> for its siblings' convenience and none of it leaks a millimetre further. The way out without publishing <code>index</code> is <code>pub use index::lookup;</code> inside <code>store</code>.</p>"}, {"n": 6, "stem": "<p><code>pub(super)</code> on an item in <code>crate::app::engine::raw</code> makes it visible where?</p>", "options": [{"key": "A", "text": "In <code>crate::app</code> and everything below it", "correct": false}, {"key": "B", "text": "In <code>crate::app::engine</code> and everything below it", "correct": true}, {"key": "C", "text": "In <code>crate::app::engine::raw</code> only", "correct": false}, {"key": "D", "text": "Anywhere in the crate", "correct": false}], "answer": "B", "why": "<p><code>super</code> is relative to where the item is <em>written</em>, not to where you want it read. The item lives in <code>raw</code>, so <code>super</code> is <code>engine</code>, and the grant covers <code>engine</code> and its descendants.</p><p>A is the trap, and it is what people usually mean when they type <code>pub(super)</code> in a deep tree. If you want the whole of <code>app</code>, the form is <code>pub(in crate::app)</code> \u2014 the path must name an ancestor module of the item.</p>"}, {"n": 7, "stem": "<p>Which of these are breaking changes to a published library? Choose all that apply.</p>", "options": [{"key": "A", "text": "Adding a field to a <code>pub struct</code> whose fields are all <code>pub</code>", "correct": true}, {"key": "B", "text": "Adding a variant to a <code>pub enum</code>", "correct": true}, {"key": "C", "text": "Adding a new <code>pub fn</code>", "correct": false}, {"key": "D", "text": "Adding a required method to a <code>pub trait</code>", "correct": true}, {"key": "E", "text": "Renaming a private module", "correct": false}], "answer": "ABD", "why": "<p>A breaks every downstream struct literal, which must name every field. B breaks every downstream <code>match</code> that was exhaustive and now is not. D breaks every downstream <code>impl</code>, which no longer implements the whole trait.</p><p>C is safe unless it collides with a name in a trait the caller has in scope. E is safe because a private module is not part of your API at all \u2014 which is the argument for keeping modules private and re-exporting with <code>pub use</code>.</p><p>A and B are the ones <code>#[non_exhaustive]</code> exists to prevent, and it has to be applied before the fact.</p>"}, {"n": 8, "stem": "<p>What does <code>#[non_exhaustive]</code> on a <code>pub struct</code> do?</p>", "options": [{"key": "A", "text": "Prevents the struct from being constructed at all", "correct": false}, {"key": "B", "text": "Stops downstream crates using a struct literal or an exhaustive pattern, so fields may be added later", "correct": true}, {"key": "C", "text": "Makes every field private", "correct": false}, {"key": "D", "text": "Adds a hidden field", "correct": false}], "answer": "B", "why": "<p>It restricts <em>other</em> crates only. Inside the defining crate the struct behaves normally \u2014 you can still write a literal \u2014 so the type is not made awkward for its own author.</p><p>C is close but wrong in a way that matters: the fields keep their own visibility, so downstream code can still read <code>config.retries</code> if that field is <code>pub</code>. What it cannot do is construct the value with a literal or destructure it without a <code>..</code>, which is exactly the pair of things that would break when you add a field.</p>"}, {"n": 9, "stem": "<p>What does a <code>use</code> statement do?</p>", "options": [{"key": "A", "text": "Creates a local alias for a path in the current module", "correct": true}, {"key": "B", "text": "Compiles the named module into the binary", "correct": false}, {"key": "C", "text": "Loads a file from disk", "correct": false}, {"key": "D", "text": "Marks a dependency as required", "correct": false}], "answer": "A", "why": "<p><code>use</code> is a naming convenience and nothing else. The whole crate is compiled whether you write it or not, and an external dependency is pulled in by <code>Cargo.toml</code>, not by <code>use</code>.</p><p>That it is purely a name is why <code>use</code> is an <em>item</em> with visibility of its own: plain <code>use</code> keeps the alias to yourself, and <code>pub use</code> re-exports the name so anyone who can see your module can reach the item through it.</p>"}, {"n": 10, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>mod json { pub struct Value; }\nmod toml { pub struct Value; }\n\nuse json::Value;\nuse toml::Value;</code></pre></div>", "options": [{"key": "A", "text": "Yes \u2014 the two types are distinct", "correct": false}, {"key": "B", "text": "No \u2014 the name <code>Value</code> is defined twice in this module", "correct": true}, {"key": "C", "text": "No \u2014 you cannot <code>use</code> from a private module", "correct": false}, {"key": "D", "text": "Yes, and the second <code>use</code> shadows the first", "correct": false}], "answer": "B", "why": "<p><code>error[E0252]: the name Value is defined multiple times</code>. The two <em>types</em> are genuinely distinct; the two <em>aliases</em> are not, and a module cannot hold two items of the same name in the same namespace.</p><p>D is the tempting one, because shadowing works for <code>let</code> bindings. It does not work for items: items in a module are unordered, so there is no \"second\" one to win. The fix is <code>as</code> \u2014 <code>use toml::Value as TomlValue;</code> \u2014 or importing the modules and writing <code>json::Value</code> in full.</p>"}, {"n": 11, "stem": "<p>Why does <code>Option</code> need no <code>use</code> but <code>HashMap</code> does?</p>", "options": [{"key": "A", "text": "<code>Option</code> is in the standard prelude, which the compiler injects into every module", "correct": true}, {"key": "B", "text": "<code>Option</code> is a keyword", "correct": false}, {"key": "C", "text": "<code>Option</code> is built into the language, <code>HashMap</code> is a library type", "correct": false}, {"key": "D", "text": "<code>HashMap</code> is in a different crate", "correct": false}], "answer": "A", "why": "<p>The prelude is a small list \u2014 <code>Option</code>, <code>Result</code>, <code>String</code>, <code>Vec</code>, <code>Box</code>, <code>Clone</code>, <code>Iterator</code>, <code>Drop</code> and a few dozen more \u2014 re-exported into every module automatically.</p><p>C is wrong and worth correcting: <code>Option</code> is an ordinary <code>enum</code> defined in the standard library, with no compiler magic beyond the prelude entry. The prelude is kept deliberately small because every name in it is a name you can no longer use for your own type without shadowing it.</p>"}, {"n": 12, "stem": "<p>A workspace shares which of these across its members? Choose all that apply.</p>", "options": [{"key": "A", "text": "One <code>Cargo.lock</code>", "correct": true}, {"key": "B", "text": "One <code>target/</code> directory", "correct": true}, {"key": "C", "text": "One crate root", "correct": false}, {"key": "D", "text": "A single <code>cargo test</code> at the root that runs everything", "correct": true}], "answer": "ABD", "why": "<p>The shared lockfile is the real prize: every member resolves to the same version of every dependency, so a workspace cannot end up with two incompatible <code>serde</code> versions linked into one binary. The shared <code>target/</code> means a dependency built for one member is not rebuilt for the next.</p><p>C is not a thing \u2014 each member is its own package with its own crates. And because crates form a DAG, two members cannot depend on each other cyclically, so a workspace split has to follow a real layering.</p>"}, {"n": 13, "stem": "<p>Why put the logic of a command-line tool in <code>src/lib.rs</code> rather than <code>src/main.rs</code>?</p>", "options": [{"key": "A", "text": "A binary crate cannot be <code>use</code>d, so nothing in <code>main.rs</code> can be reached by an integration test", "correct": true}, {"key": "B", "text": "Binaries compile more slowly than libraries", "correct": false}, {"key": "C", "text": "<code>main.rs</code> cannot contain modules", "correct": false}, {"key": "D", "text": "Cargo will not run tests in a binary crate", "correct": false}], "answer": "A", "why": "<p>A binary crate has no importable name. <code>tests/cli.rs</code> is compiled as its own crate and can <code>use my_tool::...</code> \u2014 the library \u2014 but there is no path by which it could reach anything defined in <code>main.rs</code>.</p><p>D is nearly true and worth being precise about: <code>#[cfg(test)]</code> unit tests inside <code>main.rs</code> do run. What you lose is the integration test, which is the one that exercises your code through the same public API a user has. So <code>main.rs</code> gets argument parsing and exit codes, and everything else lives in the library.</p>"}, {"n": 14, "stem": "<p><code>src/util.rs</code> exists in your package. <code>src/lib.rs</code> does not mention it. What happens?</p>", "options": [{"key": "A", "text": "A compile error \u2014 an undeclared file", "correct": false}, {"key": "B", "text": "A warning about an unused file", "correct": false}, {"key": "C", "text": "Nothing at all; the file is not compiled", "correct": true}, {"key": "D", "text": "It is compiled and available as <code>crate::util</code>", "correct": false}], "answer": "C", "why": "<p><code>mod</code> declares; the filesystem does not. A <code>.rs</code> file that no <code>mod</code> names is inert text as far as <code>rustc</code> is concerned \u2014 no error, no warning, no module.</p><p>This is the most disorienting difference from Python and JavaScript, where the filesystem <em>is</em> the module graph. The symptom is usually \"my new function does not exist\", and the fix is one line \u2014 <code>mod util;</code> \u2014 in the parent.</p>"}, {"n": 15, "stem": "<p>You want a type defined in <code>crate::internal::v2</code> to be reachable by users as <code>my_crate::Client</code>, without exposing <code>internal</code>. What do you write?</p>", "options": [{"key": "A", "text": "<code>pub mod internal;</code> at the root", "correct": false}, {"key": "B", "text": "<code>pub use internal::v2::Client;</code> at the root", "correct": true}, {"key": "C", "text": "Move the file to <code>src/client.rs</code>", "correct": false}, {"key": "D", "text": "<code>#[macro_export]</code> on the type", "correct": false}], "answer": "B", "why": "<p><code>pub use</code> publishes an item at a new path without moving it and without opening the modules above it. <code>internal</code> stays private, so it can be renamed, split, or rewritten, and no user is affected \u2014 nobody ever wrote that path.</p><p>A does the opposite of what you want: it makes every module name in your internal tree part of your semver commitment. This is why a well-designed <code>lib.rs</code> is mostly private <code>mod</code> lines and a short list of <code>pub use</code> lines, and that short list is the API design.</p>"}]}