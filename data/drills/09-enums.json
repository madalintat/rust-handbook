{"unit": "09-enums", "questions": [{"n": 1, "stem": "<p>What is <code>std::mem::size_of::&lt;Shape&gt;()</code> on a 64-bit machine?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>enum Shape {\n    Circle(f64),\n    Rect(f64, f64),\n    Nothing,\n}</code></pre></div>", "options": [{"key": "A", "text": "8, the size of the largest field", "correct": false}, {"key": "B", "text": "17, both fields plus a one-byte tag", "correct": false}, {"key": "C", "text": "24, a tag padded to 8 plus the two-word payload", "correct": true}, {"key": "D", "text": "40, every variant laid out end to end", "correct": false}], "answer": "C", "why": "<p>An enum value must be able to hold any variant, so its size is the largest payload plus room for the discriminant, rounded up to the alignment. Here the biggest payload is <code>Rect</code>'s two <code>f64</code>s (16 bytes), the tag needs one byte, and <code>f64</code> alignment forces the payload to start at offset 8. Total 24.</p><p>D is the tempting one and describes a <em>union of structs</em>, which is what you would hand-build in C. The variants overlap; they are not concatenated.</p>"}, {"n": 2, "stem": "<p>Which of these is true of <code>Option&lt;&amp;T&gt;</code>? Choose all that apply.</p>", "options": [{"key": "A", "text": "It is the same size as <code>&amp;T</code>", "correct": true}, {"key": "B", "text": "<code>None</code> is represented by the all-zero bit pattern", "correct": true}, {"key": "C", "text": "It stores a separate one-byte tag next to the pointer", "correct": false}, {"key": "D", "text": "It requires a heap allocation for the <code>Some</code> case", "correct": false}], "answer": "AB", "why": "<p>A reference is never null, so the bit pattern <code>0</code> is a <span class=\"term\" data-g=\"A bit pattern a type can never legally hold, which the compiler is free to reuse to mean something else, such as the empty case of an Option.\" data-t=\"niche\">niche</span>: a value the payload can never take. The compiler uses it for <code>None</code> and stores no tag at all. <code>Option&lt;&amp;T&gt;</code> is 8 bytes, same as <code>&amp;T</code>.</p><p>This is why making nullability explicit in Rust is free. The same trick applies to <code>Box&lt;T&gt;</code>, <code>&amp;mut T</code>, <code>NonZeroU32</code> and <code>char</code>. It does <em>not</em> apply to <code>Option&lt;u8&gt;</code>, which is 2 bytes, because every bit pattern of a <code>u8</code> is already a legal value.</p>"}, {"n": 3, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>enum Signal { Red, Amber, Green }\n\nfn go(s: Signal) -&gt; bool {\n    match s {\n        Signal::Red =&gt; false,\n        Signal::Green =&gt; true,\n    }\n}</code></pre></div>", "options": [{"key": "A", "text": "Yes, the two cases it lists are the important ones", "correct": false}, {"key": "B", "text": "No, <code>error[E0004]</code>, <code>Signal::Amber</code> is not covered", "correct": true}, {"key": "C", "text": "Yes, and <code>Amber</code> falls through to <code>false</code>", "correct": false}, {"key": "D", "text": "No, match arms cannot return <code>bool</code>", "correct": false}], "answer": "B", "why": "<p><code>match</code> is an expression and must produce a value on every possible input. There is no arm for <code>Amber</code>, so there is no value, and the program is rejected.</p><p>C is the C intuition and the reason this matters. A C <code>switch</code> with no <code>default</code> silently does nothing for the unlisted case; Rust makes it a build failure with a line number. Adding a fourth signal later turns every incomplete match in the codebase into an error naming its file. That is the single most useful refactoring property in the language.</p>"}, {"n": 4, "stem": "<p>What does this print?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let expected = 404;\nlet code = 200;\nlet s = match code {\n    expected =&gt; \"matched\",\n    _ =&gt; \"no\",\n};\nprintln!(\"{s}\");</code></pre></div>", "options": [{"key": "A", "text": "<code>matched</code>, with a warning that the second arm is unreachable", "correct": true}, {"key": "B", "text": "<code>no</code>", "correct": false}, {"key": "C", "text": "It does not compile, since <code>expected</code> is not a constant", "correct": false}, {"key": "D", "text": "<code>matched</code>, with no warning at all", "correct": false}], "answer": "A", "why": "<p>A bare lowercase identifier in a pattern is always a <span class=\"term\" data-g=\"A name introduced by `let` and attached to a value, immutable unless declared `mut` and live until the end of its scope.\" data-t=\"binding\">binding</span>, never a comparison. <code>expected</code> here shadows the outer <code>expected</code>, matches any <code>i32</code>, and binds <code>200</code> to it. The second arm can never be reached, which is what the <code>unreachable_pattern</code> warning says.</p><p>B is what everyone expects the first time and it is the whole trap. To compare against a value you need a <code>const</code> (uppercase by convention, which is why the convention exists) or a guard: <code>n if n == expected</code>.</p>"}, {"n": 5, "stem": "<p>Why do match guards not count toward exhaustiveness?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>match n {\n    x if x &lt; 0 =&gt; \"neg\",\n    x if x &gt;= 0 =&gt; \"non-neg\",\n}   // error[E0004]</code></pre></div>", "options": [{"key": "A", "text": "Because guards run at runtime and patterns at compile time", "correct": false}, {"key": "B", "text": "Because a guard is an arbitrary expression, so deciding whether a set of them is total is undecidable", "correct": true}, {"key": "C", "text": "Because <code>i32</code> has too many values to enumerate", "correct": false}, {"key": "D", "text": "Because the two guards overlap at zero", "correct": false}], "answer": "B", "why": "<p>A guard may call a function, read a global, or ask the clock. Proving that two such expressions between them cover every input is equivalent to solving the halting problem, so rather than special-case simple arithmetic the rule is uniform: an arm with a guard contributes nothing to the exhaustiveness check.</p><p>C is wrong on its own terms: rustc reasons about <code>i32</code> as ranges rather than by enumeration, which is why the error text names <code>i32::MIN..=-1_i32</code> rather than listing four billion values.</p><p>The practical consequence: the arm that closes a match must be unguarded.</p>"}, {"n": 6, "stem": "<p>What is <code>s</code>'s type here?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let msg: Option&lt;String&gt; = Some(String::from(\"hi\"));\nmatch &amp;msg {\n    Some(s) =&gt; { /* here */ }\n    None =&gt; {}\n}</code></pre></div>", "options": [{"key": "A", "text": "<code>String</code>", "correct": false}, {"key": "B", "text": "<code>&amp;String</code>", "correct": true}, {"key": "C", "text": "<code>&amp;Option&lt;String&gt;</code>", "correct": false}, {"key": "D", "text": "It does not compile, since the pattern is not a reference pattern", "correct": false}], "answer": "B", "why": "<p>This is <span class=\"term\" data-g=\"The rule that matching a reference against an ordinary pattern quietly borrows instead of failing, so every name the pattern binds comes out as a reference and nothing is moved.\" data-t=\"match ergonomics\">match ergonomics</span>. The scrutinee is <code>&amp;Option&lt;String&gt;</code> and the pattern <code>Some(s)</code> is not a reference pattern, so instead of rejecting it the compiler dereferences the scrutinee and shifts the <span class=\"term\" data-g=\"Whether the names introduced by a pattern take the matched data itself or a reference to it, which the compiler chooses based on whether you matched on a value or on a reference.\" data-t=\"binding mode\">binding mode</span> to by-reference. Every binding underneath comes out borrowed: <code>s: &amp;String</code>.</p><p>D was the actual behaviour before Rust 2018, when you had to write <code>Some(ref s)</code> to get the same thing. <code>ref</code> still exists and is almost never needed now.</p>"}, {"n": 7, "stem": "<p>Which of these move the <code>String</code> out of <code>msg</code>? Choose all that apply.</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let msg: Option&lt;String&gt; = Some(String::from(\"hi\"));</code></pre></div>", "options": [{"key": "A", "text": "<code>match msg { Some(s) =&gt; s, None =&gt; String::new() }</code>", "correct": true}, {"key": "B", "text": "<code>match &amp;msg { Some(s) =&gt; s.clone(), None =&gt; String::new() }</code>", "correct": false}, {"key": "C", "text": "<code>if let Some(s) = &amp;msg { s.len() } else { 0 }</code>", "correct": false}, {"key": "D", "text": "<code>msg.unwrap_or_default()</code>", "correct": true}], "answer": "AD", "why": "<p>A and D take the value by value: matching on <code>msg</code> itself binds <code>s</code> as an owned <code>String</code>, and <code>unwrap_or_default</code> takes <code>self</code>. Both leave <code>msg</code> moved-from.</p><p>B and C match on <code>&amp;msg</code>, so the binding mode is by-reference and nothing moves: <code>s</code> is a <code>&amp;String</code> in both.</p><p>The pattern to internalise: what moves is decided by the <em>scrutinee</em>, not by the pattern. One <code>&amp;</code> in front of it changes every binding underneath.</p>"}, {"n": 8, "stem": "<p>Does this compile?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>enum Event { Click { x: i32, y: i32 }, Key(char) }\n\nfn describe(e: &amp;Event) -&gt; String {\n    match e {\n        Event::Click { x } =&gt; format!(\"click {x}\"),\n        Event::Key(c) =&gt; format!(\"key {c}\"),\n    }\n}</code></pre></div>", "options": [{"key": "A", "text": "Yes, extra fields are ignored", "correct": false}, {"key": "B", "text": "No, <code>error[E0027]</code>, the pattern does not mention field <code>y</code>", "correct": true}, {"key": "C", "text": "No, <code>error[E0004]</code>, non-exhaustive", "correct": false}, {"key": "D", "text": "Yes, and <code>y</code> is bound to its default value", "correct": false}], "answer": "B", "why": "<p>A struct-variant pattern must account for every field, either by naming it or by writing <code>..</code>. <code>Event::Click { x }</code> mentions one of two, so it is rejected with <code>pattern does not mention field y</code>.</p><p>A is the tempting answer because destructuring in JavaScript and Python does work that way. Rust makes you write <code>Event::Click { x, .. }</code> instead: two extra characters meaning \"and I know there are others\". Adding a field later then breaks only the patterns that did <em>not</em> opt out, which is usually exactly the set you want to revisit.</p>"}, {"n": 9, "stem": "<p>Which of these <code>let else</code> blocks compile? Choose all that apply.</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn f(x: Option&lt;u16&gt;) -&gt; u16 {\n    let Some(n) = x else { /* HERE */ };\n    n\n}</code></pre></div>", "options": [{"key": "A", "text": "<code>return 0;</code>", "correct": true}, {"key": "B", "text": "<code>0</code>", "correct": false}, {"key": "C", "text": "<code>panic!(\"no value\")</code>", "correct": true}, {"key": "D", "text": "<code>std::process::exit(1)</code>", "correct": true}, {"key": "E", "text": "<code>n = 0;</code>", "correct": false}], "answer": "ACD", "why": "<p>The <code>else</code> block must have type <code>!</code>, meaning it must not finish. After the statement <code>n</code> has to be in scope and bound, and the only way that can be guaranteed is if the failing path never reaches the next line. <code>return</code>, <code>panic!</code>, <code>exit</code>, <code>break</code> and <code>continue</code> all qualify.</p><p>B produces a <code>u16</code>, which is a perfectly good value and precisely what is not allowed: <code>else clause of let...else does not diverge</code>.</p><p>E is a nice distractor: you cannot assign to <code>n</code>, because <code>n</code> does not exist yet. That is the whole reason for the rule.</p>"}, {"n": 10, "stem": "<p>What does this evaluate to?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let code = 418;\nlet s = match code {\n    n @ 200..=299 =&gt; format!(\"ok {n}\"),\n    n @ 400..=499 =&gt; format!(\"client {n}\"),\n    _ =&gt; \"other\".to_string(),\n};</code></pre></div>", "options": [{"key": "A", "text": "<code>\"other\"</code>", "correct": false}, {"key": "B", "text": "<code>\"client 400\"</code>", "correct": false}, {"key": "C", "text": "<code>\"client 418\"</code>", "correct": true}, {"key": "D", "text": "It does not compile, since <code>@</code> cannot be used with a range", "correct": false}], "answer": "C", "why": "<p><code>n @ 400..=499</code> tests the value against the range <strong>and</strong> binds the matched value to <code>n</code>. Without the <code>@</code>, a range pattern throws the value away and you have nothing to interpolate.</p><p>B is the trap: <code>n</code> is the value that matched, not the start of the range. The binding sees <code>418</code>.</p><p><code>@</code> composes with any pattern, not only ranges: <code>msg @ Message::Quit</code> binds the whole enum value while still testing its variant.</p>"}, {"n": 11, "stem": "<p>Which of these can share one match arm via <code>|</code>?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>enum Shape { Circle(f64), Square(f64), Rect(f64, f64) }</code></pre></div>", "options": [{"key": "A", "text": "<code>Shape::Circle(r) | Shape::Square(r)</code>", "correct": true}, {"key": "B", "text": "<code>Shape::Circle(r) | Shape::Rect(w, h)</code>", "correct": false}, {"key": "C", "text": "<code>Shape::Circle(r) | Shape::Rect(r, _)</code> where the arm uses <code>r</code> as an <code>f64</code>", "correct": false}, {"key": "D", "text": "<code>Shape::Circle(_) | Shape::Rect(w, _)</code>", "correct": false}], "answer": "A", "why": "<p>Every alternative in an or-pattern must bind <strong>the same set of names at the same types</strong>, because the arm body is one piece of code compiled once. A binds <code>r: &amp;f64</code> on both sides, so it is fine.</p><p>B fails with <code>error[E0408]: variable r is not bound in all patterns</code>, and <code>w</code> and <code>h</code> are not bound in the first. D fails the same way for <code>w</code>.</p><p>C is the interesting one: it is legal. Nothing requires the field to be in the same position or to have the same name, only that the binding's name and type agree. That flexibility is also why a typo like <code>Rect(w, _)</code> when you meant <code>Rect(r, _)</code> is caught rather than silently doing something else.</p>"}, {"n": 12, "stem": "<p>Adding a variant to a public enum breaks downstream <code>match</code>es. Which of these is the <em>intended</em> way for a library to opt out of that?</p>", "options": [{"key": "A", "text": "Always include <code>_ =&gt; {}</code> in the library's own matches", "correct": false}, {"key": "B", "text": "Mark the enum <code>#[non_exhaustive]</code>, which forces downstream matches to have a wildcard", "correct": true}, {"key": "C", "text": "Use a struct with a tag field instead", "correct": false}, {"key": "D", "text": "There is no way, since enums in public APIs can never gain variants", "correct": false}], "answer": "B", "why": "<p><code>#[non_exhaustive]</code> tells other crates \"this list will grow\", and the compiler then <em>requires</em> a <code>_</code> arm in any match on it outside the defining crate. Adding a variant later is then a non-breaking change. <code>io::ErrorKind</code> is the canonical example.</p><p>A does nothing for downstream users, because the library's own matches are not the problem. And note the asymmetry: inside the defining crate, matches on a <code>#[non_exhaustive]</code> enum are still checked exhaustively, so the author still gets the compile errors. You only give up the guarantee at the crate boundary, which is exactly where you wanted to.</p>"}, {"n": 13, "stem": "<p><code>Option&lt;T&gt;</code> and <code>Result&lt;T, E&gt;</code> are\u2026</p>", "options": [{"key": "A", "text": "Compiler built-ins with special syntax support", "correct": false}, {"key": "B", "text": "Ordinary enums defined in the standard library, in ordinary Rust", "correct": true}, {"key": "C", "text": "Structs with a hidden boolean flag", "correct": false}, {"key": "D", "text": "Traits implemented by every fallible type", "correct": false}], "answer": "B", "why": "<p>Both are two-variant enums you could have written yourself:</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>enum Option&lt;T&gt; { Some(T), None }\nenum Result&lt;T, E&gt; { Ok(T), Err(E) }</code></pre></div><p>There is no magic in the type. What the language adds around them is convenience (<code>?</code>, the <code>Try</code> trait, the prelude importing their variants unqualified) plus the layout optimisations any enum gets. Understanding that they are just enums is what makes <code>match</code>, <code>if let</code> and the combinator methods on them stop feeling like special cases.</p>"}, {"n": 14, "stem": "<p>Why does this state machine take <code>state</code> by value rather than <code>&amp;mut self</code>?</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>fn step(state: Connection, ev: Event) -&gt; Connection { /* ... */ }</code></pre></div>", "options": [{"key": "A", "text": "It is faster, with no pointer indirection", "correct": false}, {"key": "B", "text": "Consuming the old state makes using a stale state a compile error", "correct": true}, {"key": "C", "text": "It lets an arm move a field out of the old state into the new one", "correct": true}, {"key": "D", "text": "<code>&amp;mut self</code> cannot be used in a <code>match</code>", "correct": false}], "answer": "BC", "why": "<p>B is the design argument. Once <code>step</code> has consumed the old <code>Connection</code>, naming it afterwards is <code>error[E0382]</code>. The bug \"we read the state we just replaced\" becomes unwriteable.</p><p>C is the practical one: <code>(Connection::Live { socket, .. }, Event::Idle) =&gt;</code> can move the <code>TcpStream</code> straight into the next state, with no clone and no <code>Option</code> dance. Through a <code>&amp;mut self</code> you would need <code>std::mem::replace</code> or <code>Option::take</code> to get the same effect.</p><p>A is wrong in general: if the enum has a fat variant, moving it by value copies the whole thing on every transition.</p>"}, {"n": 15, "stem": "<p>A <code>Job</code> is either queued, running with a worker id, or finished with an exit code. Which representation makes the invalid states unrepresentable?</p>", "options": [{"key": "A", "text": "<code>struct Job { queued: bool, running: bool, worker: u32, exit_code: i32 }</code>", "correct": false}, {"key": "B", "text": "<code>struct Job { state: u8, worker: Option&lt;u32&gt;, exit_code: Option&lt;i32&gt; }</code>", "correct": false}, {"key": "C", "text": "<code>enum Job { Queued, Running { worker: u32 }, Finished { exit_code: i32 } }</code>", "correct": true}, {"key": "D", "text": "<code>struct Job { state: String, data: Vec&lt;u8&gt; }</code>", "correct": false}], "answer": "C", "why": "<p>C has exactly three values' worth of shape, and each carries precisely the data that state needs. There is no way to build a <code>Queued</code> job with a worker id, because there is no syntax for it.</p><p>A allows <code>queued &amp;&amp; running</code>, plus a <code>worker</code> that means nothing when queued: eight combinations, of which three are legal. B is better but still lets <code>state = 1</code> pair with <code>worker: None</code>, and the mapping from <code>1</code> to \"running\" lives in your head.</p><p>The heuristic: a <code>bool</code> next to two <code>Option</code>s where only some combinations are legal is an enum trying to get out. Once it is an enum, the invalid combinations stop being something you have to write tests for.</p>"}]}