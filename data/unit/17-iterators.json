{"slug": "17-iterators", "num": 17, "title": "Iterators", "blurb": "One required method buys about seventy free ones, none of which run until something asks for an element.", "concepts": ["iterator", "next", "laziness", "adapter", "consumer", "into_iter", "collect", "turbofish", "fuse", "zero-cost abstraction"], "needs": ["05-ownership", "14-traits", "16-closures"], "words": 1675, "mins": 7, "toc": [{"id": "one-required-method", "text": "One required method", "level": 2}, {"id": "the-whole-trait", "text": "The whole trait", "level": 3}, {"id": "for-is-next-in-a-loop", "text": "`for` is `next` in a loop", "level": 3}, {"id": "nothing-runs-until-something-asks", "text": "Nothing runs until something asks", "level": 2}, {"id": "the-adapter-that-did-not-adapt", "text": "The adapter that did not adapt", "level": 3}, {"id": "one-element-at-a-time-not-one-stage-at-a-time", "text": "One element at a time, not one stage at a time", "level": 3}, {"id": "the-three-ways-in", "text": "The three ways in", "level": 2}, {"id": "adapters-and-consumers", "text": "Adapters and consumers", "level": 2}, {"id": "adapters-worth-knowing", "text": "Adapters worth knowing", "level": 3}, {"id": "consumers", "text": "Consumers", "level": 3}, {"id": "collect-and-the-one-that-surprises-people", "text": "collect, and the one that surprises people", "level": 2}, {"id": "it-is-generic-over-its-return-type", "text": "It is generic over its return type", "level": 3}, {"id": "collect", "text": "`collect::<Result<Vec<_>, _>>()`", "level": 3}, {"id": "writing-your-own", "text": "Writing your own", "level": 2}, {"id": "the-zero-cost-claim", "text": "The zero-cost claim", "level": 2}], "lead": "<p class=\"lede\"><code>Iterator</code> is the best argument for traits in the whole standard library, and the argument is arithmetic. The trait requires <strong>one</strong> method. It gives back about seventy. Write <code>next</code> for your type and <code>map</code>, <code>filter</code>, <code>zip</code>, <code>sum</code>, <code>rev</code> and <code>collect</code> arrive with it, already written, already fast.</p><p>The second fact is the one people trip over: calling an adapter does nothing at all.</p>", "parts": [{"id": "one-required-method", "title": "One required method", "intro": "", "subs": [{"id": "the-whole-trait", "text": "The whole trait", "html": "<div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>pub trait Iterator {\n    type Item;\n\n    fn next(&amp;mut self) -&gt; Option&lt;Self::Item&gt;;\n\n    // ~70 more, every one with a default body written in terms of next\n}</code></pre></div><p><code>next</code> advances the iterator and yields <code>Some(item)</code>, or <code>None</code> when it is finished. That is the entire contract. <code>map</code>, <code>filter</code>, <code>sum</code>, <code>fold</code>, <code>collect</code> are <strong>default methods</strong> \u2014 code that already exists in the trait, calling <code>next</code> through <code>Self</code>. Implement <code>next</code> and you inherit all of it without writing a line.</p><div class=\"callout\"><div class=\"ct\">Note</div><p>This is what a trait with default methods is for. One required method, seventy free ones, monomorphised per type so none of them cost anything at run time.</p></div>", "words": 112, "mins": 1}, {"id": "for-is-next-in-a-loop", "text": "`for` is `next` in a loop", "html": "<div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>for line in text.lines() {\n    println!(\"{line}\");\n}</code></pre></div><p>is sugar for approximately</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let mut it = IntoIterator::into_iter(text.lines());\nwhile let Some(line) = it.next() {\n    println!(\"{line}\");\n}</code></pre></div><p>There is no separate iteration protocol in the language and no special case in the compiler. <code>for</code> calls one trait method until it returns <code>None</code>, which is why anything implementing <code>Iterator</code> works in a <code>for</code> loop with no registration anywhere.</p>", "words": 67, "mins": 1}], "words": 179, "mins": 1}, {"id": "nothing-runs-until-something-asks", "title": "Nothing runs until something asks", "intro": "", "subs": [{"id": "the-adapter-that-did-not-adapt", "text": "The adapter that did not adapt", "html": "<div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let names = vec![\"ada\", \"grace\"];\nnames.iter().map(|n| expensive(n));   // warning: unused `Map` that must be used</code></pre></div><p><code>expensive</code> is never called. Not once. <code>map</code> does not map \u2014 it <em>returns a struct</em> called <code>Map</code> holding the source iterator and the closure, and that struct sits there doing nothing until somebody calls <code>next</code> on it. Every adapter is like this, which is why they all carry <code>#[must_use]</code>: dropping one on the floor is always a bug, and it is a common enough bug to be worth a warning.</p><div class=\"memory\"><div class=\"mt\">an adapter chain is a nested struct</div><pre>     v.iter().filter(p).map(f)\n\n     \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n     \u2502 Map { f }                        \u2502  \u25c0\u2500\u2500 .next() asked here\n     \u2502   \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510   \u2502\n     \u2502   \u2502 Filter { p }             \u2502   \u2502      each layer calls\n     \u2502   \u2502   \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510   \u2502   \u2502      next() on the one inside\n     \u2502   \u2502   \u2502 Iter { ptr, end }\u2502   \u2502   \u2502\n     \u2502   \u2502   \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518   \u2502   \u2502\n     \u2502   \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518   \u2502\n     \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n     three words on the stack. No Vec, no allocation, no intermediate list.</pre></div>", "words": 161, "mins": 1}, {"id": "one-element-at-a-time-not-one-stage-at-a-time", "text": "One element at a time, not one stage at a time", "html": "<div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let n = (1..=3)\n    .map(|x| { println!(\"map {x}\"); x * 2 })\n    .filter(|x| { println!(\"filter {x}\"); *x &gt; 2 })\n    .count();</code></pre></div><p>prints <code>map 1</code>, <code>filter 2</code>, <code>map 2</code>, <code>filter 4</code>, <code>map 3</code>, <code>filter 6</code>. Each element travels the entire chain before the next one is pulled. The stages are interleaved because there is nowhere to buffer a stage \u2014 no intermediate collection exists.</p><div class=\"callout compare\"><div class=\"ct\">Coming from elsewhere</div><p><strong>Python</strong> \u2014 a list comprehension <code>[f(x) for x in xs]</code> builds the whole list; <code>map</code>/<code>filter</code>/generators are lazy like Rust's. <strong>Java</strong> streams and <strong>C++20</strong> ranges match Rust closely. <strong>Go</strong> has neither, so the loop is written out.</p><p>The consequence is practical, not aesthetic: <code>v.iter().map(parse).find(ok)</code> on a million-element vector calls <code>parse</code> until the first hit and then stops. The eager version parses a million things and throws away 999,999 of them.</p></div>", "words": 145, "mins": 1}], "words": 306, "mins": 1}, {"id": "the-three-ways-in", "title": "The three ways in", "intro": "<table><thead><tr><th>call</th><th>yields</th><th>what happens to the collection</th></tr></thead><tbody><tr><td><code>v.iter()</code></td><td><code>&amp;T</code></td><td>borrowed \u2014 <code>v</code> is usable afterwards</td></tr><tr><td><code>v.iter_mut()</code></td><td><code>&amp;mut T</code></td><td>borrowed uniquely \u2014 you may write through it</td></tr><tr><td><code>v.into_iter()</code></td><td><code>T</code></td><td>consumed \u2014 <code>v</code> is moved and gone</td></tr></tbody></table><p>That is ownership from unit 05 with nothing added. An iterator is just another value that holds either a borrow or the thing itself.</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>for s in &amp;v      { }   // iter()      \u2014 s: &amp;String,     v survives\nfor s in &amp;mut v  { }   // iter_mut()  \u2014 s: &amp;mut String, v survives\nfor s in v       { }   // into_iter() \u2014 s: String,      v is moved</code></pre></div><p><code>for</code> reaches its decision by calling <code>IntoIterator::into_iter</code> on whatever it was given, and <code>&amp;Vec&lt;T&gt;</code>, <code>&amp;mut Vec&lt;T&gt;</code> and <code>Vec&lt;T&gt;</code> are three different types with three different impls.</p><div class=\"callout gotcha\"><div class=\"ct\">Gotcha</div><p><code>into_iter</code> on a <em>reference</em> does not give you owned items. <code>(&amp;v).into_iter()</code> yields <code>&amp;T</code>, because the impl it selects is the one on <code>&amp;Vec&lt;T&gt;</code>.</p><p>This bites in generic code: a function taking <code>impl IntoIterator&lt;Item = String&gt;</code> will not accept <code>&amp;v</code>, and the error talks about <code>&amp;String</code> where <code>String</code> was expected. The name <code>into_</code> describes only one of the three impls.</p></div>", "subs": [], "words": 192, "mins": 1}, {"id": "adapters-and-consumers", "title": "Adapters and consumers", "intro": "", "subs": [{"id": "adapters-worth-knowing", "text": "Adapters worth knowing", "html": "<table><thead><tr><th>adapter</th><th>yields</th><th>the thing to remember</th></tr></thead><tbody><tr><td><code>map(f)</code></td><td><code>f(x)</code></td><td></td></tr><tr><td><code>filter(p)</code></td><td>items where <code>p</code> holds</td><td><code>p</code> receives <code>&amp;Item</code>, so on <code>.iter()</code> that is <code>&amp;&amp;T</code></td></tr><tr><td><code>filter_map(f)</code></td><td>the <code>Some</code>s of <code>f(x)</code></td><td>one pass instead of <code>filter().map()</code></td></tr><tr><td><code>enumerate()</code></td><td><code>(usize, x)</code></td><td>counts the <em>iterator</em>, so after <code>skip(3)</code> it starts at 0</td></tr><tr><td><code>zip(other)</code></td><td><code>(a, b)</code></td><td>stops when the shorter one does</td></tr><tr><td><code>take(n)</code> / <code>skip(n)</code></td><td>prefix / suffix</td><td></td></tr><tr><td><code>take_while(p)</code> / <code>skip_while(p)</code></td><td>up to / from the first failure</td><td>not <code>filter</code></td></tr><tr><td><code>chain(other)</code></td><td>all of one, then all of the other</td><td></td></tr><tr><td><code>flatten()</code></td><td>the items of the items</td><td>flattens <code>Option</code> and <code>Result</code> too</td></tr><tr><td><code>flat_map(f)</code></td><td><code>map(f).flatten()</code></td><td></td></tr><tr><td><code>rev()</code></td><td>back to front</td><td>needs <code>DoubleEndedIterator</code></td></tr><tr><td><code>peekable()</code></td><td>the same items, plus <code>peek()</code></td><td>how hand-written parsers are shaped</td></tr></tbody></table><div class=\"callout gotcha\"><div class=\"ct\">Gotcha</div><p><code>take_while</code> is not <code>filter</code>.</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let v = [1, 5, 2];\nv.iter().take_while(|n| **n &lt; 3).count();   // 1 \u2014 the 5 ends it, 2 is never seen\nv.iter().filter(|n| **n &lt; 3).count();       // 2</code></pre></div><p><code>take_while</code> also <em>consumes</em> the element that failed. It has already been pulled out of the source iterator and is not there for anyone else.</p></div><p><code>windows</code> and <code>chunks</code> are slice methods, not adapters, because they need to look at contiguous memory that a general iterator does not have:</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let temps = [3, 7, 4, 9];\nlet rises = temps.windows(2).filter(|w| w[1] &gt; w[0]).count();   // 2 \u2014 overlapping\nlet pairs = temps.chunks(2);                                     // [3,7] [4,9] \u2014 not</code></pre></div>", "words": 223, "mins": 1}, {"id": "consumers", "text": "Consumers", "html": "<p>Something has to call <code>next</code>, and that something is a consumer.</p><table><thead><tr><th>consumer</th><th>gives back</th></tr></thead><tbody><tr><td><code>collect()</code></td><td>anything implementing <code>FromIterator</code></td></tr><tr><td><code>sum()</code> / <code>product()</code></td><td>one number</td></tr><tr><td><code>fold(init, f)</code></td><td>one value \u2014 the operation the rest are built from</td></tr><tr><td><code>count()</code> / <code>last()</code> / <code>nth(n)</code></td><td></td></tr><tr><td><code>any(p)</code> / <code>all(p)</code> / <code>find(p)</code> / <code>position(p)</code></td><td>short-circuiting</td></tr><tr><td><code>min_by_key(f)</code> / <code>max_by_key(f)</code></td><td>an <code>Option</code></td></tr><tr><td><code>partition(p)</code></td><td>two collections</td></tr><tr><td><code>for_each(f)</code></td><td><code>()</code></td></tr></tbody></table><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let total = v.iter().fold(0, |acc, x| acc + x);   // sum(), written out longhand</code></pre></div><div class=\"callout gotcha\"><div class=\"ct\">Gotcha</div><p><code>any</code>, <code>all</code>, <code>find</code> and <code>position</code> take <code>&amp;mut self</code>, not <code>self</code>. They stop at the answer and leave the iterator sitting exactly where they stopped:</p><div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let mut it = \"key=value\".chars();\nlet ok = it.any(|c| c == '=');       // true, and '=' has been consumed\nlet rest: String = it.collect();     // \"value\"</code></pre></div><p>That is a feature when you meant it and a very confusing bug when you did not.</p></div>", "words": 147, "mins": 1}], "words": 370, "mins": 2}, {"id": "collect-and-the-one-that-surprises-people", "title": "collect, and the one that surprises people", "intro": "", "subs": [{"id": "it-is-generic-over-its-return-type", "text": "It is generic over its return type", "html": "<div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let v: Vec&lt;i32&gt;      = (1..4).collect();\nlet s: String        = vec!['h', 'i'].into_iter().collect();\nlet m: HashMap&lt;_, _&gt; = vec![(\"a\", 1)].into_iter().collect();\nlet n                = (1..4).collect::&lt;Vec&lt;i32&gt;&gt;();   // turbofish instead</code></pre></div><p>Inference normally flows inwards from arguments. <code>collect</code> has no arguments, so the target type has to come from the annotation on the binding or from a <span class=\"term\" data-g=\"The ::&lt;Type&gt; syntax for supplying type arguments explicitly where inference cannot decide, as in parse::&lt;i32&gt;().\" data-t=\"turbofish\">turbofish</span>. <code>error[E0282]: type annotations needed</code> on a <code>collect</code> line always means this and never means anything else.</p>", "words": 70, "mins": 1}, {"id": "collect", "text": "`collect::<Result<Vec<_>, _>>()`", "html": "<div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>let nums: Result&lt;Vec&lt;i32&gt;, _&gt; =\n    [\"1\", \"2\", \"x\", \"4\"].iter().map(|s| s.parse::&lt;i32&gt;()).collect();\n// Err(ParseIntError { .. })  \u2014 and \"4\" was never parsed</code></pre></div><p>An iterator of <code>Result&lt;T, E&gt;</code> collects into <code>Result&lt;Vec&lt;T&gt;, E&gt;</code>. All <code>Ok</code> gives the vector; the first <code>Err</code> is returned immediately and the rest of the iterator is never advanced. One method replaces a loop, a <code>mut</code> vector, a <code>match</code> and an early <code>return</code> \u2014 and it short-circuits, which the naive loop usually forgets to.</p><p>The same impl exists for <code>Option</code>, and for every target collection, so <code>Result&lt;HashMap&lt;_, _&gt;, E&gt;</code> works identically.</p>", "words": 95, "mins": 1}], "words": 165, "mins": 1}, {"id": "writing-your-own", "title": "Writing your own", "intro": "<div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>struct Fib { a: u64, b: u64 }\n\nimpl Iterator for Fib {\n    type Item = u64;\n\n    fn next(&amp;mut self) -&gt; Option&lt;u64&gt; {\n        let out = self.a;\n        self.a = self.b;\n        self.b = out + self.b;\n        Some(out)\n    }\n}\n\nlet first_ten: Vec&lt;u64&gt; = Fib { a: 0, b: 1 }.take(10).collect();</code></pre></div><p>Nine lines, and <code>Fib</code> now has <code>take</code>, <code>zip</code>, <code>sum</code>, <code>skip_while</code>, <code>collect</code> and the rest of the seventy. It never returns <code>None</code>, which is fine: an infinite iterator is only a problem if something consumes all of it, and laziness means nothing does until you say so.</p><div class=\"callout gotcha\"><div class=\"ct\">Gotcha</div><p>Returning <code>None</code> is not a promise. The trait permits an iterator to yield <code>Some</code> again afterwards, and some deliberately do. Adapters that need the guarantee call <code>.fuse()</code>, which latches <code>None</code> permanently.</p><p>Make your own <code>next</code> stay <code>None</code>. Do not assume anyone else's does.</p></div>", "subs": [], "words": 147, "mins": 1}, {"id": "the-zero-cost-claim", "title": "The zero-cost claim", "intro": "<div class=\"codeblock \" data-lang=\"rust\"><div class=\"cb-head\"><span>rust</span></div><pre><code>(0..n).filter(|x| x % 3 == 0).map(|x| x * x).sum::&lt;u64&gt;()</code></pre></div><p>compiles to the same machine code as the <code>for</code> loop with an <code>if</code> in it. That is worth justifying rather than asserting, and the justification is three steps:</p><ol><li><strong>The chain is a value, not a pipeline.</strong> <code>Sum&lt;Map&lt;Filter&lt;Range&gt;&gt;&gt;</code> is one struct built from three, entirely on the stack. No allocation, no channel, no intermediate <code>Vec</code>.</li><li><strong>Every call is statically known.</strong> The closures are unique unnameable types, so <code>Filter::next</code> calls <em>this</em> predicate, not a function pointer. There is no vtable and nothing to guess.</li><li><strong>So it all inlines.</strong> <code>Range::next</code> into <code>Filter::next</code> into <code>Map::next</code> into the loop inside <code>sum</code>. What is left after inlining is a counter, a modulo, a multiply and an add.</li></ol><p>The loop you would have written, arrived at by inlining rather than by trust.</p><p>Often it is faster than the index loop, for a reason worth knowing: <code>for i in 0..v.len() { v[i] }</code> performs a bounds check on every access, because the compiler must prove <code>i</code> is in range at each one. <code>for x in &amp;v</code> performs none \u2014 the iterator holds the end pointer and cannot walk off it by construction.</p><div class=\"callout\"><div class=\"ct\">Note</div><p>The costs are real but they are not run time. A chain of eight adapters is a type eight layers deep, so a mistake in the middle prints all eight in the error, and the compiler has more to inline. You pay in compile time and in the width of your diagnostics.</p></div>", "subs": [], "words": 249, "mins": 1}]}